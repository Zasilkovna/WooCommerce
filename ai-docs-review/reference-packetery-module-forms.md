## ai-docs/reference/packetery-module-forms.md @ b34fe03c — 2026-09-22

Verified against the code in the working tree of branch `docs/teams` at HEAD `a6ef312a`. `git diff b34fe03c..a6ef312a -- src/Packetery/Module/Forms` is empty, so the code is identical to the documented commit. All paths below are relative to the repository root; `Forms/` stands for `src/Packetery/Module/Forms/`. The three counts of line 25 were measured by the convention of the earlier rounds: `find src/Packetery/Module/Forms -name '*.php' | wc -l` = 9, non-blank non-comment lines of those files = 1066, `grep -cE '^\s*(final )?public( static)? function'` summed over them = 37. No test file enters the counts.

| line | claim | class | evidence |
|---|---|---|---|
| 17-18 | The module builds the admin forms of the plugin and validates what the administrator writes into them | CONFIRMED | eight form classes in `Forms/`; validation in `Forms/CarrierFormFactory.php:439` `validateOptions` and `Forms/ShippingClassFormFactory.php:148` |
| 18-20 | The largest form is the carrier settings form; it holds the price tables, the limits and the payment rules of one carrier | CONFIRMED | `Forms/CarrierFormFactory.php` is the largest file of the module (569 lines); `createForm` adds `weight_limits`, `product_value_limits`, `surcharge_limits` and `disallowed_checkout_payment_methods` (`:327-331`) |
| 20-23 | The module also builds the per class form, the currency rates form, the log filter, the diagnostic logging switch, the stored until form and the bug report form | CONFIRMED | one class per form: `ShippingClassFormFactory`, `CurrencyRatesFormFactory`, `LogFilterFormFactory`, `DiagnosticsLoggingFormFactory`, `StoredUntilFormFactory`, `BugReportForm`. The anchor covers only the per class form; the other five were confirmed by lookup |
| 25 | The module holds 9 files | CONFIRMED | `find src/Packetery/Module/Forms -name '*.php' \| wc -l` = 9; `ai-docs/ast/map.json` lists the same nine classes |
| 25 | 1066 lines of logic | CONFIRMED | non-blank non-comment lines of those nine files = 1066; `ai-docs/manifest.yaml:160` says the same |
| 25 | 37 public methods | CONFIRMED | `grep -cE '^\s*(final )?public( static)? function'` summed over the nine files = 37 |
| 25-27 | The module writes the carrier settings to the WordPress option of that carrier | CONFIRMED | `Forms/CarrierFormFactory.php:531` `update_option( OptionPrefixer::getOptionId( $options['id'] ), $options )`; `Forms/ShippingClassFormFactory.php:222` writes the per class section into the same option |
| 26-27 | "every other value belongs to the module that asked for the form" | INCORRECT | The module writes a second value itself. `Forms/DiagnosticsLoggingFormFactory.php:54`: `$this->wpAdapter->updateOption( OptionNames::PACKETERY_DIAGNOSTICS_LOGGING_ENABLED, $values['enabled'] );`. The document's own table row at line 43 says the same ("Stores the switch of the diagnostic logging"), so the claim also contradicts this page |
| 29 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 33 | The module exposes one factory for each form, and a helper with the shared parts | CONFIRMED | eight form classes plus `Forms/ShippingFormHelper.php`; `Forms/FormData/OrderFormData.php` is a data object, not a factory |
| 37 | Carrier form `createForm` builds the settings form of one carrier and sets its stored values | CONFIRMED | `Forms/CarrierFormFactory.php:94`; `:342` `$form->setDefaults( $carrierOptions )` from `get_option( $optionId )` |
| 38 | Carrier form template `createFormTemplate` builds the empty containers that the browser copies for a new limit | CONFIRMED | `Forms/CarrierFormFactory.php:58` builds a form named `<optionId>_template` with one row (index 0) of each limit container; rendered by `Carrier/OptionsPage.php:134,145` and `Carrier/ShippingClassPage.php:70` as `formTemplate` |
| 39 | Carrier form save `updateOptions`, `validateOptions` check the limits and write the settings of the carrier | CONFIRMED | `Forms/CarrierFormFactory.php:439` and `:499`; both are wired at `:332-333` (`onValidate`, `onSuccess`) |
| 40 | Shipping class form `createFromClassAndCarrier` builds the price form of one carrier and one shipping class | CONFIRMED | `Forms/ShippingClassFormFactory.php:50` |
| 41 | Currency rates form `createForm` builds one rate field for each currency that the shop does not use | CONFIRMED | `Forms/CurrencyRatesFormFactory.php:47-51`: iterates `$this->currencyConversion->getList()` and skips `$this->wcAdapter->getWoocommerceCurrency()` |
| 42 | Log filter form `create` builds the filter of the log page as a form of the query string | CONFIRMED | `Forms/LogFilterFormFactory.php:29`, `:31` `$form->setMethod( 'GET' )` |
| 43 | Diagnostic logging form `onFormSuccess` stores the switch of the diagnostic logging | CONFIRMED | `Forms/DiagnosticsLoggingFormFactory.php:52-54` |
| 44 | Stored until form `createForm` builds the small form of the storage date of a packet | CONFIRMED | `Forms/StoredUntilFormFactory.php:37`: `packet_id` and `packetery_stored_until` fields |
| 45 | Bug report form `onFormSuccess` sends the message of the administrator and can send a copy | CONFIRMED | `Forms/BugReportForm.php:73` `$this->bugReportEmail->sendBugReport( $email, $message, $sendCopy )` |
| 46 | Limit helper `addWeightLimit`, `checkOverlapping`, `sortLimits` build a repeatable limit, reject two limits that overlap and sort them | CONFIRMED | `Forms/ShippingFormHelper.php:47`, `:196` (duplicate threshold of the limit key raises the "rules are overlapping" error), `:214` (`array_multisort`) |
| 50-51 | The carrier form is built from the abilities of the carrier, so two carriers get two different forms | CONFIRMED | `Forms/CarrierFormFactory.php:198` `supportsCod()`, `:226` `isCarDelivery()`, `:253` `hasPickupPoints()`, `:320` `supportsAgeVerification()` |
| 52-53 | The form always holds the active switch, the display name, the pricing type and one price table; the pricing type decides whether the table holds weight limits or order value limits | CONFIRMED | `Forms/CarrierFormFactory.php:100-110` and `:184-191`. Both limit containers are always added (`:158`, `:184`); the pricing type select toggles which one the browser shows (`:133-139`) and which one the save keeps (`:513-525`) |
| 57 | `active`, `name` — always — the switch and the name that the checkout shows | CONFIRMED | `Forms/CarrierFormFactory.php:100` `addCheckbox( OptionsPage::FORM_FIELD_ACTIVE )`, `:105` `addText( OptionsPage::FORM_FIELD_NAME )` |
| 58 | `weight_limits`, `product_value_limits` — the selected pricing type — repeatable rows of a limit and a price | CONFIRMED | `Forms/ShippingFormHelper.php:47` adds `weight` + `price`, `:83` adds `value` + `price`; both rule sets are conditional on the pricing type select |
| 59 | `free_shipping_limit`, `coupon_free_shipping` — always — the order value that makes the delivery free, and the coupon rules [anchor `ShippingFormHelper.php#addProductValueLimit`] | INCORRECT | The anchored method builds one product value limit row (`value` and `price` fields, `Forms/ShippingFormHelper.php:83-115`) and never touches these two fields. Both are added by `Forms/CarrierFormFactory.php:221` (`free_shipping_limit`) and `:241-245` (container `coupon_free_shipping` with `active` and `allow_for_fees`). The claim itself holds; the anchor does not support it |
| 60 | `default_COD_surcharge`, `surcharge_limits`, `cod_rounding` — the carrier supports cash on delivery — the surcharge table and the rounding rule | CONFIRMED | condition `Forms/CarrierFormFactory.php:198` `$carrier->supportsCod()`; surcharge rows `Forms/ShippingFormHelper.php:116`; `default_COD_surcharge` `:199` and `cod_rounding` `:217` sit in the factory, not in the anchored helper method |
| 61 | `dimensions_restrictions` — always — either the three dimensions, or the longest side and the sum of the sides | CONFIRMED | `Forms/CarrierFormFactory.php:247` adds the container always; `:249-254` chooses `length`/`width`/`height` for Z-BOX, Z-POINT and numeric pickup point carriers, `:279-299` `maximum_length` and `dimensions_sum` otherwise |
| 62 | `vendor_groups` — the carrier is a compound carrier — one checkbox for each vendor group of the country | CONFIRMED | `Forms/CarrierFormFactory.php:380` `getAvailableVendors` returns `null` for a carrier id that is not compound, and `:403-432` turns the vendor codes into one checkbox each |
| 63 | `address_validation` — the carrier delivers to an address in a supported country — off, optional or required | CONFIRMED | `Forms/CarrierFormFactory.php:305-318`: not car delivery, no pickup points, country in `Carrier::ADDRESS_VALIDATION_COUNTRIES`; options `none`, `optional`, `required` |
| 64 | `age_verification_fee` — the carrier supports age verification — the fee that the cart adds | CONFIRMED | `Forms/CarrierFormFactory.php:320-325` |
| 65 | `days_until_shipping`, `shipping_time_cut_off` — the carrier is a car delivery carrier | CONFIRMED | `Forms/CarrierFormFactory.php:226-238` under `$carrier->isCarDelivery()` |
| 66 | `max_cart_value`, `class_calculation_type` — the shop uses the carrier configuration of WooCommerce | CONFIRMED | `Forms/CarrierFormFactory.php:143-156` and `:172-183`, both under `$this->optionsProvider->isWcCarrierConfigEnabled()` |
| 68 | The validation rejects two limits of one table that overlap | CONFIRMED | `Forms/CarrierFormFactory.php:461-489` calls `checkOverlapping` for the weight, the product value and the surcharge table |
| 69-70 | "it rejects a compound carrier with fewer vendor groups than the minimum" [anchor `CarrierFormFactory.php#isAvailableVendorsCountLowerThanRequiredMinimum`] | INCORRECT | The anchored method is not part of the validation. It is called in `getVendorCheckboxesConfig` (`:405`) to leave the checkboxes out of the form, and in `Carrier/OptionsPage.php:306`. What `validateOptions` rejects is a different set: `:453-457` raises the error when at least `MINIMUM_CHECKED_VENDORS` groups are offered but fewer than that are **checked** — "Check at least two types of pickup points…" |
| 71-72 | The save merges the new limits with the stored ones, sorts them and keeps the per class section | CONFIRMED | `Forms/CarrierFormFactory.php:509-512` (per class section from the persisted options), `:513-530` `mergeNewLimits` + `sortLimits` |
| 76 | "packetery-module-forms depends on the modules that own the values of each form" — completeness of the list of lines 78-83 | INCORRECT | The list leaves out `packetery-module-framework`. Six of the nine files import `Packetery\Module\Framework\WpAdapter` and `CurrencyRatesFormFactory` also imports `Packetery\Module\Framework\WcAdapter`; `ai-docs/manifest.yaml:162-164` lists that module with the path `src/Packetery/Module/Framework`. `packetery-module-payment` is **not** missing: `PaymentGatewayHelper` lives in `src/Packetery/Module`, which is `packetery-module-root` |
| 78 | references → packetery-core | CONFIRMED | `use Packetery\Core\CoreHelper`, `Packetery\Core\Entity\Carrier`, `Packetery\Core\Rounder` |
| 79 | references → packetery-module-root | CONFIRMED | `use Packetery\Module\FormFactory`, `FormValidators`, `MessageManager`, `PaymentGatewayHelper` — all in `src/Packetery/Module` |
| 80 | references → packetery-module-carrier | CONFIRMED | `use Packetery\Module\Carrier\{CarrierOptionsFactory, EntityRepository, OptionPrefixer, Options, OptionsPage, PacketaPickupPointsConfig, ShippingClassPage}` |
| 81 | references → packetery-module-options | CONFIRMED | `use Packetery\Module\Options\{OptionNames, OptionsProvider, Page}` |
| 82 | references → packetery-module-order | CONFIRMED | `use Packetery\Module\Order\CurrencyConversion` in `Forms/CurrencyRatesFormFactory.php` |
| 83 | references → packetery-module-email | CONFIRMED | `use Packetery\Module\Email\BugReportEmail` in `Forms/BugReportForm.php` |
| 85-87 | "The carrier form reads and writes the settings of `packetery-module-carrier`, and it uses the option name convention of that module" [anchor `ShippingFormHelper.php#createUrl`] | INCORRECT | `createUrl` (`Forms/ShippingFormHelper.php:224-247`) builds an admin URL: `add_query_arg( $params, admin_url( 'admin.php' ) )` with `page`, the country code, the carrier id and the class slug. It says nothing about option names. The convention is `Carrier\OptionPrefixer::getOptionId()`, used at `Forms/CarrierFormFactory.php:59`, `:96`, `:531` and `Forms/ShippingClassFormFactory.php:51`, `:185`. The claim itself holds; the anchor does not support it |
| 87-89 | The currency rates form takes the currency list from `packetery-module-order` and the stored rates from `packetery-module-options` | CONFIRMED | `Forms/CurrencyRatesFormFactory.php:48` `$this->currencyConversion->getList()` (`Packetery\Module\Order\CurrencyConversion`), `:40` `$this->optionsProvider->getCustomCurrencyRates()` |
| 90-91 | "The bug report form hands the message to `packetery-module-email`" [anchor `BugReportForm.php#onFormValidate`] | INCORRECT | `onFormValidate` (`Forms/BugReportForm.php:119-134`) only rejects an empty message: `$form->addError( … 'Message is required.' … )`. The hand-off is in `onFormSuccess`: `Forms/BugReportForm.php:73` `$this->bugReportEmail->sendBugReport( $email, $message, $sendCopy )`. The claim itself holds; the anchor points at the wrong method |
| 91-93 | The forms come from the form factory of `packetery-module-root`, which holds the validation messages | CONFIRMED | `Forms/StoredUntilFormFactory.php:38` `$this->formFactory->create( $name )`; `src/Packetery/Module/FormFactory.php:33-45` sets `Validator::$messages[…]` for `MIN`, `MAX`, `INTEGER`, `FLOAT`, `FILLED`, `LENGTH`, `MAX_LENGTH` |
| 97-99 | "The names of most carrier settings are string literals of the factory, and only the fields of the price table have constants" | INCORRECT | Fields outside the price table also have constants, and the constants are not the factory's. `Carrier/OptionsPage.php:33-39` declares `FORM_FIELD_NAME = 'name'`, `FORM_FIELD_ACTIVE = 'active'`, `FORM_FIELD_PRICING_TYPE = 'pricing_type'`, `FORM_FIELD_MAX_CART_VALUE = 'max_cart_value'`, `FORM_FIELD_CLASS_CALC_TYPE = 'class_calculation_type'` next to the two limit container constants; the factory uses them (`Forms/CarrierFormFactory.php:100`, `:105`, `:145`, `:174`). What is literal in the factory is the rest (`free_shipping_limit`, `coupon_free_shipping`, `dimensions_restrictions`, `surcharge_limits`, `cod_rounding`, `default_COD_surcharge`, `address_validation`, `age_verification_fee`, `days_until_shipping`, `shipping_time_cut_off`, `vendor_groups`) |
| 99-100 | A change of one key needs a change in the factory and in the settings object of the carrier module | CONFIRMED | the same literals are read in `Carrier/Options.php:66`, `:107`, `:121`, `:148`, `:157`, `:177`, `:204`, `:227-230` |
| 102-103 | The data object of the order form has no reader in this module, so its use lives elsewhere | CONFIRMED | `Forms/FormData/OrderFormData.php` holds public properties only; the single reader is `Module/Api/Internal/OrderController.php:219` `$form->getValues( OrderFormData::class )` |
| 103-105 | The shipping classes are read once into a static variable, so a class that appears during one request is not seen | CONFIRMED | `Forms/ShippingFormHelper.php:22` `private static ?array $shippingClasses = null;`, `:136-139` returns the cached value |
| 105-106 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rniE 'todo\|fixme' src/Packetery/Module/Forms/` returns 0 lines |

CONFIRMED 43 · INCORRECT 7 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-forms.yaml

The separate fragment files no longer exist; the records of this module live in `ai-docs/manifest.yaml` (`aliases:` line 12, `modules:` lines 158-161, `exposes:` lines 327-359). The `modules:` section of that file carries no `evidence:` key for any of its 25 entries, so the module record is verified on `name`, `path`, `lines` and `doc` only.

| entry | class | evidence |
|---|---|---|
| `aliases:` — `packetery-module-forms` (line 12) | CONFIRMED | the namespace `Packetery\Module\Forms` exists and is documented by `reference/packetery-module-forms.md` |
| `modules:` — `name: packetery-module-forms`, `path: src/Packetery/Module/Forms` (158-159) | CONFIRMED | the directory holds the nine files of the module; `ai-docs/ast/map.json` lists the same namespace |
| `modules:` — `lines: 1066` (160) | CONFIRMED | non-blank non-comment lines of the nine `*.php` files = 1066, the same number as line 25 of the document |
| `modules:` — `doc: reference/packetery-module-forms.md` (161) | CONFIRMED | the file exists and its front matter says `module: packetery-module-forms` |
| `exposes:` — library `create`, `module: packetery-module-forms`, `auth: none`, evidence `Forms/LogFilterFormFactory.php#create`, notes "Builds the filter form of the log page." | CONFIRMED | `Forms/LogFilterFormFactory.php:29`; the form is the GET filter of the `packeta-logs` page (`:34`) |
| `exposes:` — library `createForm`, `module: packetery-module-forms`, `auth: none`, evidence `Forms/CarrierFormFactory.php#createForm`, notes "Builds the settings form of one carrier, validates it and writes the carrier option." | CONFIRMED | `Forms/CarrierFormFactory.php:94`; `:332-333` wires `onValidate` to `validateOptions` and `onSuccess` to `updateOptions`, which calls `update_option` at `:531` |
| `exposes:` — library `createFromClassAndCarrier`, `module: packetery-module-forms`, `auth: none`, evidence `Forms/ShippingClassFormFactory.php#createFromClassAndCarrier`, notes "Builds the price form of one carrier and one WooCommerce shipping class." | CONFIRMED | `Forms/ShippingClassFormFactory.php:50` |
| no entry with `resolved_by: packetery-module-forms`, and no entry of `calls:`, `owns_data:` or `unknowns:` names the module | CONFIRMED | `grep -c 'resolved_by: packetery-module-forms' ai-docs/manifest.yaml` = 0; the only carrier option store of `owns_data:` is credited to `packetery-module-carrier`, which matches the dependency direction of lines 85-87 of the document |

CONFIRMED 8 · INCORRECT 0 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
