## ai-docs/reference/packetery-module-root.md @ c961aa4f — 2026-09-22

Verified against branch `docs/teams`, HEAD `a6ef312a`. Module directory from `covers:` is
`src/Packetery/Module`; the counts below use the files directly in that directory, because the
document itself says the subdirectories are separate modules.

| line | claim | class | evidence |
|---|---|---|---|
| 17–18 | The module holds the classes of the plugin namespace and starts the plugin [VERIFY: src/Packetery/Module/Plugin.php#run] | CONFIRMED | `Plugin.php:54` `public function run(): void`; 28 PHP files sit directly in `src/Packetery/Module` |
| 18–19 | "The module owns the tasks that no other module owns." | UNCERTAIN | No anchor and no way to test the negative; the repository gives no ownership list |
| 19–20 | It schedules the background jobs [VERIFY: src/Packetery/Module/CronService.php#register] | CONFIRMED | `CronService.php:80` `public function register(): void`, `as_schedule_recurring_action` and `as_schedule_cron_action` at lines 96–116 |
| 20–21 | It creates and migrates the database tables [VERIFY: src/Packetery/Module/Upgrade.php#check] | CONFIRMED | `Upgrade.php:133` `public function check(): void` calls `runCreateTables()` (line 139) and the version blocks |
| 21–22 | "it removes every trace of the plugin at uninstall" [VERIFY: src/Packetery/Module/Uninstaller.php#uninstall] | INCORRECT | `Uninstaller.php:42-52` deletes nothing unless a constant is set: `if ( defined( 'PACKETERY_DEBUG' ) ... ) { return; }` and `if ( defined( 'PACKETERY_REMOVE_ALL_DATA' ) && constant( 'PACKETERY_REMOVE_ALL_DATA' ) === true )`. The default install removes nothing. The document states the condition only at line 115 |
| 22–23 | It wraps the database of WordPress [VERIFY: src/Packetery/Module/WpdbAdapter.php#dbDelta] | CONFIRMED | `WpdbAdapter.php:397` `public function dbDelta( string $createTableQuery, string $tableName ): bool` wraps the WordPress function |
| 25 | "the helpers that every module uses" | INCORRECT | No helper of this module is used by every module. `WidgetOptionsBuilder` is used by two: `Checkout` and `Order`. `ContextResolver` by three (`Order`, `Shipping`, `Views`), `MessageManager` by eight, `ModuleHelper` by ten, out of 23 module subdirectories |
| 26 | Admin context detection [VERIFY: src/Packetery/Module/ContextResolver.php#isOrderGridPage] | CONFIRMED | `ContextResolver.php:42` `public function isOrderGridPage(): bool` |
| 27 | Flash messages [VERIFY: src/Packetery/Module/MessageManager.php#flash_message] | CONFIRMED | `MessageManager.php:85` `public function flash_message( string $message, ... ): void` |
| 27–28 | Widget parameters [VERIFY: src/Packetery/Module/WidgetOptionsBuilder.php#getCarrierForCheckout] | CONFIRMED | `WidgetOptionsBuilder.php:83` `public function getCarrierForCheckout( Entity\Carrier $carrier, string $optionId ): array` |
| 28–29 | Unit conversion [VERIFY: src/Packetery/Module/ModuleHelper.php#convertToMillimeters] | CONFIRMED | `ModuleHelper.php:229` `public static function convertToMillimeters( float $number ): ?float` |
| 29–30 | "28 files, 1833 lines of logic and 126 public methods" | CONFIRMED | `find src/Packetery/Module -maxdepth 1 -name '*.php' \| wc -l` = 28; non-empty non-comment lines of those files = 1833; `grep -cE '^\s*(final )?public( static)? function'` over those files = 126. No test file is counted. `manifest.yaml:200` gives the same 1833 |
| 30–31 | The subdirectories of the namespace are separate modules | CONFIRMED | The 23 subdirectories of `src/Packetery/Module` each have a `modules:` entry in `ai-docs/manifest.yaml` (lines 113–213) |
| 33 | `> ⚠ add business context (elicitation)` | PENDING | Standard elicitation placeholder |
| 37–38 | The module exposes the entry point and the services that the other modules take from the container | CONFIRMED | `bootstrap.php:96-97` builds the container and calls `CompatibilityBridge::setContainer( $container )`; `Plugin.php:54` is the entry point |
| 42 | `run` registers the hooks through the hook module [VERIFY: src/Packetery/Module/Plugin.php#run] | CONFIRMED | `Plugin.php:55` `$this->hookRegistrar->register();`, type `Packetery\Module\Hooks\HookRegistrar` |
| 43 | `getAppIdentity` builds the identity string for Packeta [VERIFY: src/Packetery/Module/Plugin.php#getAppIdentity] | CONFIRMED | `Plugin.php:79` `public static function getAppIdentity(): string`, doc block "Gets software identity for Packeta APIs" |
| 44 | `register`, `deactivate` register the jobs and remove them on deactivation [VERIFY: src/Packetery/Module/CronService.php#register] | CONFIRMED | `CronService.php:80` and `CronService.php:126` `public static function deactivate(): void` with five `as_unschedule_action` calls |
| 45 | `check`, `runCreateTables` create the tables and run the migration [VERIFY: src/Packetery/Module/Upgrade.php#check] | CONFIRMED | `Upgrade.php:133` and `Upgrade.php:241` `public function runCreateTables(): bool` |
| 46 | `uninstall` drops the tables and deletes the options and the transients [VERIFY: src/Packetery/Module/Uninstaller.php#uninstall] | CONFIRMED | `Uninstaller.php:41`; `cleanUp()` at line 70 runs five `DROP TABLE IF EXISTS`, then `deleteTransient` and `deleteOption` calls |
| 47 | `WpdbAdapter` wraps the database and holds the table names [VERIFY: src/Packetery/Module/WpdbAdapter.php#getPacketeryPrefix] | CONFIRMED | `WpdbAdapter.php:294` `public function getPacketeryPrefix(): string`; public table-name properties at lines 27, 34, 41, 48, 55 |
| 48 | `processClauses`, `processHposClauses` add the filters and the sorting to both order storages [VERIFY: src/Packetery/Module/QueryProcessor.php#processClauses] | CONFIRMED | `QueryProcessor.php:55-56` hooks `posts_clauses` (legacy posts) and `woocommerce_orders_table_query_clauses` (HPOS); methods at lines 69 and 118 |
| 49 | `getCountryCodesForShippingZone` gives the country codes and expands a continent [VERIFY: src/Packetery/Module/ShippingZoneRepository.php#getCountryCodesForShippingZone] | CONFIRMED | `ShippingZoneRepository.php:132`; the continent branch is at line 122, `if ( $zoneLocation->type === 'continent' ... )` |
| 50 | `getCarrierForCheckout`, `createPickupPointForAdmin` build the widget parameters [VERIFY: src/Packetery/Module/WidgetOptionsBuilder.php#createPickupPointForAdmin] | CONFIRMED | `WidgetOptionsBuilder.php:117` `public function createPickupPointForAdmin( Order $order ): array`; checkout variant at line 83 |
| 51 | `post`, `get` send the HTTP requests and raise one exception type [VERIFY: src/Packetery/Module/WebRequestClient.php#post] | CONFIRMED | `WebRequestClient.php:32` and `:50`; both throw `WebRequestException` only |
| 53–55 | The module also gives the shipping method, the form factory with its validators, the flash messages, the dashboard widget and the transient name constants [VERIFY: src/Packetery/Module/Transients.php#Transients] | CONFIRMED | `ShippingMethod.php`, `FormFactory.php` + `FormValidators.php` + `FormRules.php`, `MessageManager.php`, `DashboardWidget.php`, `Transients.php:7` `class Transients` with the prefix constants |
| 55–57 | A bridge holds the container, because WooCommerce builds a shipping method outside it [VERIFY: src/Packetery/Module/CompatibilityBridge.php#getContainer] | CONFIRMED | `CompatibilityBridge.php:41` `public static function getContainer(): Container`, doc block "Storage to use in ShippingMethod"; `ShippingMethod.php:37` `$container = CompatibilityBridge::getContainer();` |
| 61–62 | Five recurring jobs, through the Action Scheduler of WooCommerce, not the cron of WordPress | CONFIRMED | `CronService.php:21-25` holds five hook constants; `CronService.php:91` returns when `as_has_scheduled_action` does not exist; only `as_*` functions are used, no `wp_schedule_event` |
| 62–63 | The module plans a job only when the same job has no schedule yet [VERIFY: src/Packetery/Module/CronService.php#register] | CONFIRMED | Every branch is guarded by `as_has_scheduled_action( ... ) === false` (lines 95, 98, 105, 108, 113) |
| 67 | Log deletion, `packetery_cron_log_auto_deletion_hook`, every day at 02:00 [VERIFY: src/Packetery/Module/CronService.php#CRON_LOG_AUTO_DELETION_HOOK] | CONFIRMED | `CronService.php:21` constant; `CronService.php:96` `new \DateTime( 'next day 02:00', wp_timezone() )` with `DAY_IN_SECONDS` |
| 68 | Transient purge, `packetery_cron_purge_transients`, every day at 02:10 [VERIFY: src/Packetery/Module/CronService.php#register] | CONFIRMED | `CronService.php:25` constant; lines 99–103, `'next day 02:10'` with `DAY_IN_SECONDS` |
| 69 | Carrier feed, `packetery_cron_carriers_hook`, every day at 09:10 [VERIFY: src/Packetery/Module/CronService.php#CRON_CARRIERS_HOOK] | CONFIRMED | `CronService.php:22` constant; `CronService.php:106` `'next day 09:10'` with `DAY_IN_SECONDS` |
| 70 | Packet status, `packetery_cron_packet_status_sync_hook`, six times a day from Monday to Friday [VERIFY: src/Packetery/Module/CronService.php:23] | CONFIRMED | Line 23 holds the constant; `CronService.php:111` `'10 2,6,10,14,18,22 * * 1-5'` — six times a day, Monday to Friday |
| 71 | Packet status weekend, `packetery_cron_packet_status_sync_hook_weekend`, once a day on Saturday and Sunday [VERIFY: src/Packetery/Module/CronService.php#register] | CONFIRMED | `CronService.php:116` `'10 3 * * 6,0'`. Note: the constant at line 24 is `private`, so the hook name is not part of the public interface |
| 73–74 | The deactivation removes all five schedules [VERIFY: src/Packetery/Module/CronService.php#deactivate] | CONFIRMED | `CronService.php:126-132`, five `as_unschedule_action` calls |
| 74–76 | The upgrade owns `plugin-schema` of five tables; it compares the stored version with the version constant and runs the migration blocks in between [VERIFY: src/Packetery/Module/Upgrade.php#check] | CONFIRMED | `Upgrade.php:134-139` `get_option( OptionNames::VERSION )` against `Plugin::VERSION`, then `runCreateTables()`; the five tables are the `packeteryLog`, `packeteryCarrier`, `packeteryOrder`, `packeteryCustomsDeclaration` and `packeteryCustomsDeclarationItem` properties of `WpdbAdapter` |
| 76–78 | The oldest block moves the Packeta data of an order from the WooCommerce metadata into the order table [VERIFY: src/Packetery/Module/Upgrade.php#migrateWpOrderMetadata] | CONFIRMED | `Upgrade.php:142` `version_compare( $oldVersion, '1.2.0', '<' )` calls `migrateWpOrderMetadata()` (line 255), which reads the order meta and calls `$this->orderRepository->save( $orderEntity )` |
| 78–81 | The uninstall runs only under the constant, drops five tables and deletes the options, the transients and the product metadata [VERIFY: src/Packetery/Module/Uninstaller.php#cleanUp] | CONFIRMED | `Uninstaller.php:70-131`; the product metadata is the `DELETE FROM postmeta` on `META_AGE_VERIFICATION_18_PLUS` and `META_DISALLOWED_SHIPPING_RATES` at lines 123–130 |
| 88–94 | `references →` list: packetery-core, -hooks, -order, -carrier, -options, -log, -checkout | INCORRECT | The list is incomplete. The root files also depend on: `packetery-module-framework` (`WpAdapter`/`WcAdapter` in `MessageManager.php`, `ModuleHelper.php`, `ShippingZoneRepository.php`, `Uninstaller.php`, `WeightCalculator.php`, `WidgetOptionsBuilder.php`, `WpdbAdapter.php`, `WpdbAdapterFactory.php`), `packetery-module-exception` (`ShippingMethod.php:8`, `WidgetOptionsBuilder.php`, `WpdbAdapter.php`), `packetery-module-dashboard` (`Plugin.php:` `Dashboard\DashboardPage`, `DashboardWidget.php`), `packetery-module-forms` (`DashboardWidget.php` `Forms\ShippingFormHelper`), `packetery-module-views` (`DashboardWidget.php` `Views\UrlBuilder`), `packetery-module-upgrade` (`Upgrade.php` `Upgrade\Version_1_4_2`), `packetery-module-product` (`Uninstaller.php:127-128` `Product\Entity::META_...`) and `packetery-module-customsdeclaration` (`Upgrade.php` `customsDeclarationRepository`) |
| 96–97 | The plugin class asks packetery-module-hooks to register every hook [VERIFY: src/Packetery/Module/Plugin.php#run] | CONFIRMED | `Plugin.php:55` `$this->hookRegistrar->register();`; `Hooks/HookRegistrar.php:408` calls `$this->cronService->register();`, so the cron hooks go through the same entry |
| 97–99 | The jobs call the log purger, the transient purger, the carrier downloader and the packet synchroniser [VERIFY: src/Packetery/Module/CronService.php#register] | CONFIRMED | `CronService.php:82-86` binds `Log\Purger::autoDeleteHook`, `Options\TransientPurger::purge`, `Carrier\Downloader::runAndRender` and `Order\PacketSynchronizer::syncStatuses` |
| 99–101 | The upgrade creates the tables through the repositories that own them [VERIFY: src/Packetery/Module/Upgrade.php#runCreateTables] | CONFIRMED | `Upgrade.php:421-461`: `logRepository`, `carrierRepository`, `orderRepository` and `customsDeclarationRepository`, each through `createOrAlterTable()` |
| 101–103 | The web request client implements the interface of packetery-core, and the core uses it for every REST call [VERIFY: src/Packetery/Module/WebRequestClient.php#get] | CONFIRMED | `WebRequestClient.php:20` `class WebRequestClient implements IWebRequestClient`; the only REST caller in the core is `Core/Api/Rest/PickupPointValidate.php:49` `$this->webRequestClient->post( ... )`, and it takes `IWebRequestClient` |
| 103–105 | The weight calculation reads the packaging weight from packetery-module-options [VERIFY: src/Packetery/Module/WeightCalculator.php#calculateOrderWeight] | CONFIRMED | `WeightCalculator.php:65` `$weightKg += $this->optionsProvider->getPackagingWeight();`, type `Packetery\Module\Options\OptionsProvider` |
| 109–111 | The database wrapper calls the schema function two times and compares the two answers [VERIFY: src/Packetery/Module/WpdbAdapter.php#dbDelta] | CONFIRMED | `WpdbAdapter.php:399-400` `$result1 = dbDelta( $createTableQuery ); $result2 = dbDelta( $createTableQuery );`, compared at lines 411–418. The code comment gives the reason as "it means it failed", not the wording of the document, but the mechanism matches |
| 112–113 | A failed query goes to the log only when the query names a table of the plugin [VERIFY: src/Packetery/Module/WpdbAdapter.php#isPacketeryTableQueried] | CONFIRMED | `WpdbAdapter.php:316` `if ( $this->getLastWpdbError() !== '' && $this->isPacketeryTableQueried( (string) $this->wpdb->last_query ) )` |
| 115–116 | The uninstall deletes nothing without the constant, and a debug constant stops it as well [VERIFY: src/Packetery/Module/Uninstaller.php#uninstall] | CONFIRMED | `Uninstaller.php:42-45` `PACKETERY_DEBUG` early return; line 46 `PACKETERY_REMOVE_ALL_DATA` gate |
| 116–118 | The context detection reads the global variables of WordPress [VERIFY: src/Packetery/Module/ContextResolver.php#isOrderDetailPage] | CONFIRMED | `ContextResolver.php:60` `global $pagenow, $typenow, $plugin_page;` |
| 118–120 | The migration ignores the result of the save, and a comment says so [VERIFY: src/Packetery/Module/Upgrade.php#migrateWpOrderMetadata] | CONFIRMED | `Upgrade.php:340-341` `$this->orderRepository->save( $orderEntity ); // Ignore errors. Save the metadata even if the previous call fails.` |
| 120–122 | The shipping method sets its enabled state as a fixed value, and a comment says it can become a setting [VERIFY: src/Packetery/Module/ShippingMethod.php#__construct] | CONFIRMED | `ShippingMethod.php:29` `$this->enabled = 'yes'; // This can be added as a setting.` |
| 122–123 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -cE 'TODO\|FIXME'` over the 28 files of the directory returns 0 for every file |

CONFIRMED 46 · INCORRECT 3 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-root.yaml

The separate fragment file no longer exists; the entries below are read from `ai-docs/manifest.yaml`.

| entry | class | evidence |
|---|---|---|
| `aliases:` line 20 — `packetery-module-root` | CONFIRMED | The namespace `Packetery\Module` exists and holds 28 files |
| `modules:` line 198–199 — `name: packetery-module-root`, `path: src/Packetery/Module` | CONFIRMED | The directory exists and matches the `covers:` of the document. The `modules:` schema has no `evidence:` key, so none is missing here |
| `modules:` line 200 — `lines: 1833` | CONFIRMED | Non-empty non-comment lines of the 28 files directly in `src/Packetery/Module` = 1833; no test file counted |
| `modules:` line 201 — `doc: reference/packetery-module-root.md` | CONFIRMED | The file exists and its front matter has `module: packetery-module-root` |
| `exposes:` line 276–284 — `kind: library`, `name: WpdbAdapter`, `evidence: src/Packetery/Module/WpdbAdapter.php#getPacketeryPrefix` | CONFIRMED | `WpdbAdapter.php:294` `public function getPacketeryPrefix(): string`; the notes on the table names match the properties at lines 27–55 |
| `exposes:` line 522–530 — `name: packetery_cron_carriers_hook`, `evidence: src/Packetery/Module/CronService.php#CRON_CARRIERS_HOOK` | CONFIRMED | `CronService.php:22` constant, scheduled daily at line 106, bound to `Carrier\Downloader::runAndRender` at line 84 |
| `exposes:` line 531–539 — `name: packetery_cron_packet_status_sync_hook`, `evidence: src/Packetery/Module/CronService.php:23` | CONFIRMED | Line 23 is `public const CRON_PACKET_STATUS_SYNC_HOOK = 'packetery_cron_packet_status_sync_hook';`, bound to `Order\PacketSynchronizer::syncStatuses` at line 85 |
| `exposes:` line 599–606 — `kind: library`, `name: run`, `evidence: src/Packetery/Module/Plugin.php#run` | CONFIRMED | `Plugin.php:54`; the notes "The main plugin file calls it" match `bootstrap.php` building the container and the plugin entry |
| `owns_data:` line 719–724 — `entity: plugin-schema`, `store: mysql`, `evidence: src/Packetery/Module/Upgrade.php#runCreateTables` | CONFIRMED | `Upgrade.php:241-248` calls four private creators that delegate to `logRepository`, `carrierRepository`, `orderRepository` and `customsDeclarationRepository`; five tables result |
| `unknowns:` line 764–767 — "Where WordPress schedules the carrier feed download." `resolved_by: packetery-module-root` | CONFIRMED | `CronService.php:105-107` schedules `CRON_CARRIERS_HOOK`, and `CronService` is a file of this module |
| `unknowns:` line 776–779 — "Whether a capability check protects the settings export outside this namespace." `resolved_by: packetery-module-root` | INCORRECT | The export is hooked outside this module: `Hooks/HookRegistrar.php:587` `$this->wpAdapter->addAction( 'admin_init', [ $this->exporter, 'outputExportTxt' ] );` — that is `packetery-module-hooks`. `Options/Exporter.php:111-122` guards only on the query parameters `page` and `action` and contains no capability check. No file of `src/Packetery/Module` (top level) touches the export |
| `unknowns:` line 852–856 — "Which module gives the size limit and the age limit of the diagnostic file." `resolved_by: packetery-module-root` | INCORRECT | The size limiter is `Log/LogSizeLimiter.php:17-22` `__construct( int $logLimitSizeBytes, int $logLimitDays )`, and both values come from `config/config.neon:56` `Packetery\Module\Log\LogSizeLimiter(logLimitDays: %logLimitDays%, logLimitSizeBytes: %logLimitSizeBytes%)` with the parameters declared at `config/config.neon:15`, wired by `bootstrap.php:96` `$configurator->createContainer()`. Neither file is in `src/Packetery/Module`, so `packetery-module-root` does not give the values |

CONFIRMED 10 · INCORRECT 2 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
