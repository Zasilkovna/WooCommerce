## ai-docs/overview.md @ b34fe03c — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17–18 | The plugin adds one shipping method for each Packeta carrier | CONFIRMED | `src/Packetery/Module/Shipping/ShippingMethodBulkGenerator.php:31` `generateClasses()` builds one class per carrier from the pickup-point config and the carrier feed; `ShippingMethodGenerator::classExists( $carrierId )` at line 65 |
| 18–20 | The customer selects a pickup point or an address in the Packeta widget during the checkout [VERIFY: src/Packetery/Module/Checkout/CheckoutSettings.php#createSettings] | CONFIRMED | `createSettings()` returns `carrierConfig`, `pickupPointAttrs`, `homeDeliveryAttrs` and `carDeliveryAttrs` for the widget |
| 20–22 | The shop sends the order to Packeta as a packet, prints its label and reads its delivery status back [VERIFY: src/Packetery/Module/Order/PacketSubmitter.php#submitPacket] | CONFIRMED | `submitPacket()` sends the packet; `src/Packetery/Module/Labels/` holds the label services; `src/Packetery/Module/Order/PacketStatusResolver.php` reads the status |
| 24–25 | The plugin keeps the Packeta data of an order, the carrier list and the plugin settings in its own tables and options [VERIFY: src/Packetery/Module/Order/Repository.php#createOrAlterTable] | CONFIRMED | `createOrAlterTable()` creates the `packetery_order` table; the carrier table is created by the carrier module, the settings live in the options of `OptionNames.php` |
| 26–28 | The plugin supports the classic checkout and the block checkout, the classic order storage and the High Performance Order Storage [VERIFY: src/Packetery/Module/ModuleHelper.php#isHposEnabled] | CONFIRMED | `isHposEnabled()` returns `OrderUtil::custom_orders_table_usage_is_enabled()`; `src/Packetery/Module/Blocks/WidgetIntegration.php` covers the block checkout |
| 30 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 34 | woocommerce consists of 25 modules | CONFIRMED | `ai-docs/manifest.yaml` `modules:` holds 25 `- name:` entries |
| 34 | Eighteen of them have a document in `reference/` | CONFIRMED | `ls ai-docs/reference/` returns 18 files; 18 manifest entries carry `doc: reference/…` |
| 34–35 | [index.md](index.md) lists those documents | CONFIRMED | `ai-docs/index.md` § "woocommerce: modules" links all 18 reference pages |
| 35–36 | The seven modules below stayed under the threshold for a document of their own, and this page is their home | CONFIRMED | the 7 manifest entries with `doc: overview.md` are exactly framework, entityfactory, blocks, diagnosticslogger, upgrade, exception, payment; none of them has a file in `ai-docs/reference/` |
| 38–46 | Module table: the Path and Document columns | CONFIRMED | navigation content; each `path` and `doc` matches `ai-docs/manifest.yaml` and `ai-docs/index.md` |
| 40 | packetery-module-framework, `src/Packetery/Module/Framework`, 537 lines of logic | CONFIRMED | `find … -name '*.php' \| wc -l` = 16; non-empty non-comment lines = 537 |
| 41 | packetery-module-entityfactory, `src/Packetery/Module/EntityFactory`, 142 lines of logic | CONFIRMED | files = 4; non-empty non-comment lines = 142 |
| 42 | packetery-module-blocks, `src/Packetery/Module/Blocks`, 124 lines of logic | CONFIRMED | files = 2; non-empty non-comment lines = 124 |
| 43 | packetery-module-diagnosticslogger, `src/Packetery/Module/DiagnosticsLogger`, 65 lines of logic | CONFIRMED | files = 1; non-empty non-comment lines = 65 |
| 44 | packetery-module-upgrade, `src/Packetery/Module/Upgrade`, 44 lines of logic | CONFIRMED | files = 1; non-empty non-comment lines = 44 |
| 45 | packetery-module-exception, `src/Packetery/Module/Exception`, 25 lines of logic | CONFIRMED | files = 5; non-empty non-comment lines = 25 |
| 46 | packetery-module-payment, `src/Packetery/Module/Payment`, 16 lines of logic | CONFIRMED | files = 1; non-empty non-comment lines = 16 |
| 50–51 | The plugin starts from the main plugin file, which builds the service container and calls the plugin class [VERIFY: packeta.php#packetaPlugin] | CONFIRMED | `packeta.php:35–39` `/** @var Container $container */`, `$packetaPlugin = $container->getByType( Plugin::class ); $packetaPlugin->run();` |
| 51–53 | The plugin class hands the work to the hook module, which registers every callback of the request [VERIFY: src/Packetery/Module/Plugin.php#run] | CONFIRMED | `Plugin::run()` body is `$this->hookRegistrar->register();`; `src/Packetery/Module/Hooks/HookRegistrar.php` registers the callbacks |
| 53–55 | The upgrade runs on the `init` action, and it creates the tables when the stored version differs from the version of the code [VERIFY: src/Packetery/Module/Upgrade.php#check] | CONFIRMED | `HookRegistrar.php:384` `$this->wpAdapter->addAction( 'init', [ $this->upgrade, 'check' ] );`; `Upgrade::check()` returns early on `$oldVersion === Plugin::VERSION`, otherwise calls `$this->runCreateTables()` |
| 61 | `packetery` lives in [VERIFY: src/Packetery/Module/Options/OptionNames.php:11] | CONFIRMED | line 11 is `public const PACKETERY = 'packetery';` |
| 61 | `packetery` is read in `OptionsProvider.php` [VERIFY: …OptionsProvider.php#getAllOptions] | CONFIRMED | `OptionsProvider.php:81` `$data = $wpAdapter->getOption( OptionNames::PACKETERY );`; `getAllOptions()` returns `OptionNames::PACKETERY => $this->data` |
| 62 | `packetery_version` lives in [VERIFY: …OptionNames.php#VERSION] | CONFIRMED | line 8 `public const VERSION = 'packetery_version';` |
| 62 | `packetery_version` is read in `Upgrade.php` [VERIFY: src/Packetery/Module/Upgrade.php#check] | CONFIRMED | `Upgrade.php:134` `$oldVersion = get_option( OptionNames::VERSION );` |
| 63 | `packetery_advanced` lives in [VERIFY: …OptionNames.php#PACKETERY_ADVANCED] | CONFIRMED | line 14 `public const PACKETERY_ADVANCED = 'packetery_advanced';` |
| 63 | `packetery_advanced` is read in `OptionsProvider.php` [VERIFY: …#isWcCarrierConfigEnabled] | CONFIRMED | `OptionsProvider.php:96` `$advancedData = $wpAdapter->getOption( OptionNames::PACKETERY_ADVANCED );`; `isWcCarrierConfigEnabled()` (line 738) reads `$this->advancedData['new_carrier_settings_enabled']` |
| 64 | `packeta_feature_flags` lives in [VERIFY: …OptionNames.php#FEATURE_FLAGS] | CONFIRMED | line 21 `public const FEATURE_FLAGS = 'packeta_feature_flags';` |
| 64 | `packeta_feature_flags` is read in `src/Packetery/Module/Options/OptionsProvider.php` [VERIFY: …OptionNames.php#FEATURE_FLAGS_ERROR_COUNTER] | INCORRECT | `OptionsProvider.php` never reads `FEATURE_FLAGS`; it reads only `PACKETERY`, `PACKETERY_SYNC`, `PACKETERY_AUTO_SUBMISSION`, `PACKETERY_ADVANCED` and `PACKETERY_CURRENCY_RATES` (lines 81–101). The only use of the constant in `src/` is `src/Packetery/Module/Uninstaller.php:118` `$this->wpAdapter->deleteOption( OptionNames::FEATURE_FLAGS );`. The anchor also names a different option: `FEATURE_FLAGS_ERROR_COUNTER = 'packeta_feature_flags_error_counter'` (`OptionNames.php:20`) |
| 66–67 | The API password and the sender of the shop live in the option `packetery`, and the plugin derives the API key from the password [VERIFY: src/Packetery/Module/Options/Page.php#sanitizePacketeryOptions] | CONFIRMED | `sanitizePacketeryOptions()` does `$options['api_key'] = substr( $apiPass, 0, 16 );`; `Page.php:565` adds the `sender` field, `OptionsProvider.php:196` `return $this->get( 'sender' );`. No value is quoted, so this is not SENSITIVE |
| 68 | Never copy those values into a document. | INSTRUCTION | the line tells the reader to do something; it describes no code |
| 72–73 | The platform layer is packetery-module-framework, packetery-module-entityfactory and packetery-module-blocks | CONFIRMED | the three names and paths match `ai-docs/manifest.yaml` |
| 73–74 | They hold no Packeta business rule of their own | UNCERTAIN | `Framework` holds only adapters and traits, but `EntityFactory/SizeFactory.php:27–56` converts dimensions between cm and mm and applies the default size of a new order, which is plugin logic rather than a pure platform wrapper. No page or code states the boundary |
| 74 | The modules with a document use them | UNCERTAIN | 82 files outside `Framework` reference `WpAdapter`/`WcAdapter`, and `ai-docs/dependencies.md:58` says "Every module → packetery-module-framework". No evidence that every documented module uses `EntityFactory` and `Blocks` as well |
| 78 | packetery-module-framework wraps the functions of WordPress and of WooCommerce in two adapters and a set of traits [VERIFY: src/Packetery/Module/Framework/WpAdapter.php#WpAdapter] | CONFIRMED | the directory holds 16 files: `WpAdapter.php`, `WcAdapter.php` and 14 traits (`HookTrait.php`, `OptionTrait.php`, `WcCartTrait.php` …) |
| 78 | …so no other module calls a global function | INCORRECT | global calls exist outside `Framework`, for example `src/Packetery/Module/QueryProcessor.php:55` `add_filter( 'posts_clauses', [ $this, 'processClauses' ], 10, 2 );`, `src/Packetery/Module/ModuleHelper.php:57` `return add_query_arg( $queryVars, admin_url( $path ) );` and `src/Packetery/Module/Upgrade.php:134` `get_option( OptionNames::VERSION )`. `ai-docs/architecture.md:152` repeats the same claim ("Platform adapters — Every call of a WordPress or WooCommerce function") |
| 79 | packetery-module-entityfactory builds the entities of the domain module from the data of WordPress and of the API [VERIFY: src/Packetery/Module/EntityFactory/SizeFactory.php#SizeFactory] | CONFIRMED | `Address.php:25` `fromWcStoreOptions(): Entity\Address`, `Carrier.php:37` `fromDbResult()` and `:67` `fromNonFeedCarrierData()`, all returning `Packetery\Core\Entity` objects |
| 80 | packetery-module-blocks integrates the Packeta widget into the block checkout of WooCommerce [VERIFY: src/Packetery/Module/Blocks/WidgetIntegration.php#WidgetIntegration] | CONFIRMED | `class WidgetIntegration implements IntegrationInterface` (line 26), the interface coming from `Automattic\WooCommerce\Blocks\Integrations` |
| 82–83 | The adapters of packetery-module-framework are the lowest layer of the plugin, and they depend on no other module [VERIFY: src/Packetery/Module/Framework/WcAdapter.php#WcAdapter] | CONFIRMED | the `use` list of `WcAdapter.php` (lines 12–24) holds only WooCommerce and PHP classes; `WpAdapter.php` (lines 12–17) only WordPress and PHP classes. No `Packetery\` import |
| 87–88 | The four small modules are packetery-module-diagnosticslogger, packetery-module-upgrade, packetery-module-exception and packetery-module-payment | CONFIRMED | the four names and paths match `ai-docs/manifest.yaml`, and the overview table at lines 43–46 lists the same four |
| 92 | packetery-module-diagnosticslogger writes a diagnostic file when the shop enables the diagnostic logging [VERIFY: src/Packetery/Module/DiagnosticsLogger/DiagnosticsLogger.php#getPacketaLogPath] | CONFIRMED | `getPacketaLogPath()` returns `$logDir . '/packeta.log'`; `log()` (line 49) returns early unless `isLoggingEnabled()` (line 93) reads `OptionNames::PACKETERY_DIAGNOSTICS_LOGGING_ENABLED` as true |
| 93 | packetery-module-upgrade holds the one migration that belongs to a single plugin version [VERIFY: src/Packetery/Module/Upgrade/Version_1_4_2.php#run] | CONFIRMED | the directory holds only `Version_1_4_2.php`, whose `run()` (line 44) calls `deduplicateCarrierPrefix()` |
| 94 | packetery-module-exception holds the exception classes of the plugin, and it carries no logic [VERIFY: src/Packetery/Module/Exception/InvalidCarrierException.php#InvalidCarrierException] | INCORRECT | the completeness claim fails: four more exception classes live outside the module — `src/Packetery/Core/Api/WebRequestException.php`, `src/Packetery/Core/Api/InvalidRequestException.php`, `src/Packetery/Core/Api/RestException.php` and `src/Packetery/Core/Api/Rest/Exception/InvalidApiKeyException.php`. The "no logic" half holds: the module has 0 public methods |
| 95 | packetery-module-payment answers whether a payment method is cash on delivery [VERIFY: src/Packetery/Module/Payment/PaymentHelper.php#isCodPaymentMethod] | CONFIRMED | `isCodPaymentMethod()` returns `in_array( $paymentMethod, $this->optionsProvider->getCodPaymentMethods(), true )` |
| 97 | The upgrade module holds only the migration of one version | CONFIRMED | `ls src/Packetery/Module/Upgrade/` returns `Version_1_4_2.php` only |
| 97–98 | The upgrade of the schema and of the settings lives in `packetery-module-root` [VERIFY: src/Packetery/Module/Upgrade.php#runCreateTables] | CONFIRMED | `Upgrade.php` stands directly in `src/Packetery/Module`, which `ai-docs/manifest.yaml` maps to `packetery-module-root`; `check()` calls `runCreateTables()` and then migrates the stored data and writes `update_option( OptionNames::VERSION, Plugin::VERSION )` (line 238). `ai-docs/architecture.md:127` and `:141` use the same two anchors |

CONFIRMED 39 · INCORRECT 3 · UNCERTAIN 2 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 1
