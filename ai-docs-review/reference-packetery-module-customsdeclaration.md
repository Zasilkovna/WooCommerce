## ai-docs/reference/packetery-module-customsdeclaration.md @ b34fe03c — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 6 | `source-commit: b34fe03c` | CONFIRMED | `git cat-file -t b34fe03c` = commit, on branch `docs/teams` |
| 8 | `covers: [src/Packetery/Module/CustomsDeclaration]` | CONFIRMED | Directory exists and holds one file, `Repository.php`; no subdirectory, no test directory |
| 13 | Repo, module, type and status header | CONFIRMED | Matches the front matter and the `ai-docs/reference/` layout |
| 17-18 | The module stores the customs declaration of an order and the items of that declaration | CONFIRMED | `Repository.php:71` `getByOrderNumber`, `:135` `getItemsByCustomsDeclarationId`, two tables created at `:368` and `:396` |
| 18-20 | A shipment that leaves the customs union needs the declaration, and the order module sends it to Packeta with the packet | CONFIRMED | `Order/CreatePacketMapper.php:130` `if ( $carrier->requiresCustomsDeclarations() === false )`, then `:134-218` maps the declaration and its items into the packet attributes; `Order/Builder.php:222` reads it with `getByOrderNumber`. The anchor shows the read only; the send is in `Order/CreatePacketMapper.php` |
| 21 | 1 file, 284 lines of logic, 12 public methods | CONFIRMED | `find src/Packetery/Module/CustomsDeclaration -name '*.php' \| wc -l` = 1; non-blank non-comment lines = 284; `grep -cE '^\s*(final )?public( static)? function'` = 12 (`__construct` plus 11 methods). Same as `modules[].lines: 284` in `ai-docs/manifest.yaml` |
| 23-24 | The module owns the `customs-declaration` of an order in two tables and keeps the invoice file and the export document inside them | CONFIRMED | `createOrAlterTable():368` and `createOrAlterItemTable():396`; `invoice_file` `:377` and `ead_file` `:380` are `mediumblob` columns of the declaration table. `ai-docs/manifest.yaml` `owns_data` names the same entity |
| 25 | The module holds no user interface; the metabox of the order module writes through it | CONFIRMED | The module directory holds `Repository.php` only, no template and no Latte file; `Order/CustomsDeclarationMetabox.php:446` `save()`, `:477` `saveItem()`, `:460` `deleteItem()` |
| 27 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder |
| 31 | The module exposes one repository | CONFIRMED | One class, `Repository.php:22` `class Repository`; the directory holds no other file |
| 35 | `getByOrderNumber` gives the declaration of an order with its items and with lazy files | CONFIRMED | `:71-127`: `setItems( $this->getItemsByCustomsDeclarationId(...) )` `:124`; the file getters take closures `:100-122` |
| 36 | `getItemsByCustomsDeclarationId`, `getCustomsDeclarationItemRows` give the items as entities or as rows | CONFIRMED | `:135-152` builds entities with `createItemFromStandardizedStructure()` `:148`; `:154-175` returns `get_results( ..., ARRAY_A )` |
| 37 | `save` inserts or updates the declaration and writes the files separately | CONFIRMED | `:184`: insert `:186`, update `:194`; file columns in their own `update()` calls `:204`, `:215`, `:227`, `:238` |
| 38 | `saveItem` inserts or updates one item | CONFIRMED | `:258`: insert `:260`, update `:266` |
| 39 | `delete`, `deleteItem` delete the declaration with its items, or one item | CONFIRMED | `delete():303` calls `deleteItems():286`, which loops over `deleteItem():279`, then deletes the declaration row `:311`. The anchor covers the second half only |
| 40 | `createOrAlterTable`, `createOrAlterItemTable` create or change the two tables | CONFIRMED | `:368` and `:396`, both end in `dbDelta()` |
| 41 | `declarationToDbArray`, `declarationItemToDbArray` map the entities to the columns | CONFIRMED | `:321-339` and `:347-361` |
| 45 | The module owns the declaration table and the item table | CONFIRMED | `wpdbAdapter->packeteryCustomsDeclaration` `:384` and `packeteryCustomsDeclarationItem` `:412` |
| 49 | `id`, `order_id` — int and bigint, not null | CONFIRMED | `:371` `` `id` int(11) UNSIGNED NOT NULL AUTO_INCREMENT ``, `:372` `` `order_id` bigint(20) UNSIGNED NOT NULL `` |
| 50 | `ead`, `mrn` — varchar; export document number and movement reference number | CONFIRMED | `:373` `varchar(50) NOT NULL`, `:379` `varchar(32) NULL`. The anchor `declarationToDbArray` carries the names (`:325`, `:330`); the types come from `createOrAlterTable` in the same file |
| 51 | `invoice_number`, `invoice_issue_date`, `delivery_cost` — varchar, date and decimal | CONFIRMED | `:375` `varchar(255)`, `:376` `date`, `:374` `decimal(13,2) UNSIGNED` |
| 52 | `invoice_file`, `ead_file` — mediumblob, null; stored in the row itself | CONFIRMED | `:377` and `:380` `mediumblob NULL DEFAULT NULL`; written by `save()` `:206` and `:229` |
| 53 | `invoice_file_id`, `ead_file_id` — varchar, null; identifiers that Packeta gives to the stored files | CONFIRMED | `:378` and `:381` `varchar(255) NULL`; `Order/PacketSubmitter.php:251` `setInvoiceFileId( $invoiceFileResponse->getId() )` and `:287` for the export document |
| 54 | `id`, `customs_declaration_id` — int, not null | CONFIRMED | `:399` and `:400`, both `int(11) UNSIGNED NOT NULL` |
| 55 | `customs_code` varchar(8), `country_of_origin` char(2), stored in upper case | CONFIRMED | `:401` `varchar(8) NOT NULL`, `:406` `char(2) NOT NULL`; `:356` `strtoupper( $customsDeclarationItem->getCountryOfOrigin() )` |
| 56 | `product_name`, `product_name_en` — varchar; local language and English | CONFIRMED | `:404` `varchar(255) NULL DEFAULT NULL`, `:403` `varchar(255) NOT NULL` |
| 57 | `value`, `units_count`, `weight` — decimal and int | CONFIRMED | `:402` `decimal(13,2)`, `:405` `int(11)`, `:407` `decimal(10,3)` |
| 58 | `is_food_or_book`, `is_voc` — tinyint(1) | CONFIRMED | `:408` and `:409` `tinyint(1) NOT NULL`; cast at `:358-359` |
| 60-61 | The read asks only whether a file exists and loads the content when the caller wants it | CONFIRMED | `:84-85` `` `invoice_file` IS NOT NULL AS `has_invoice_file_content` `` and the same for `ead_file`; the content is fetched inside the closures `:102` and `:114` |
| 62-63 | The save leaves both files out of the row update and writes each one with its own statement | CONFIRMED | `declarationToDbArray():322-332` holds no file column at all; the files are written by separate `update()` calls `:204`, `:215`, `:227`, `:238`. With the default `$fieldsToOmit = [ 'invoice_file', 'ead_file' ]` `:184` those statements are skipped |
| 67-68 | The module depends on the entities of the domain module and on the database wrapper | CONFIRMED | `use` statements `:12-17`: `Core\CoreHelper`, `Core\Entity\CustomsDeclaration`, `Core\Entity\CustomsDeclarationItem`, `Module\WpdbAdapter` |
| 70 | references → packetery-core | CONFIRMED | `:12-14` |
| 71 | references → packetery-module-root | CONFIRMED | `:17` `use Packetery\Module\WpdbAdapter`; the class lives in `src/Packetery/Module/WpdbAdapter.php`, the path of `packetery-module-root` in `ai-docs/manifest.yaml:198-199` |
| 72 | references → packetery-module-entityfactory | CONFIRMED | `:15` `use Packetery\Module\EntityFactory`; module listed in `ai-docs/manifest.yaml:150` |
| 73 | references → packetery-module-exception | CONFIRMED | `:16` `use Packetery\Module\Exception\DeleteErrorException`; module listed in `ai-docs/manifest.yaml:154` |
| 70-73 | The list of four references is complete | CONFIRMED | `Repository.php` holds exactly six `use` statements (`:12-17`) and they name these four modules only; the file is the whole module. No `packetery-module-framework` or `packetery-module-payment` dependency exists here — neither module is referenced from this namespace |
| 75-76 | The module maps the rows to the declaration entity and the item entity of `packetery-core` [VERIFY: Repository.php#getCustomsDeclarationItemRows] | INCORRECT | The anchored symbol does not map anything. `getCustomsDeclarationItemRows():154-175` returns `$this->wpdbAdapter->get_results( sprintf( ... ), ARRAY_A )` — raw rows. The mapping is `$this->entityFactory->fromStandardizedStructure( $customsDeclarationRow, $orderNumber )` `:98` and `$this->entityFactory->createItemFromStandardizedStructure( $row )` `:148`; the anchor should name one of those |
| 77 | It builds those entities with the factory of `packetery-module-entityfactory` | CONFIRMED | `EntityFactory\CustomsDeclaration` injected `:44`, used `:98` and `:148` |
| 77-78 | It reaches the database through the wrapper of `packetery-module-root` | CONFIRMED | Every statement goes through `$this->wpdbAdapter` (`:56`, `:72`, `:155`, `:186`, `:194`, `:280`, `:311`, `:388`, `:416`); no direct `$wpdb` use |
| 78-79 | A failed delete raises the exception of `packetery-module-exception` [VERIFY: Repository.php#delete] | CONFIRMED | `delete():303` and `deleteItem():279` call `WpdbAdapter::delete()`, which does `throw new DeleteErrorException( ... )` at `Module/WpdbAdapter.php:195`; both methods carry `@throws DeleteErrorException` |
| 80 | The order module reads the declaration when it sends a packet, and its metabox writes it | CONFIRMED | `Order/Builder.php:222` `setCustomsDeclaration( ...getByOrderNumber( $order->getNumber() ) )`; `Order/PacketSubmitter.php:218-288` uses it and calls `save()` `:252`, `:288`; `Order/CustomsDeclarationMetabox.php:446`, `:477` |
| 80-82 | The deletion of an order deletes the declaration with its items in one call [VERIFY: Repository.php#deleteItem] | INCORRECT | The anchored symbol is the wrong one: `deleteItem( int $itemId ): void` `:279-281` deletes one item row and nothing else. The one call is `delete():303-312`, which runs `deleteItems()` and then deletes the declaration row; `Order/Repository.php:705` `$this->customsDeclarationRepository->delete( (string) $orderId )` is the caller |
| 82-84 | The schema of the two tables is created by the upgrade of `packetery-module-root`, which calls this repository | CONFIRMED | `Module/Upgrade.php:455-463` `createCustomsDeclarationTables()` calls `createOrAlterTable()` and `createOrAlterItemTable()`; `Upgrade.php` sits in `src/Packetery/Module`, the path of `packetery-module-root`, and `ai-docs/overview.md:98` says the same |
| 88-90 | The two documents live in the table as binary columns, so the table grows and a backup carries the files | CONFIRMED | `:377` and `:380` `mediumblob`; no external storage path in the module |
| 91 | The upload limit of a file belongs to the metabox of the order module and not to this module | CONFIRMED | `Order/CustomsDeclarationMetabox.php:38` `private const MAX_UPLOAD_FILE_MEGABYTES = 16;`, checked `:568`; `Repository.php` holds no size check |
| 93-95 | The names of the two file columns are a default argument of the save method, and the other column names are written in the statements | CONFIRMED | `:184` `array $fieldsToOmit = [ 'invoice_file', 'ead_file' ]`; every other column name is a literal in `:322-332`, `:348-360` and the two `CREATE TABLE` statements |
| 95-97 | Neither table holds an index other than the primary key, so a read by the order number scans the table | CONFIRMED | `:382` and `:410` `PRIMARY KEY (`id`)` is the only key in both statements; the read filters on `order_id` `:87` and `:58` |
| 98 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rcE 'TODO|FIXME' src/Packetery/Module/CustomsDeclaration/` = 0 |

CONFIRMED 45 · INCORRECT 2 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-customsdeclaration.yaml

The separate fragments no longer exist; the entries below are the ones of this module in `ai-docs/manifest.yaml`.

| entry | class | evidence |
|---|---|---|
| `modules[]` (`:134-137`) — name `packetery-module-customsdeclaration`, path `src/Packetery/Module/CustomsDeclaration`, lines 284, doc `reference/packetery-module-customsdeclaration.md` | CONFIRMED | Directory exists with one `.php` file; non-blank non-comment lines of that file = 284; the document file exists. The entry carries no `evidence:` key, like every other entry of the `modules:` list in this manifest |
| `exposes[]` (`:403-410`) — kind library, name `getByOrderNumber`, module `packetery-module-customsdeclaration`, consumers `[woocommerce]`, auth none, evidence `Repository.php#getByOrderNumber`, notes "Gives the customs declaration of an order with its items." | CONFIRMED | `Repository.php:71` is public and checks no capability and no nonce; `:124` attaches the items. Consumers inside this repository only: `Order/Builder.php:222` and `Order/CustomsDeclarationMetabox.php:206`, `:225`, `:438` |
| `owns_data[]` (`:700-705`) — entity `customs-declaration`, store mysql, module `packetery-module-customsdeclaration`, evidence `Repository.php#createOrAlterTable`, notes "Two tables: the declaration of an order and its items. The invoice file and the export document are stored in the declaration row." | CONFIRMED | `createOrAlterTable():368` and `createOrAlterItemTable():396` create the two tables; `invoice_file` `:377` and `ead_file` `:380` are columns of the declaration table only |
| No other entry names this module: no `events`, no `interfaces`, no `calls[]` and no `unknowns[].resolved_by` | CONFIRMED | `grep -n customsdeclaration ai-docs/manifest.yaml` returns the alias `:9`, the module entry `:134`, `:137`, the `exposes` entry `:405` and the `owns_data` entry `:704` only; `grep -n 'resolved_by: packetery-module-customsdeclaration'` returns nothing |

CONFIRMED 4 · INCORRECT 0 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
