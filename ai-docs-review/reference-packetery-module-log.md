## ai-docs/reference/packetery-module-log.md @ b34fe03c — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17-19 | Every call to Packeta, every carrier update and every failed table creation writes one row into the log table [VERIFY: DbLogger.php#add] | INCORRECT | `DbLogger::add()` (DbLogger.php:33-39) stores whatever `Record` the caller passes; it proves no completeness. `Core\Log\ILogger` is injected only under `Module/Carrier`, `Module/Labels`, `Module/Log`, `Module/Options`, `Module/Order` and `Module/Upgrade.php`, and `ArgumentTypeErrorLogger::log()` (ArgumentTypeErrorLogger.php:37) writes through `$this->wcAdapter->getLogger()` to the WooCommerce log, not into the log table. |
| 19-20 | The administrator reads those rows on an admin page with a filter [VERIFY: Page.php#createLogListUrl] | CONFIRMED | `Page::SLUG = 'packeta-logs'` (Page.php:31), `register()` (Page.php:78), `render()` builds the filter from `LogFilterFormFactory` (Page.php:52, 154); `createLogListUrl()` (Page.php:322) adds `order_id` and `log_action` to the link. |
| 22-24 | packetery-module-log implements the logger interface of the domain module, so the other modules write through one service and know nothing about the storage [VERIFY: Repository.php#remapToRecord] | INCORRECT | The anchor does not show the interface. The implementation is `class DbLogger implements ILogger` (DbLogger.php:22, interface `Packetery\Core\Log\ILogger`). `Repository::remapToRecord()` (Repository.php:98) only maps database rows to `Record` objects. |
| 24 | The module holds 8 files | CONFIRMED | `find src/Packetery/Module/Log -name '*.php' | wc -l` = 8 (no test file under that path). |
| 24-25 | 727 lines of logic | CONFIRMED | Non-empty, non-comment lines of those 8 files = 727. Matches `lines: 727` in ai-docs/manifest.yaml:176. |
| 25 | 41 public methods | CONFIRMED | `grep -cE '^\s*(final )?public( static)? function'` over the 8 files = 41 (Repository 8, LogSizeLimiterState 13, DbLogger 6, Page 5, LogSizeLimiter 3, ListTableFork 2, ArgumentTypeErrorLogger 2, Purger 2). |
| 25-26 | The settings export of the plugin reads the records of the last days through the same service [VERIFY: DbLogger.php#getForPeriodAsArray] | CONFIRMED | `Module/Options/Exporter.php:177` calls `$this->dbLogger->getForPeriodAsArray( [ [ 'after' => '5 days ago' ] ] )`. |
| 26-28 | A second class cuts the diagnostic file of the plugin to a size limit [VERIFY: LogSizeLimiter.php#getLimitedFilePartAsString] | CONFIRMED | `LogSizeLimiter::getLimitedFilePartAsString()` (LogSizeLimiter.php:25) reads the file and keeps the records that fit `logLimitSizeBytes`; the caller is `Module/Email/BugReportAttachment.php`. |
| 30 | `> ⚠ add business context (elicitation)` | PENDING | Standard elicitation placeholder. |
| 34 | packetery-module-log exposes the logger, the admin page and two helpers | INCORRECT | The set is larger than two helpers, and the table right below contradicts it: besides `DbLogger` and `Page` it lists `Purger::autoDeleteHook` (Purger.php:43), `LogSizeLimiter` (LogSizeLimiter.php:10), `ArgumentTypeErrorLogger` (ArgumentTypeErrorLogger.php:9) and `ListTableFork` (ListTableFork.php:9); `Repository` and `LogSizeLimiterState` are further classes of the module. |
| 38 | `add` stores one record and sets its date when the caller gives none [VERIFY: DbLogger.php#add] | CONFIRMED | DbLogger.php:34-36 `if ( $record->date === null ) { $record->date = CoreHelper::now(); }`, then `$this->logRepository->save( $record )`. |
| 39 | `getRecords`, `countRecords` give the records of a filter and their number [VERIFY: DbLogger.php#getRecords] | CONFIRMED | DbLogger.php:47 `find()`, DbLogger.php:61-65 `buildQueryConditions()` + `countRows()`. |
| 40 | `getForPeriodAsArray` gives the records of a time range, oldest first, for the settings export [VERIFY: DbLogger.php#getForPeriodAsArray] | CONFIRMED | DbLogger.php:82 `setOrderBy( [ 'date' => 'ASC' ] )`; caller `Module/Options/Exporter.php:177`. |
| 41 | `deleteOld` deletes the records older than a date modifier [VERIFY: DbLogger.php#deleteOld] | CONFIRMED | DbLogger.php:70 delegates to Repository.php:84-89 `DELETE FROM ... WHERE \`date\` < %s` with `CoreHelper::now()->modify( $before )`. |
| 42 | `autoDeleteHook` deletes the old records with the window from the plugin option [VERIFY: Purger.php#autoDeleteHook] | CONFIRMED | Purger.php:44 `deleteOld( get_option( self::PURGER_OPTION_NAME, self::PURGER_MODIFIER_DEFAULT ) )`. |
| 43 | `packeta-logs` is the admin page with the filter, the pagination and the manual deletion [VERIFY: Page.php#SLUG] | CONFIRMED | Page.php:31 `SLUG`; filter form Page.php:154; pagination Page.php:375-386 via `ListTableFork`; manual deletion Page.php:92-101 and 222. |
| 44 | `register` adds the page under the Packeta dashboard [VERIFY: Page.php#register] | CONFIRMED | Page.php:78-85 `add_submenu_page( DashboardPage::SLUG, ... )`. |
| 45 | `createLogListUrl`, `hasAnyRows` build the link to the log of one order and answer whether it holds a row [VERIFY: Page.php#hasAnyRows] | CONFIRMED | Page.php:306-308 `countRows( $orderId ) > 0`; Page.php:322-338 builds the link with `PARAM_ORDER_ID`. |
| 46 | `getLimitedFilePartAsString`, `isFileFreshEnough` cut a text log file to a size limit and an age limit [VERIFY: LogSizeLimiter.php#getLimitedFilePartAsString] | CONFIRMED | LogSizeLimiter.php:25-70 (size) and LogSizeLimiter.php:121-133 (`filemtime` against `getCutoffTimestamp()`). |
| 47 | `log` writes a wrong argument type into the WooCommerce log [VERIFY: ArgumentTypeErrorLogger.php#LEVEL_ERROR] | CONFIRMED | ArgumentTypeErrorLogger.php:37-57 builds the message and ArgumentTypeErrorLogger.php:24-28 writes it with `self::LEVEL_ERROR` through `$this->wcAdapter->getLogger()`. |
| 49 | The log page needs the `manage_woocommerce` capability | CONFIRMED | Page.php:83 passes `'manage_woocommerce'` to `add_submenu_page`. |
| 49-50 | It shows 50 records on one page [VERIFY: Page.php#ITEMS_PER_PAGE] | CONFIRMED | Page.php:44 `private const ITEMS_PER_PAGE = 50;`, used at Page.php:128, 134-135, 379. |
| 50-52 | The pagination comes from a small child of the WordPress list table, which makes two protected methods public [VERIFY: ListTableFork.php#renderPagination] | CONFIRMED | ListTableFork.php:9 `extends WP_List_Table`; ListTableFork.php:13 `setPaginationArgs()` calls `set_pagination_args()` and ListTableFork.php:20 `renderPagination()` calls `pagination()`. Both parent methods are protected in WordPress core, which is not vendored in this repository. |
| 56-57 | The module owns one table and the name of that table comes from the database wrapper of the plugin [VERIFY: Repository.php#createOrAlterTable] | CONFIRMED | Repository.php:150 `'CREATE TABLE ' . $this->wpdbAdapter->packeteryLog`; it is the only `CREATE TABLE` in the module. |
| 61 | log.`id` int(11), auto increment, primary key [VERIFY: Repository.php#createOrAlterTable] | CONFIRMED | Repository.php:151 and Repository.php:158 `PRIMARY KEY  (\`id\`)`. |
| 62 | log.`order_id` bigint(20) unsigned, null [VERIFY: Repository.php#buildQueryConditions] | INCORRECT | The anchor does not carry the claim. `buildQueryConditions()` (Repository.php:232-263) handles only the date range, the status and the search and never names `order_id`; the column is declared in `createOrAlterTable` (Repository.php:152) and the order filter is built in `getWhereClause`: `$where[] = $this->wpdbAdapter->prepare( '\`order_id\` = %d', $orderId );` (Repository.php:215). |
| 63 | log.`title` varchar(255) [VERIFY: Repository.php#save] | CONFIRMED | Repository.php:153 declares the column; `save()` writes `'title' => $record->title` (Repository.php:194). |
| 64 | log.`params` text, values stored as text [VERIFY: Repository.php#remapToRecord] | CONFIRMED | Repository.php:154 `\`params\` text NOT NULL`; `save()` stores `wp_json_encode` output (Repository.php:183) and `remapToRecord()` reads it back with `json_decode` (Repository.php:109). |
| 65 | log.`status` varchar(255), `STATUS_SUCCESS` or `STATUS_ERROR` [VERIFY: Page.php#getTranslatedActions] | INCORRECT | `getTranslatedActions()` (Page.php:203) maps the action constants only and contains no status. The statuses are translated in `render()`: `Record::STATUS_ERROR => ...`, `Record::STATUS_SUCCESS => ...` (Page.php:105-108); the constants live in `src/Packetery/Core/Log/Record.php:38-39` and the column in Repository.php:155. |
| 66 | log.`action` varchar(255), for example the packet sending or the carrier list update [VERIFY: Page.php#getTranslatedActions] | CONFIRMED | Repository.php:156; `Record::ACTION_PACKET_SENDING` (Core/Log/Record.php:20) and `Record::ACTION_CARRIER_LIST_UPDATE` (Core/Log/Record.php:24) are among the keys of `getTranslatedActions()` (Page.php:203). |
| 67 | log.`date` datetime [VERIFY: DbLogger.php#add] | CONFIRMED | Repository.php:157 `\`date\` datetime NOT NULL`; DbLogger.php:34-36 fills it. |
| 69-70 | The action values and the status values are constants of the domain module, and this module only stores and translates them [VERIFY: Repository.php#remapToRecord] | CONFIRMED | `src/Packetery/Core/Log/Record.php:20-39` holds the `ACTION_*` and `STATUS_*` constants; `remapToRecord()` copies `$log->status` and `$log->action` unchanged (Repository.php:102, 105) and `Page::getTranslatedActions()` only translates them. |
| 71-72 | The filter of the page builds the SQL conditions from the order number, the action, the status, the date range and a text search [VERIFY: Repository.php#buildQueryConditions] | INCORRECT | `buildQueryConditions()` (Repository.php:232-263) builds three of the five: the date range (`\`date\` >= %s`, `\`date\` <= %s`), the status and the search (`(\`title\` LIKE %s OR \`params\` LIKE %s)`). The order number and the action are added afterwards in `getWhereClause()` (Repository.php:213-219). |
| 76-79 | A scheduled job deletes the records older than the window of the plugin option, and the default window is in a constant of the purger [VERIFY: Purger.php#PURGER_MODIFIER_DEFAULT] | CONFIRMED | Purger.php:20 `PURGER_MODIFIER_DEFAULT = '- 90 days'`, used as the fallback of `get_option` at Purger.php:44. |
| 79 | The option name is a constant of the same class [VERIFY: Purger.php#PURGER_OPTION_NAME] | CONFIRMED | Purger.php:19 `PURGER_OPTION_NAME`. |
| 81-83 | The administrator deletes the old records from the page as well, and that action uses a fixed window of seven days with a nonce check [VERIFY: Page.php#NONCE_DELETE_OLD] | CONFIRMED | Page.php:42 `private const NONCE_DELETE_OLD`; Page.php:94-96 `verifyNonce( $nonce, self::NONCE_DELETE_OLD )`; Page.php:222-223 `$this->logger->deleteOld( '-7 days' )`. |
| 83-86 | The module keeps the newest records that fit into a size limit and an age limit, and it drops a record that is larger than the limit by itself [VERIFY: LogSizeLimiter.php#isFileFreshEnough] | INCORRECT | The anchor does not carry the claim. `isFileFreshEnough()` (LogSizeLimiter.php:121-133) only compares `filemtime` with the cutoff. The described behaviour is in `flushCurrentRecord()` (LogSizeLimiter.php:75-99): `if ( $recordLength > $this->logLimitSizeBytes ) { $state->emptyCurrentRecordLines(); return; }` and the `while` loop that shifts the oldest records off the queue. |
| 86-87 | The state of that cut lives in a small object [VERIFY: LogSizeLimiterState.php#LogSizeLimiterState] | CONFIRMED | LogSizeLimiterState.php:7 `class LogSizeLimiterState`, instantiated at LogSizeLimiter.php:36. |
| 91-92 | The module depends on the domain module for the record shape and on the plugin for the page | CONFIRMED | `use Packetery\Core\Log\Record` (DbLogger.php:15, Page.php:14) and `use Packetery\Module\Dashboard\DashboardPage` (Page.php:16). |
| 94 | references → packetery-core | CONFIRMED | `Packetery\Core\CoreHelper`, `Packetery\Core\Log\ILogger`, `LogPageArguments`, `Record`. |
| 95 | references → packetery-module-root | CONFIRMED | `Packetery\Module\WpdbAdapter` (Repository.php:11), `Packetery\Module\ModuleHelper`, `Packetery\Module\MessageManager` (Page.php:19-20). |
| 96 | references → packetery-module-forms | CONFIRMED | `Packetery\Module\Forms\LogFilterFormFactory` (Page.php:17). |
| 97 | references → packetery-module-dashboard | CONFIRMED | `Packetery\Module\Dashboard\DashboardPage` (Page.php:16). |
| 98 | references → packetery-module-views | CONFIRMED | `Packetery\Module\Views\UrlBuilder` (Page.php:21). |
| 94-98 | The structured lines are the dependency set of the module | INCORRECT | The set is incomplete: the module also uses `Packetery\Module\Framework\WcAdapter` and `Packetery\Module\Framework\WpAdapter` (ArgumentTypeErrorLogger.php:6-7, Page.php:18), so `references → packetery-module-framework` is missing. `WpAdapter` is a constructor dependency of `Page` (Page.php:51, 61) and both adapters are constructor dependencies of `ArgumentTypeErrorLogger` (ArgumentTypeErrorLogger.php:16). |
| 100-101 | The module implements the logger interface of `packetery-core` and stores its record objects [VERIFY: DbLogger.php#countRecords] | INCORRECT | The anchor does not carry the claim. `countRecords()` (DbLogger.php:61-65) returns a row count and stores nothing. The implementation is `class DbLogger implements ILogger` (DbLogger.php:22) and the storing is `add()` → `Repository::save()` (DbLogger.php:38, Repository.php:172). |
| 101-102 | It reaches the database through the wrapper of `packetery-module-root` [VERIFY: Repository.php#save] | CONFIRMED | Repository.php:201 `$this->wpdbAdapter->insertReplaceHelper( $this->wpdbAdapter->packeteryLog, $data, null, 'REPLACE' )`; `WpdbAdapter` is `Packetery\Module\WpdbAdapter`. |
| 102-103 | The filter form comes from `packetery-module-forms` | CONFIRMED | `LogFilterFormFactory` is a constructor dependency (Page.php:52, 62) and is used in `render()` (Page.php:154). |
| 103-104 | The page is a child of the dashboard page of `packetery-module-dashboard` [VERIFY: Page.php#register] | CONFIRMED | Page.php:79 passes `DashboardPage::SLUG` as the parent slug. |
| 104-106 | The links of the page use the URL builder of `packetery-module-views` [VERIFY: Page.php#createLogListUrl] | INCORRECT | `createLogListUrl()` (Page.php:322-338) builds the link with `$this->wpAdapter->addQueryArg( $params, admin_url( 'admin.php' ) )` and never touches `UrlBuilder`; `createPageUrl()` (Page.php:340) does the same. `UrlBuilder` appears only at Page.php:174-175, where `buildAssetUrl()` returns the logo image URLs. |
| 110-113 | The manual deletion uses a fixed window of seven days as a literal, while the scheduled deletion has a constant and an option [VERIFY: Page.php#deleteOldLogs] | CONFIRMED | Page.php:222-223 `deleteOld( '-7 days' )` against Purger.php:19-20 and Purger.php:44. |
| 115-116 | The schedule of the automatic deletion is not in this namespace: the module gives the callback and the plugin registers it [VERIFY: Purger.php#autoDeleteHook] | CONFIRMED | `Module/CronService.php:82` `add_action( self::CRON_LOG_AUTO_DELETION_HOOK, [ $this->logPurger, 'autoDeleteHook' ] )`; `src/Packetery/Module/Log/` contains no `add_action` or `wp_schedule_*` for the purge. |
| 116-118 | The log table has no index other than the primary key, so a filter over a large table reads the whole table [VERIFY: Repository.php#createOrAlterTable] | CONFIRMED | Repository.php:150-159 declares only `PRIMARY KEY  (\`id\`)`; the filter queries use `WHERE` on `order_id`, `action`, `status`, `date` and `LIKE` on `title`/`params`. |
| 118-119 | The page capability is a literal of the registration | CONFIRMED | Page.php:83 passes the literal `'manage_woocommerce'`, not a constant or an adapter call. |
| 119-120 | It differs from the capability of the other Packeta pages [VERIFY: Page.php#register] | INCORRECT | Three other Packeta pages use the same capability: `Module/Dashboard/DashboardPage.php:68`, `Module/Order/CollectionPrint.php:213` and `Module/Order/LabelPrint.php:278` all pass `'manage_woocommerce'`. Only `Module/Options/Page.php:226` and `Module/Carrier/OptionsPage.php:105` use `'manage_options'`. |
| 120-121 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rniE 'TODO|FIXME' src/Packetery/Module/Log/` returns 0 matches. |

CONFIRMED 44 · INCORRECT 11 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-log.yaml

Separate fragments no longer exist. The entries below were verified in `ai-docs/manifest.yaml`.

| entry | class | evidence |
|---|---|---|
| `aliases` — `packetery-module-log` (manifest.yaml:15) | CONFIRMED | The module id matches the `module:` field of the front matter of the document. |
| `modules[packetery-module-log].path: src/Packetery/Module/Log` (manifest.yaml:175) | CONFIRMED | The directory exists and matches `covers: [src/Packetery/Module/Log]` in the front matter. |
| `modules[packetery-module-log].lines: 727` (manifest.yaml:176) | CONFIRMED | Non-empty, non-comment lines of the 8 `src` PHP files = 727. |
| `modules[packetery-module-log].doc: reference/packetery-module-log.md` (manifest.yaml:177) | CONFIRMED | `ai-docs/reference/packetery-module-log.md` exists and is the document under review. |
| `modules[packetery-module-log].evidence` | NOT FOUND | The key is absent. It is absent from the whole `modules:` block (`sed -n '113,213p' ai-docs/manifest.yaml | grep -c evidence` = 0), so this is the schema of the section, not a defect of this module. |
| `exposes[kind: library, name: add].evidence: src/Packetery/Module/Log/DbLogger.php#add` (manifest.yaml:301-306) | CONFIRMED | `DbLogger::add( Record $record )` exists at DbLogger.php:33 and is the `ILogger` entry point. |
| `exposes[kind: library, name: add].notes` — "Every module writes its API results through it" | INCORRECT | `Packetery\Core\Log\ILogger` is injected only under `Module/Carrier`, `Module/Labels`, `Module/Log`, `Module/Options`, `Module/Order` and `Module/Upgrade.php`. `ArgumentTypeErrorLogger::log()` in this very module writes through `$this->wcAdapter->getLogger()` instead (ArgumentTypeErrorLogger.php:22-28), and `Module/DiagnosticsLogger/DiagnosticsLogger.php` is a further logger outside this path. |
| `exposes[kind: html, name: packeta-logs]` (manifest.yaml:479-486) | CONFIRMED | `Page::SLUG = 'packeta-logs'` (Page.php:31); `auth: wordpress-capability` and the note about `manage_woocommerce` match Page.php:83. |
| `owns_data[entity: log]` (manifest.yaml:708-712) | CONFIRMED | `store: mysql` and `evidence: Repository.php#createOrAlterTable` match Repository.php:149-162; the note "It holds no index other than the primary key" matches Repository.php:158. |
| `calls:` and `unknowns[].resolved_by` — no entry names packetery-module-log | CONFIRMED | `grep -n 'packetery-module-log' ai-docs/manifest.yaml` returns only lines 15, 174, 305, 482 and 710. The module makes no outbound call to an external service: its only I/O is the WordPress database through `WpdbAdapter` and the local file read in `LogSizeLimiter`. |

CONFIRMED 8 · INCORRECT 1 · UNCERTAIN 0 · NOT FOUND 1 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
