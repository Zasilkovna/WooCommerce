## ai-docs/reference/packetery-module-email.md @ b34fe03c — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 17-20 | The module puts Packeta order data into shop emails; the shop owner writes a shortcode and the module replaces it with the value of that order [VERIFY: EmailShortcodes.php#register] | CONFIRMED | `EmailShortcodes.php:26-42` registers 14 shortcodes through `wpAdapter->addShortcode()`; each callback resolves the order and returns its value (`trackingNumber:59-66`) |
| 17-18 | The module sends the bug report of the administrator to the Packeta support | CONFIRMED | `BugReportEmail.php:36-61` `sendBugReport()` sends to `$this->supportEmailAddress`; the address is injected in `config/config.neon:12` (value not quoted) |
| 20 | The module holds 3 files | CONFIRMED | `find src/Packetery/Module/Email -name '*.php' \| wc -l` = 3 (BugReportAttachment, BugReportEmail, EmailShortcodes); no test files under the module directory |
| 20-21 | 347 lines of logic | CONFIRMED | non-blank non-comment lines of the same 3 files = 347 (113 + 61 + 173); matches `ai-docs/manifest.yaml:148` `lines: 347` |
| 21 | 21 public methods | CONFIRMED | `grep -cE '^\s*(final )?public( static)? function'` = 2 (BugReportEmail) + 3 (BugReportAttachment) + 16 (EmailShortcodes) = 21 |
| 23-25 | The bug report collects the settings export, the WooCommerce system status and the recent log files in one archive [VERIFY: BugReportAttachment.php#createAttachments] | CONFIRMED | `BugReportAttachment.php:56-60`: `addFromString('packeta-settings.txt', $this->exporter->getExportContent())`, `addWooCommerceSystemStatusToZip()`, `addPacketaWcLogsToZip()`, `addWpDebugLogToZip()`, `addTracyLogsToZip()` |
| 27 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 31 | The module exposes fourteen shortcodes and the bug report | CONFIRMED | 14 `addShortcode()` calls in `EmailShortcodes.php:27-41`; `BugReportEmail.php:36` `sendBugReport()` |
| 35 | `register` registers every Packeta shortcode of the email templates [VERIFY: EmailShortcodes.php#register] | CONFIRMED | `EmailShortcodes.php:26-42`; no other `addShortcode()` call exists in the module |
| 36 | `packeta_tracking_number`, `packeta_tracking_url` give the packet barcode and the tracking address [VERIFY: #trackingNumber] | CONFIRMED | `EmailShortcodes.php:65` `$order->getPacketBarcode()`, `:77` `$order->getPacketTrackingUrl()` |
| 37 | `packeta_pickup_point_id`, `..._name`, `..._address` give the identifier, the name and the full address of the pickup point [VERIFY: #pickupPointAddress] | CONFIRMED | `EmailShortcodes.php:94` `getId()`, `:128` `getName()`, `:145` `getFullAddress()` |
| 38 | `packeta_pickup_place`, `..._street`, `..._city`, `..._zip` give one part of the pickup point address [VERIFY: #pickupPointStreet] | CONFIRMED | `EmailShortcodes.php:111` `getPlace()`, `:162` `getStreet()`, `:179` `getCity()`, `:196` `getZip()` |
| 38 | `packeta_pickup_point_country` gives one part of the pickup point address | INCORRECT | `EmailShortcodes.php:213` returns `$order->getShippingCountry() ?? ''`, not a pickup point field; the page itself says so at lines 74-78, so the table contradicts its own limitations section |
| 39 | `packeta_carrier_name` gives the name of the carrier of the order [VERIFY: #carrierName] | CONFIRMED | `EmailShortcodes.php:225` `return $order->getCarrier()->getName();` |
| 40 | `packeta_if_packet_submitted`, `packeta_if_pickup_point`, `packeta_if_carrier` show their content only when the order matches the condition [VERIFY: #ifPacketSubmitted] | CONFIRMED | `EmailShortcodes.php:231-238` (`getPacketId() === null` → `''`), `:243-250` (`getPickupPoint() === null` → `''`), `:255-266` (`isExternalCarrier() === false` → `''`); otherwise `doShortcode( $content )` |
| 41 | `sendBugReport` sends the message of the administrator with the diagnostic archive [VERIFY: BugReportEmail.php#sendBugReport] | CONFIRMED | `BugReportEmail.php:55-61` `wpMail( $to, $subject, body, $headers, $this->bugReportAttachment->createAttachments() )` |
| 42 | `createAttachments` builds the archive of the settings export, the system status and the logs [VERIFY: BugReportAttachment.php#createAttachments] | CONFIRMED | `BugReportAttachment.php:43-69` |
| 43 | `addFreshLogsToZipByPrefix` adds the recent log files of one name prefix to the archive [VERIFY: #addFreshLogsToZipByPrefix] | CONFIRMED | `BugReportAttachment.php:130-145`: `strpos( $fileName, $prefix ) === 0 && $this->logSizeLimiter->isFileFreshEnough( $filePath )` |
| 45-46 | Every shortcode takes the order number in one attribute and gives empty text when the order does not exist [VERIFY: #ifPickupPoint] | CONFIRMED | `EmailShortcodes.php:47-54` `findOrder()` reads `$shortcodeAttributes['order_id']`; every one of the 14 callbacks calls it and returns `''` on `null` (e.g. `:244-247`) |
| 46-48 | The archive stays empty when the installation has no archive extension of PHP [VERIFY: #createAttachments] | CONFIRMED | `BugReportAttachment.php:45-47` `if ( ! class_exists( 'ZipArchive' ) ) { return $attachments; }` with `$attachments = []` |
| 54 | references → packetery-core | CONFIRMED | `EmailShortcodes.php:7` `use Packetery\Core\Entity\Order;` |
| 55 | references → packetery-module-root | INCORRECT | No symbol of `packetery-module-root` (manifest path `src/Packetery/Module`, manifest.yaml:198-201) is used. The full set of namespaced references of the module is `Packetery\Core\Entity\Order`, `Packetery\Latte\Engine`, `Packetery\Module\Framework\WcAdapter`, `Packetery\Module\Framework\WpAdapter`, `Packetery\Module\Log\LogSizeLimiter`, `Packetery\Module\Options\Exporter`, `Packetery\Module\Order\Repository`, `Packetery\Tracy\Debugger`. The constant `PACKETERY_PLUGIN_DIR` (`BugReportEmail.php:66`) is defined in `constants.php:2` at the repository root, outside `src/Packetery/Module` |
| 56 | references → packetery-module-order | CONFIRMED | `EmailShortcodes.php:9` `use Packetery\Module\Order\Repository;` |
| 57 | references → packetery-module-options | CONFIRMED | `BugReportAttachment.php:10` `use Packetery\Module\Options\Exporter;` |
| 58 | references → packetery-module-log | CONFIRMED | `BugReportAttachment.php:9` `use Packetery\Module\Log\LogSizeLimiter;` |
| 54-58 | The structured lines are the dependency set of the module | INCORRECT | `packetery-module-framework` is missing although all three files depend on it: `EmailShortcodes.php:8` and `BugReportEmail.php:8` `use Packetery\Module\Framework\WpAdapter;`, `BugReportAttachment.php:7-8` `WcAdapter` + `WpAdapter`. The module exists in the manifest (`ai-docs/manifest.yaml:162-165`, `path: src/Packetery/Module/Framework`) |
| 60-61 | The shortcodes read the order entity and its pickup point of `packetery-core` through the repository of `packetery-module-order` [VERIFY: #pickupPointName] | CONFIRMED | `EmailShortcodes.php:53` `$this->orderRepository->findById()`, `:123` `$order->getPickupPoint()`; `Repository` is `Packetery\Module\Order`, `Order` is `Packetery\Core\Entity` |
| 62-64 | The archive takes the settings export of `packetery-module-options` and the size limit of the diagnostic file of `packetery-module-log` [VERIFY: #addTracyLogsToZip] | CONFIRMED | `BugReportAttachment.php:56` `$this->exporter->getExportContent()`, `:121` `$this->logSizeLimiter->getLimitedFilePartAsString()`. The anchor is imprecise: `addTracyLogsToZip:106-117` reaches the size limiter only through `addLimitedLogContentsToZip()` and holds no `exporter` call; the exporter evidence is `createAttachments:56` |
| 64-66 | The address of the support comes to the email class as a constructor argument, so its value lives outside this module [VERIFY: BugReportEmail.php#createEmailBody] | INCORRECT | The anchor does not evidence the claim: `createEmailBody:64-82` never touches `supportEmailAddress`. The claim itself holds at `BugReportEmail.php:26` (`string $supportEmailAddress` in `__construct`) and `config/config.neon:12` + `:54` (value not quoted); the anchor should point at the constructor |
| 66-68 | The email leaves the shop through WordPress; the module adds the archive as attachment and the sender address as the reply address [VERIFY: #sendBugReport] | CONFIRMED | `BugReportEmail.php:55-61` `$this->wpAdapter->wpMail(...)` with `createAttachments()` as the fifth argument; `:47` header `Reply-To: {$replyEmail}` |
| 69-70 | The shortcodes need no other module at runtime, because the order repository gives them the whole order entity [VERIFY: #carrierName] | INCORRECT | They need `packetery-module-framework` at runtime: `EmailShortcodes.php:18-21` takes `WpAdapter` in the constructor, `:27-41` `register()` calls `$this->wpAdapter->addShortcode()` and `:237`, `:249`, `:265` call `$this->wpAdapter->doShortcode( $content )` while rendering. They also need `packetery-module-order` at runtime (`:53` `$this->orderRepository->findById()`), which the sentence itself names |
| 74-78 | `packeta_pickup_point_country` gives the shipping country of the order, not the country of the pickup point [VERIFY: #pickupPointCountry] | CONFIRMED | `EmailShortcodes.php:202-214`: it loads the pickup point, returns early when it is `null`, then returns `$order->getShippingCountry() ?? ''` |
| 78 | A template that uses it for a pickup point order shows the country of the customer | CONFIRMED | follows from `EmailShortcodes.php:213`; the shipping country is the address of the customer |
| 80-81 | The names of the log files and the path of the WordPress debug file are fixed strings of the archive class [VERIFY: #addWpDebugLogToZip] | CONFIRMED | `BugReportAttachment.php:97` `WP_CONTENT_DIR . '/debug.log'`, `:102` target name `'debug.log'`; the other fixed names are `:112` `[ 'packeta.log', 'exception.log', 'error.log' ]` and the prefixes `:90` `'packeta-'`, `:116` `'exception-'` |
| 81-83 | The bug report carries the shop settings, so it inherits the masking of the settings export and nothing more | CONFIRMED | `BugReportAttachment.php:56` adds `$this->exporter->getExportContent()` unchanged; the only masking is in `src/Packetery/Module/Options/Exporter.php:130-135` (`api_password`); the system status and the log files are added verbatim (`:81`, `:123`, `:142`) |
| 83 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rniE 'todo\|fixme' src/Packetery/Module/Email/` returns 0 hits |

CONFIRMED 30 · INCORRECT 5 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-email.yaml

Verified against `ai-docs/manifest.yaml` (the separate fragments no longer exist).

| entry | class | evidence |
|---|---|---|
| `modules:` packetery-module-email — `path: src/Packetery/Module/Email` (manifest.yaml:146-147) | CONFIRMED | the directory exists and holds the 3 module files |
| `modules:` packetery-module-email — `lines: 347` (manifest.yaml:148) | CONFIRMED | non-blank non-comment lines of `src/Packetery/Module/Email/*.php` = 347; agrees with line 20-21 of the document |
| `modules:` packetery-module-email — `doc: reference/packetery-module-email.md` (manifest.yaml:149) | CONFIRMED | the file exists and its front matter has `module: packetery-module-email`, `covers: [src/Packetery/Module/Email]` |
| `exposes:` `kind: other`, `name: packeta_tracking_number`, `module: packetery-module-email`, `consumers: [woocommerce]`, `auth: none`, `evidence: EmailShortcodes.php#trackingNumber`, notes "One of fourteen shortcodes…" (manifest.yaml:505-512) | CONFIRMED | `EmailShortcodes.php:27` registers the shortcode to `trackingNumber`, defined at `:59-66`; the shortcode runs in an email template with no capability check; 14 shortcodes counted at `:27-41` |
| `exposes:` `kind: other`, `name: sendBugReport`, `module: packetery-module-email`, `consumers: [woocommerce]`, `auth: none`, `evidence: BugReportEmail.php#sendBugReport`, notes "Sends the message of the administrator and the diagnostic archive to the Packeta support." (manifest.yaml:608-615) | CONFIRMED | `BugReportEmail.php:36-61`. `auth: none` follows the convention of the file — every `kind: other` and `kind: library` PHP surface carries `auth: none`. Note for the maintainer: the only caller is `Forms/BugReportForm.php:72`, rendered by the options page that is registered with `manage_options` (`Options/Page.php:214`, `:226`) |
| completeness — every `module: packetery-module-email` / `resolved_by: packetery-module-email` record | CONFIRMED | `grep -n 'packetery-module-email' ai-docs/manifest.yaml` gives lines 11 (aliases), 146, 149, 507, 609 and nothing else; `resolved_by:` does not occur in the file at all; `calls:` holds only Packeta services (pickup point API, SOAP API, widget) and `owns_data:` no email entry, which matches a module that owns no table and calls no Packeta service |

CONFIRMED 6 · INCORRECT 0 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0

Note: the `modules:` schema has no `evidence:` field — all 25 entries carry `path`, `lines`, `doc` only, and `validate.py:812` requires `evidence:` for `exposes:`, `calls:` and `owns_data:` entries only.
