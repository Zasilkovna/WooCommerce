## ai-docs/reference/packetery-module-labels.md @ b34fe03c — 2026-09-22

Verified against `/Users/pf/projects/workshop/WooCommerce`, branch `docs/teams`, HEAD `a6ef312a`.
Module directory from `covers:` — `src/Packetery/Module/Labels`.

Counting convention (evidence for the size claims):
`find src/Packetery/Module/Labels -name '*.php' | wc -l` → 4 (no test file under `src/`);
non-empty non-comment lines of those 4 files → 291;
`grep -cE '^\s*(final )?public( static)? function'` over those files → 6 + 2 + 8 + 3 = 19
(constructors included).

| line | claim | class | evidence |
|---|---|---|---|
| 17–19 | Reads the courier number of a packet from Packeta and keeps it on the order [VERIFY: CarrierLabelService.php#getPacketaPacketIdsWithCourierNumbers] | CONFIRMED | `CarrierLabelService.php:81` `$this->soapApiClient->packetCourierNumber( $request )`; the number is stored at `:171` `$order->setCarrierNumber( $response->getNumber() )` + `:172` `save()` |
| 20–21 | Builds the print parameters: the label format and the number of label fields to skip [VERIFY: LabelPrintParametersService.php#getOffset] | CONFIRMED | `LabelPrintParametersService.php:77-94` `getOffset()`; format at `:117-122` `getLabelFormat()` |
| 23 | packetery-module-labels prints nothing by itself | CONFIRMED | no PDF or print call in the module: `grep -riE 'pdf\|->print\|header\(' src/Packetery/Module/Labels/` → 0 hits |
| 23–25 | The print pages of the order module ask this module and then call Packeta [VERIFY: LabelPrintParametersService.php#getLabelFormat] | CONFIRMED | `src/Packetery/Module/Order/LabelPrint.php:296-297` and `:355-356` — builds the request with `labelPrintParametersService->getLabelFormat()` and then calls `soapApiClient->packetsLabelsPdf` / `packetsCarrierLabelsPdf` |
| 26 | The module holds 4 files | CONFIRMED | `find src/Packetery/Module/Labels -name '*.php' \| wc -l` → 4 |
| 26 | 291 lines of logic | CONFIRMED | non-empty non-comment lines of the 4 files → 291; matches `manifest.yaml:172` `lines: 291` |
| 26 | 19 public methods | CONFIRMED | `grep -cE '^\s*(final )?public( static)? function'` → LabelPrintParametersService 6, CarrierLabelService 2, LabelPrintPacketData 8, LabelPrintPacketDataItem 3 = 19 |
| 26–28 | A carrier label needs the number the carrier gave to the packet, and Packeta returns that number only after the packet exists [VERIFY: CarrierLabelService.php#handleApiSuccess] | UNCERTAIN | `handleApiSuccess` (`:163-190`) only stores the response; no code in this repository states when Packeta issues the number. Domain statement, no evidence found here |
| 28–29 | The module keeps the number on the order and asks Packeta only once for each packet | CONFIRMED | `CarrierLabelService.php:100-109` `getExistingCarrierNumber()` returns early when `$order->getCarrierNumber() !== null`; `:73-78` skips the API call in that case |
| 31 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 35–36 | Exposes one service for the courier numbers, one for the print parameters and two small carriers of data | CONFIRMED | 4 classes: `CarrierLabelService`, `LabelPrintParametersService`, `LabelPrintPacketData`, `LabelPrintPacketDataItem` |
| 40 | `getPacketaPacketIdsWithCourierNumbers` gives the packet identifiers with their courier numbers and reads the missing ones from Packeta [VERIFY: CarrierLabelService.php#getPacketaPacketIdsWithCourierNumbers] | CONFIRMED | `CarrierLabelService.php:66-95`; returns `array<int, array{packetId, courierNumber}>` (`:64`), keyed by order id (`:71`, `:75`, `:91`) |
| 41 | `createForm` builds the selection of the first label field to use [VERIFY: LabelPrintParametersService.php#createForm] | CONFIRMED | `LabelPrintParametersService.php:57-75`, `addSelect( 'offset', … )` over `0..$maxOffset` |
| 42 | `getOffset` takes the offset from the query, from the form, or gives none [VERIFY: LabelPrintParametersService.php#getOffset] | CONFIRMED | `:80-93` query → `:87-91` form → `:93` `return null`. One further path is not mentioned: `:81-83` returns `0` when `$maxOffset === 0` |
| 43 | `getLabelFormat`, `getLabelFormatByOrder` give the format of the Packeta label or of the carrier label [VERIFY: LabelPrintParametersService.php#getLabelFormatByOrder] | CONFIRMED | `:117-122` and `:124-130`, both read `get_packeta_label_format()` / `get_carrier_label_format()` |
| 44 | `removeExternalCarriers` removes the orders of external carriers when the print does not support them [VERIFY: LabelPrintParametersService.php#removeExternalCarriers] | CONFIRMED | `:96-110`; returns the input unchanged when `$isCarrierLabels` or `$fallbackToPacketaLabel` is true, otherwise keeps only `! $order->isExternalCarrier()` |
| 45 | `addItem`, `getItems`, `getPacketIds`, `getOrderByOrderId` hold the orders and the packet identifiers of one print [VERIFY: LabelPrintPacketData.php#getOrderByOrderId] | CONFIRMED | `LabelPrintPacketData.php:26`, `:34`, `:41`, `:86`; all four are public |
| 46 | `getOrder`, `getPacketId` — one order and its packet in the print list [VERIFY: LabelPrintPacketDataItem.php#getPacketId] | CONFIRMED | `LabelPrintPacketDataItem.php:19-25` |
| 48–49 | The module asks Packeta for a courier number only when the order does not hold one [VERIFY: CarrierLabelService.php#handleApiSuccess] | INCORRECT | The claim is true, but the anchored symbol does not show it. `handleApiSuccess` (`:163-190`) only stores the response and writes the note; the condition is in `getExistingCarrierNumber` (`:100-109`) called from `getPacketaPacketIdsWithCourierNumbers` (`:73-78`) |
| 49–50 | It then stores the number on the order and adds a note to it | CONFIRMED | `CarrierLabelService.php:171-172` `setCarrierNumber()` + `save()`; `:175-183` `add_order_note()` + `save()` |
| 50–52 | A wrong API password stops the whole run, every other fault is written to the log and the run continues with the next order [VERIFY: CarrierLabelService.php#handleApiError] | CONFIRMED | `:112-119` returns `false` on `hasWrongPassword()`, caller returns `[]` at `:85`; `:121` `logError()` then `:144` `return true`, caller does `continue` at `:88` |
| 56–57 | Depends on the API client, the order data and the label settings | CONFIRMED | `CarrierLabelService.php:7` `Core\Api\Soap\Client`, `:16` `Module\Order\Repository`; `LabelPrintParametersService.php:10` `Module\Options\OptionsProvider` |
| 59 | references → packetery-core | CONFIRMED | `CarrierLabelService.php:7-12` (`Core\Api\Soap\*`, `Core\Entity\Order`, `Core\Log`), `LabelPrintPacketData.php:8` |
| 60 | references → packetery-module-root | CONFIRMED | `CarrierLabelService.php:15` `Module\MessageManager` (`src/Packetery/Module/MessageManager.php`), `LabelPrintParametersService.php:8` `Module\FormFactory`; module path `src/Packetery/Module` per `manifest.yaml:199` |
| 61 | references → packetery-module-order | CONFIRMED | `CarrierLabelService.php:16` `Module\Order\Repository`, `LabelPrintParametersService.php:11` `Module\Order\LabelPrint` |
| 62 | references → packetery-module-options | CONFIRMED | `LabelPrintParametersService.php:10` `Module\Options\OptionsProvider` |
| 59–62 | The `references →` list (the four modules above) is the module's set of internal references | INCORRECT | `packetery-module-framework` is missing. Both services import and use it: `CarrierLabelService.php:13` `use Packetery\Module\Framework\WpAdapter;` (used at `:114`, `:137`, `:178`) and `LabelPrintParametersService.php:9` (used at `:63`, `:65`, `:70`). It is a module of its own — `manifest.yaml:162-164` `name: packetery-module-framework`, `path: src/Packetery/Module/Framework` |
| 64 | calls → packeta-soap-api (sync, SOAP) | CONFIRMED | `CarrierLabelService.php:81` `$this->soapApiClient->packetCourierNumber( $request )`, a blocking call on `Packetery\Core\Api\Soap\Client` |
| 66–67 | The module reads the courier number over the SOAP client of `packetery-core` [VERIFY: CarrierLabelService.php#handleApiSuccess] | INCORRECT | `handleApiSuccess` does not touch the SOAP client; its whole body (`:163-190`) works on `$this->orderRepository` and the already-received `$response`. The SOAP read is `:81` in `getPacketaPacketIdsWithCourierNumbers` |
| 67–68 | It saves the number through the order repository of `packetery-module-order` | CONFIRMED | `CarrierLabelService.php:164` `getWcOrderById()`, `:167` `getByWcOrderWithValidCarrier()`, `:172` `$this->orderRepository->save( $order )` |
| 68–69 | It takes the label type from the print page of the same module [VERIFY: LabelPrintParametersService.php#getLabelFormat] | CONFIRMED | `LabelPrintParametersService.php:121` uses `LabelPrint::LABEL_TYPE_PARAM` and `LabelPrint::ACTION_CARRIER_LABELS` from `Packetery\Module\Order\LabelPrint` |
| 70–71 | The label formats and the maximum offset of each format come from `packetery-module-options` [VERIFY: LabelPrintParametersService.php#createForm] | INCORRECT | The claim is true, but `createForm` (`:57-75`) never calls `OptionsProvider` — it receives `$maxOffset` as a parameter. The options calls are `:78` `$this->optionsProvider->getLabelMaxOffset( … )` and `:118-119` `get_packeta_label_format()` / `get_carrier_label_format()` |
| 71–73 | The module writes a note to the WooCommerce order and a flash message to the administrator, **both** through `packetery-module-root` [VERIFY: CarrierLabelService.php#handleApiError] | INCORRECT | Only the flash message goes through `packetery-module-root` (`:113` `$this->messageManager->flash_message(`, `MessageManager` at `src/Packetery/Module/MessageManager.php`). The order note is written on the WooCommerce order object itself — `:134` and `:175` `$wcOrder->add_order_note(` — and that object comes from `packetery-module-order` (`:123`, `:164` `$this->orderRepository->getWcOrderById( $orderId )`) |
| 74–75 | The print list holds order entities of `packetery-core`, so a caller needs no second read of the database [VERIFY: LabelPrintPacketData.php#getItems] | CONFIRMED | `LabelPrintPacketData.php:8` `use Packetery\Core\Entity\Order;`, `:15` `protected array $items` of `LabelPrintPacketDataItem`, each holding an `Order` (`LabelPrintPacketDataItem.php:11`); `:34-36` returns them without any repository call |
| 79–82 | The offset of the query string is converted to a number and is not compared with the maximum offset of the format, so a larger value passes this module [VERIFY: LabelPrintParametersService.php#getOffset] | CONFIRMED | `:84-86` `if ( $offsetParam !== null ) { return (int) $offsetParam; }` — `$maxOffset` is used only at `:81` (`=== 0`) and to build the form |
| 84–85 | One wrong API password ends the whole run and the module returns an empty list, so a print of many orders gives no partial result | CONFIRMED | `CarrierLabelService.php:112-118` returns `false`, `:84-86` `if ( $continueProcessing === false ) { return []; }` |
| 86–87 | Every other fault of the API skips one order only, and the administrator reads the result in the log of the plugin | CONFIRMED | `:121` `$this->logError( … )` writing `Log\Record::ACTION_CARRIER_NUMBER_RETRIEVING` / `STATUS_ERROR` (`:147-158`), then `:144` `return true` and `:88` `continue` |
| 87–89 | The print list keeps its orders in memory for the whole run [VERIFY: LabelPrintPacketData.php#getCount], so a print of a very large selection holds every order of that selection | CONFIRMED | `LabelPrintPacketData.php:15` `protected array $items` plus the `:20` `$ordersByOrderIdCache`; nothing releases them. `getCount()` exists at `:54-56` |
| 89–90 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rniE 'TODO\|FIXME' src/Packetery/Module/Labels/` → 0 hits |

CONFIRMED 32 · INCORRECT 5 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-labels.yaml

The separate fragment no longer exists; the entries live in `ai-docs/manifest.yaml`.
Rows below cover every line with `module: packetery-module-labels`, the `modules:` record and the
alias. `grep -n 'resolved_by: packetery-module-labels' ai-docs/manifest.yaml` → no hit.

| entry | class | evidence |
|---|---|---|
| `aliases:` line 14 — `packetery-module-labels` | CONFIRMED | the module exists as `src/Packetery/Module/Labels` with 4 PHP files |
| `modules:` 170–171 — `name: packetery-module-labels`, `path: src/Packetery/Module/Labels` | CONFIRMED | the directory exists and holds the 4 classes of the document |
| `modules:` 172 — `lines: 291` | CONFIRMED | non-empty non-comment lines of the 4 `src/` files → 291; equals the figure in the prose (line 26) |
| `modules:` 173 — `doc: reference/packetery-module-labels.md` | CONFIRMED | the file exists and its front matter carries `module: packetery-module-labels`, `covers: [src/Packetery/Module/Labels]` |
| `modules:` — no `evidence:` key for this module | CONFIRMED | the whole `modules:` block (lines 113–213) carries no `evidence:` key for any of the 25 modules, so nothing is claimed here and nothing can be wrong; `evidence:` is a key of the `exposes:` / `calls:` entries |
| `exposes:` 428–435 — `kind: library`, `name: getOffset`, `module: packetery-module-labels`, `consumers: [woocommerce]`, `auth: none`, `evidence: …/LabelPrintParametersService.php#getOffset` | CONFIRMED | `LabelPrintParametersService.php:77` `public function getOffset(): ?int`; the only consumer is `src/Packetery/Module/Order/LabelPrint.php:219`, in this repository. `notes` ("the first label field of the sheet that the print uses") is a loose paraphrase — the value is the number of fields to skip (`:65` `skip %s fields on first sheet`) |
| `exposes:` 436–444 — `kind: library`, `name: getPacketaPacketIdsWithCourierNumbers`, `module: packetery-module-labels`, `consumers: [woocommerce]`, `auth: none`, `evidence: …/CarrierLabelService.php#getPacketaPacketIdsWithCourierNumbers`, `notes` | CONFIRMED | `CarrierLabelService.php:66` `public function getPacketaPacketIdsWithCourierNumbers( LabelPrintPacketData $labelPrintPacketData ): array`; consumed by `src/Packetery/Module/Order/LabelPrint.php` |
| `calls:` 652–656 — `service: packeta-soap-api`, `kind: other`, `purpose: Reads the courier number of a packet.`, `module: packetery-module-labels`, `evidence: …/CarrierLabelService.php#handleApiSuccess` | INCORRECT | The `evidence:` anchor points at a symbol that makes no call. `handleApiSuccess` (`:163-190`) only reads `$response->getNumber()` and writes through `$this->orderRepository`. The SOAP call is `:81` `$this->soapApiClient->packetCourierNumber( $request )` in `getPacketaPacketIdsWithCourierNumbers` — the same wrong anchor as the prose at lines 66–67 |
| no `events:`, `entities:`, `owns_data:` or `unknowns:` entry names this module | CONFIRMED | `grep -n 'packetery-module-labels' ai-docs/manifest.yaml` → lines 14, 170, 173, 430, 438, 655 only; `grep -n 'Module/Labels' ai-docs/manifest.yaml` → lines 171, 434, 442, 656 only |

CONFIRMED 8 · INCORRECT 1 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
