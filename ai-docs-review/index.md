## ai-docs/index.md @ b34fe03c — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 6 | `source-commit: b34fe03c` | CONFIRMED | `ai-docs/manifest.yaml` `source_commit: b34fe03c3d932c007f21b970c38acd8241184d07`; `ai-docs/ast/map.md` carries the same commit |
| 17-18 | woocommerce is the Packeta plugin for WooCommerce; it adds the Packeta carriers to the checkout and sends the orders to Packeta as packets | CONFIRMED | `ai-docs/manifest.yaml` `description:` says the same sentence; `src/Packetery/Module/ShippingMethod.php`, `src/Packetery/Module/Order/PacketSubmitter.php` |
| 20-31 | documentation map: "The documentation of woocommerce is split into these pages" plus the ten listed pages (navigation table, one row) | INCORRECT | Every listed target exists, but the table omits 12 of the 18 files in `ai-docs/reference/`: api, customsdeclaration, dashboard, email, forms, labels, log, options, product, productcategory, shipping, views. `ls ai-docs/reference/` returns 18 files, the table names 6 |
| 31 | `reference/packetery-module-hooks.md` is "The registration map of every callback of the plugin" | INCORRECT | Callbacks are registered outside `Packetery\Module\Hooks` too: `src/Packetery/Module/Checkout/Checkout.php:89` `$this->wpAdapter->addAction( 'woocommerce_after_checkout_validation', ... )` plus 11 more in the same file, `src/Packetery/Module/Options/Page.php` (5), `src/Packetery/Module/Blocks/BlockHooks.php` (3), `src/Packetery/Module/Order/PacketAutoSubmitter.php` (3) |
| 35 | woocommerce contains 25 modules | CONFIRMED | `ai-docs/manifest.yaml` `modules:` holds 25 entries; `src/Packetery/Module` has 23 subdirectories, plus the root classes and `src/Packetery/Core` |
| 35 | Eighteen of them have a document in `reference/` | CONFIRMED | `ls ai-docs/reference/` returns 18 files; 18 manifest modules carry `doc: reference/...` |
| 36 | the other seven are described in `overview.md` | CONFIRMED | 7 manifest modules carry `doc: overview.md` (blocks, diagnosticslogger, entityfactory, exception, framework, payment, upgrade); `ai-docs/overview.md:40-46`, `:78-80`, `:92-95` describe exactly those seven |
| 38-49 | module table: 18 module ids, paths and document links (navigation table, one row) | CONFIRMED | Every name, path and `doc:` link matches `ai-docs/manifest.yaml` `modules:` (for example `packetery-module-root` → `src/Packetery/Module`, `packetery-core` → `src/Packetery/Core`); all 18 linked files exist |
| 56-58 | assumption: the module boundary is the PSR-4 namespace; each subdirectory of `src/Packetery/Module` is one module, the classes standing directly in that directory are `packetery-module-root` | CONFIRMED | `composer.json` `"psr-4": { "Packetery\\": "src/Packetery" }`; 23 subdirectories of `src/Packetery/Module` = the 23 `packetery-module-*` entries of the manifest; `packetery-module-root` has `path: src/Packetery/Module` and that directory holds 28 PHP files |
| 59-60 | assumption: the committed map in `ai-docs/ast` was produced at an older commit | INCORRECT | `ai-docs/ast/map.json` `"source-commit": "b34fe03c3d932c007f21b970c38acd8241184d07"` and `ai-docs/ast/map.md` `source-commit: b34fe03c3d932c007f21b970c38acd8241184d07` — the same commit the document is generated from, not an older one |
| 60-61 | assumption: the map was produced without the namespace split, so the counts of each module come from a second run of the same tool | CONFIRMED | `ai-docs/manifest.yaml` `assumptions:` repeat "the committed ai-docs/ast/map.json was produced without --split-psr4, so per-module counts come from a --split-psr4 run of the same tool"; `map.md` shows one project `WooCommerce` and no per-module counts |
| 61 | assumption: test directories are excluded from every count | CONFIRMED | The per-module counts exclude them: `ai-docs/overview.md:40` gives packetery-module-framework 537 lines, which equals the sum over `map.json` after `tests/` is dropped. Note that `map.json` itself still lists 69 `tests/` files, so the totals in `map.md` are not test-free |
| 62-63 | assumption: `packetery-module-order` holds more lines than the split rule permits | CONFIRMED | `map.json` lines summed over `src/Packetery/Module/Order` = 5101; `.claude/skills/generate-docs/SKILL.md:81` "Above 3 000 lines, split by namespace or directory into several `reference/` pages" |
| 63 | assumption: the team decided to document it as one module and to postpone the split | UNCERTAIN | No record of that decision in the repository: not in `ai-docs/manifest.yaml` `assumptions:`, not in `ai-docs/reference/packetery-module-order.md`, not in `docs/` |
| 64-65 | assumption: the names of the Packeta services follow the manifest of the PrestaShop module, so a cross-repository query finds both plugins under one name | UNCERTAIN | `ai-docs/dependencies.md:107` corroborates one name only ("packeta-widget ... prestashop uses the same name"); the PrestaShop manifest is not in this repository, so `packeta-soap-api` and `packeta-pickup-point-api` cannot be checked here |
| 69 | documentation generated from commit `b34fe03c` | CONFIRMED | Front matter `source-commit: b34fe03c`; `manifest.yaml` `source_commit: b34fe03c3d93...` |
| 73 | reference/packetery-core.md — reviewed, 53/53 CONFIRMED at the second verification | INCORRECT | `ai-docs/reference/packetery-core.md` is `confidence: reviewed`, but the record says 53 of 55 checkable rows: `ai-docs-review/reference-packetery-core.md:75` "Both tables: CONFIRMED 53 · INCORRECT 0 · UNCERTAIN 2 · NOT FOUND 0 · PENDING 1". The 2 UNCERTAIN rows belong in the denominator (`templates/verify.md`, promotion rule), so 53/55 |
| 74 | reference/packetery-module-order.md — reviewed, 82/94 CONFIRMED, 9 INCORRECT fixed | CONFIRMED | Front matter `confidence: reviewed`; `ai-docs-review/reference-packetery-module-order.md:114` "CONFIRMED 82 · INCORRECT 9 · UNCERTAIN 3 · PENDING 1" → 94 checkable rows |
| 75 | reference/packetery-module-checkout.md — reviewed, 81/89 CONFIRMED, 6 INCORRECT fixed | INCORRECT | The record has 88 checkable rows, not 89: `ai-docs-review/reference-packetery-module-checkout.md:81` "CONFIRMED 68 · INCORRECT 4 · UNCERTAIN 0 · PENDING 1" and `:104` "CONFIRMED 13 · INCORRECT 2 · UNCERTAIN 1" → 81 + 6 + 1. The PENDING row does not enter the count (`templates/verify.md`) |
| 76 | reference/packetery-module-carrier.md — reviewed, 77/88 CONFIRMED, 9 INCORRECT fixed | INCORRECT | The record has 87 checkable rows: `ai-docs-review/reference-packetery-module-carrier.md:77` "CONFIRMED 64 · INCORRECT 6 · PENDING 1" and `:101` "CONFIRMED 13 · INCORRECT 3 · UNCERTAIN 1" → 77 + 9 + 1 |
| 77 | reference/packetery-module-options.md — reviewed, 71/77 CONFIRMED, 5 INCORRECT fixed | CONFIRMED | Front matter `confidence: reviewed`; `ai-docs-review/reference-packetery-module-options.md:69` and `:91` sum to CONFIRMED 71, INCORRECT 5, UNCERTAIN 1 → 77 |
| 78 | reference/packetery-module-shipping.md — reviewed, 58/62 CONFIRMED, 3 INCORRECT fixed | CONFIRMED | `ai-docs-review/reference-packetery-module-shipping.md:82` "Both tables: CONFIRMED 58 · INCORRECT 3 · UNCERTAIN 1 · PENDING 1" → 62 |
| 79 | reference/packetery-module-api.md — reviewed, 65/68 CONFIRMED, 2 INCORRECT fixed | INCORRECT | The record has 70 checkable rows: `ai-docs-review/reference-packetery-module-api.md:60` "CONFIRMED 50 · INCORRECT 2 · UNCERTAIN 1 · PENDING 1" and `:84` "CONFIRMED 15 · INCORRECT 0 · UNCERTAIN 2" → 65 + 2 + 3 |
| 80 | The nine remaining reference pages — draft, not yet verified | INCORRECT | Eleven remain, not nine: 18 reference pages minus the 7 named above (customsdeclaration, dashboard, email, forms, hooks, labels, log, product, productcategory, root, views). All eleven carry `confidence: draft` and none has a record in `ai-docs-review/` |
| 81 | overview.md, architecture.md, dependencies.md, index.md — draft, not yet verified | CONFIRMED | All four carry `confidence: draft` in their front matter; `ai-docs-review/` holds records for reference pages only |
| 83 | The lowest confidence is `draft` | CONFIRMED | `grep "^confidence:" ai-docs/*.md ai-docs/reference/*.md` returns only `draft` and `reviewed` |
| 83-84 | A verification run of those documents, and a fix of every finding, raises them to `reviewed` | CONFIRMED | `.claude/skills/generate-docs/templates/verify.md`: "After fixing an INCORRECT finding the ceiling is `reviewed`" |
| 84-85 | A document reaches `verified` only when every checked row is confirmed at the first verification | CONFIRMED | `templates/verify.md`: all rows CONFIRMED and 0 SENSITIVE/INSTRUCTION → `verified`; after a fix the ceiling is `reviewed` and `verified` needs another pass |
| 89-90 | the blind spots are carried machine-readably by `manifest.yaml`, section `unknowns` | CONFIRMED | `ai-docs/manifest.yaml` `unknowns:` holds 33 entries with `question`, `checked`, `resolved_by`, `blocks` |
| 90-91 | "They are not tasks for the reader — they are findings to report when a query runs into them." | INSTRUCTION | The sentence directs the reader or agent instead of describing the code. It is the standard wording of `.claude/skills/generate-docs/templates/index.md`, copied verbatim |
| 95 | unknown: whether the open permission callback of the four checkout REST routes is a decision or a defect; searched the permission callbacks of both controllers, which differ; resolved by a person owning the plugin security | CONFIRMED | `src/Packetery/Module/Api/Internal/CheckoutController.php` registers four routes, each with `'permission_callback' => '__return_true'` (:55, :65, :75, :85), against `current_user_can( 'edit_posts' )` in `OrderController.php`; the manifest entry matches word for word |
| 96 | unknown: whether a capability check protects the settings export; searched the export class and the page that builds its link; resolved by the namespace that registers the hook | CONFIRMED | Manifest question and `checked` match; the hook is registered in `src/Packetery/Module/Hooks/HookRegistrar.php:587` `$this->wpAdapter->addAction( 'admin_init', [ $this->exporter, 'outputExportTxt' ] )`, with the export class in `src/Packetery/Module/Options/Exporter.php`. Note the manifest names `packetery-module-root` as `resolved_by`, which the code does not support |
| 97 | unknown: which Packeta API version and WSDL the plugin runs against; searched the constructor arguments of the SOAP client | CONFIRMED | `src/Packetery/Core/Api/Soap/Client.php:42` `__construct( ?string $apiPassword, string $wsdlUrl )` — the WSDL arrives from outside and no version is named; manifest entry matches |
| 98 | unknown: whether the carrier feed host and the pickup point validation host are one service; searched the endpoint constant and the REST client, which name two hosts | CONFIRMED | `src/Packetery/Module/Carrier/Downloader.php:24` holds the feed endpoint constant and `src/Packetery/Core/Api/Rest/PickupPointValidate.php` the validation client; the two hosts differ. Manifest entry matches |
| 99 | unknown: which consumers outside this repository use the domain module; searched this repository only; resolved by other documented repositories | CONFIRMED | Manifest entry "Which consumers outside this repository use Packetery\Core", `checked: this repository only`, `resolved_by: other documented repositories` |
| 100 | unknown: which business rules decide the order status of each packet status, the disallowed gateways and the supported currency switchers — resolved by a person owning the order process | INCORRECT | The manifest gives two different resolvers for the three grouped questions: "Which payment gateways the shops must disallow for a carrier by default" and "Which currency switcher plugins the shops must support" both carry `resolved_by: a person owning the checkout`; only "Which order status the shop wants for each packet status" carries `resolved_by: a person owning the order process` |
| 101 | unknown: which data a shop must keep after it removes the plugin, and whether the job schedules fit a large shop; searched the uninstall constant and the five schedules; resolved by a person owning the plugin | CONFIRMED | Manifest: "Which data a shop must keep after it removes the plugin" (`checked: the constant that permits the uninstall and the list of the deleted options`, `resolved_by: a person owning the plugin`) and "Whether the schedules of the background jobs fit the load of a large shop" (`checked: the five schedules of the cron service`, `resolved_by: a person owning the plugin operation`) |
| 102 | unknown: how an installation with a read only plugin directory gets the class of a new carrier; searched the generator and the bulk generator; resolved by a person owning the plugin release | CONFIRMED | Manifest entry matches: `checked: the generator and the bulk generator of this module`, `resolved_by: a person owning the plugin release` |
| 103 | unknown: whether a category restriction must win over a product restriction, and which products need age verification; searched the two metadata keys, which the checkout reads separately; resolved by a person owning the catalogue | CONFIRMED | Both manifest entries carry `resolved_by: a person owning the catalogue`; the first carries exactly this `checked` value |
| 104 | unknown: whether the shortcode of the pickup point country must give the country of the point or of the customer; searched the method behind that shortcode, which reads the shipping country; resolved by a person owning the email templates | CONFIRMED | Manifest entry matches word for word |
| 106 | The manifest carries every question of every module | CONFIRMED | `unknowns:` holds 33 entries; the seven verification records confirm each verified module's questions landed there (for example carrier `unknowns[0..2]`, api `unknowns[0..1]`, checkout `unknowns[0..2]`) |
| 106-107 | The table above groups the questions that share one answer, and it keeps the question of every group | INCORRECT | The ten rows cover 14 of the 33 manifest questions. Nineteen questions appear in no row and share no answer with a listed row — for example "How long a shop must keep the log records" (`resolved_by: a person owning the plugin operation`), "Which setup steps a shop must finish before the plugin is usable" (`a person owning the onboarding`), "Where WordPress schedules the purge of the expired checkout transients" (`the module that registers the cron service`), "Whether the order grid of the classic shape must stay supported" (`a person owning the plugin release`) |

CONFIRMED 29 · INCORRECT 10 · UNCERTAIN 2 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 1
