## ai-docs/reference/packetery-module-dashboard.md @ b34fe03c — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 6 | `source-commit: b34fe03c` | CONFIRMED | `git cat-file -t b34fe03c` = commit; `git merge-base --is-ancestor b34fe03c HEAD` passes on `docs/teams` |
| 8 | `covers: [src/Packetery/Module/Dashboard]` | CONFIRMED | Directory exists, holds 4 `.php` files, no subdirectory |
| 13 | Repo, module, type and status header | CONFIRMED | Matches the front matter and the `ai-docs/reference/` layout |
| 17-18 | Gives the home page of the plugin, and every other Packeta page is a child of it [VERIFY: DashboardPage.php#register] | CONFIRMED | `DashboardPage::register():64` calls `addSubmenuPage( self::SLUG, … )`; every other Packeta admin page passes `DashboardPage::SLUG` as the parent: `Options/Page.php:223`, `Carrier/OptionsPage.php:102`, `Log/Page.php:80`, `Order/LabelPrint.php:275`, `Order/CollectionPrint.php:210`. Note: the top-level menu entry with that slug is created outside this module, in `Options/Page.php:211` `registerMenuPage()` |
| 18-20 | The page shows the setup steps and marks each finished step [VERIFY: DashboardItemBuilder.php#buildItems] | CONFIRMED | `buildItems():67-127` returns 8 `DashboardItem` objects, each with a boolean `$isFinished` argument; `DashboardPage::render():85` passes them to `template/dashboard/home.latte` |
| 20-21 | 4 files, 291 lines of logic, 13 public methods | CONFIRMED | `find src/Packetery/Module/Dashboard -name '*.php' \| wc -l` = 4; non-blank non-comment lines of those files = 291; `grep -cE '^\s*(final )?public( static)? function'` = 6+2+2+3 = 13. `ai-docs/ast/map.json` gives the same per-file line split (29+38+159+65 = 291); `tests/Module/Dashboard/DashboardItemBuilderTest.php` is outside the count |
| 23-26 | Reads the state from the settings, the carriers, the products and the shipping zones of WooCommerce [VERIFY: DashboardHelper.php#isPacketaShippingMethodActive] | CONFIRMED | `buildItems()` reads `optionsProvider` (`:74`, `:82`, `:113`, `:123`), `carrierUpdater->getLastUpdate()` (`:95`) and `carrierEntityRepository->getAllActiveCarriersList()` (`:101`), `hasProductsWithPacketaSettings()` (`:88`) and `dashboardHelper->isPacketaShippingMethodActive()` (`:107`). The anchor itself evidences the shipping-zone part only (`DashboardHelper.php:30-47`) |
| 28 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder |
| 32 | The module exposes the page and the builder of its steps | INCORRECT | Incomplete against the table it introduces and against the code: the table also lists `DashboardItem` and `DashboardHelper`, and `DashboardHelper` is the one member consumed outside the module — `DashboardWidget.php:85` injects it and `:158` calls `isPacketaShippingMethodActive()` for `hasPacketaShipping` |
| 36 | `packeta-home` — admin home page and parent of every Packeta page [VERIFY: DashboardPage.php#SLUG] | CONFIRMED | `DashboardPage.php:15` `public const SLUG = 'packeta-home';`, used as the parent slug by the five pages listed above |
| 37 | `register` adds the page and needs the `manage_woocommerce` capability [VERIFY: DashboardPage.php#register] | CONFIRMED | `DashboardPage.php:68` passes `'manage_woocommerce'` to `addSubmenuPage()` |
| 38 | `render` runs the carrier update and renders the home template [VERIFY: DashboardPage.php#render] | CONFIRMED | `render():79-80` calls `carrierUpdater->startUpdate( $redirectUrl )` and `runUpdate()`, then `:82` renders `template/dashboard/home.latte` |
| 39 | `buildItems` builds the eight steps and decides which are finished [VERIFY: DashboardItemBuilder.php#buildItems] | CONFIRMED | 8 `new DashboardItem(…)` calls with sort orders 1 to 8, each with a finished expression |
| 40 | `DashboardItem` holds caption, link, description, order and the finished state [VERIFY: DashboardItem.php#getSortOrder] | INCORRECT | The anchor does not evidence the claim: `getSortOrder():60-62` returns the sort order only. The five properties are the constructor arguments at `DashboardItem.php:34-46`; the anchor should be `#__construct` (or the class) |
| 41 | `isPacketaShippingMethodActive` answers whether a zone holds an active Packeta method [VERIFY: DashboardHelper.php#isPacketaShippingMethodActive] | CONFIRMED | `DashboardHelper.php:34-44` loops the zones of the `shipping-zone` data store and `get_shipping_methods( true )` (enabled only), returning true on `ShippingProvider::isPacketaMethod( $method->id )` |
| 43-45 | The eight steps are account settings, carrier configuration mode, product settings, carrier update, carrier settings, shipping zone, packet status synchronisation, automatic submission [VERIFY: DashboardItemBuilder.php#buildItems] | CONFIRMED | Same order in `buildItems()`: "Basic settings of the Packeta plugin" (1), "Carrier type setting" (2), "Product settings" (3), "Carriers update" (4), "Carrier settings" (5), "Shipping zone settings" (6), "Set up shipment status tracking" (7), "Set up automatic shipment submission" (8) |
| 46-47 | The link of the carrier update stays empty while the account has no API password [VERIFY: DashboardItemBuilder.php#getCarrierUpdateUrl] | CONFIRMED | `getCarrierUpdateUrl():150-152`: `if ( $this->optionsProvider->get_api_password() === null ) { return null; }`; `DashboardItem::$url` is `?string` |
| 51, 53-58 | The module depends on every module whose state a step reports; the `references →` list | INCORRECT | The list is incomplete. `packetery-module-framework` (`src/Packetery/Module/Framework`, a module of its own in `manifest.yaml:113+`) is used by all three service classes: `WpAdapter` in `DashboardPage.php:9` and `DashboardItemBuilder.php:9`, `WcAdapter` in `DashboardHelper.php:7`. `packetery-module-shipping` is used in `DashboardHelper.php:8` (`ShippingProvider::isPacketaMethod`). Neither is listed |
| 53 | references → packetery-module-root | CONFIRMED | `DashboardPage.php:10` uses `Packetery\Module\ModuleHelper` (`src/Packetery/Module/ModuleHelper.php`), the path of `packetery-module-root` |
| 54 | references → packetery-module-options | CONFIRMED | `DashboardItemBuilder.php:10-11` uses `Packetery\Module\Options` and `Options\OptionsProvider` |
| 55 | references → packetery-module-carrier | CONFIRMED | `DashboardItemBuilder.php:7-8` uses `Carrier\EntityRepository`, `Carrier\OptionsPage` and `CarrierUpdater`; `DashboardPage.php:8` uses `CarrierUpdater` |
| 56 | references → packetery-module-order | CONFIRMED | `DashboardItemBuilder.php:12` uses `Packetery\Module\Order\PacketSynchronizer` |
| 57 | references → packetery-module-product | CONFIRMED | `DashboardItemBuilder.php:13` uses `Packetery\Module\Product`; `hasProductsWithPacketaSettings()` reads `Product\Entity::META_AGE_VERIFICATION_18_PLUS` and `Product\Entity::META_DISALLOWED_SHIPPING_RATES` |
| 58 | references → packetery-module-views | CONFIRMED | `DashboardPage.php:11` uses `Packetery\Module\Views\UrlBuilder`; `render():88-89` calls `buildAssetUrl()` for the two logos |
| 60-62 | The steps read the settings of options, the carrier list and the last update of carrier, and the status mapping of order [VERIFY: DashboardItemBuilder.php#buildItems] | CONFIRMED | `optionsProvider->get_api_password()`/`get_sender()` (`:74`), `carrierEntityRepository->getAllActiveCarriersList()` (`:101`), `carrierUpdater->getLastUpdate()` (`:95`), `packetSynchronizer->getDefaultPacketStatuses()` with `optionsProvider->getStatusSyncingPacketStatuses()` and `getExistingStatusSyncingOrderStatuses()` (`:113-114`) |
| 62-64 | One step asks whether any product holds a Packeta setting and reads the product metadata [VERIFY: DashboardItemBuilder.php#hasProductsWithPacketaSettings] | CONFIRMED | `hasProductsWithPacketaSettings():176-208` runs a `WP_Query` over `post_type: product` with a meta query on the two `Product\Entity::META_*` keys |
| 65-66 | The links of the steps come from the URL builder of `packetery-module-views` [VERIFY: DashboardItem.php#getUrl] | INCORRECT | The step links come from the framework adapter, not from `Views\UrlBuilder`: `DashboardItemBuilder.php:71,80,92,99,105,110,120` build them with `$this->wpAdapter->adminUrl( … )`, and `getNewestProductUrl():139` with `$this->wpAdapter->getEditPostLink( … )`. `UrlBuilder` is used only in `DashboardPage::render():88-89`, and only for the two logo asset URLs. The anchor `DashboardItem::getUrl():52-54` merely returns the stored string |
| 66-68 | The shipping zone step reads the zones of WooCommerce through the adapters of `packetery-module-root` [VERIFY: DashboardHelper.php#isPacketaShippingMethodActive] | INCORRECT | The adapter belongs to `packetery-module-framework`, not to `packetery-module-root`: `DashboardHelper.php:7` `use Packetery\Module\Framework\WcAdapter;` and `:32` `$this->wcAdapter->dataStoreLoad( 'shipping-zone' )`. `manifest.yaml` maps `packetery-module-framework` to `src/Packetery/Module/Framework` and `packetery-module-root` to `src/Packetery/Module` |
| 67-69 | It looks for a method of the Packeta shipping module in them [VERIFY: DashboardHelper.php#isPacketaShippingMethodActive] | CONFIRMED | `DashboardHelper.php:40` `ShippingProvider::isPacketaMethod( $method->id )`, from `Packetery\Module\Shipping` (`:8`) |
| 69-70 | No other module reads this one | INCORRECT | Seven files outside the module read it: `Plugin.php:65-68`, `Hooks/HookRegistrar.php:669`, `Options/Page.php:214,223`, `Carrier/OptionsPage.php:102`, `Log/Page.php:80`, `Order/LabelPrint.php:275`, `Order/CollectionPrint.php:210` and `Views/AssetManager.php:266` use `DashboardPage::SLUG`; `DashboardWidget.php:11,85` imports and injects `DashboardHelper` |
| 70 | The dashboard is the top of the page tree | CONFIRMED | Every other Packeta admin page registers under `DashboardPage::SLUG` (see line 17-18). The top-level menu entry that carries the slug is registered in `Options/Page.php:211-218` |
| 70 | Nothing depends on its data | INCORRECT | `DashboardWidget.php:158` builds the `hasPacketaShipping` flag of the WordPress dashboard widget from `dashboardHelper->isPacketaShippingMethodActive()` |
| 74-78 | The product settings check asks for a product whose metadata is not an empty array and compares against a serialised empty value written as a literal [VERIFY: DashboardItemBuilder.php#hasProductsWithPacketaSettings] | CONFIRMED | `DashboardItemBuilder.php:192-200`: `'key' => Product\Entity::META_DISALLOWED_SHIPPING_RATES, 'compare' => 'EXISTS'` combined with `'value' => 'a:0:{}', 'compare' => '!='` |
| 78 | A change of that serialisation breaks the check without an error | CONFIRMED | The method returns the result of `$query->have_posts()` only (`:204-208`); a non-matching literal yields an empty result set and a silent `false`, with no exception and no log call in the file |
| 80-81 | The page runs the carrier update while it renders, so an open dashboard can start a download [VERIFY: DashboardPage.php#render] | CONFIRMED | `render():74-80` calls `carrierUpdater->startUpdate( $redirectUrl )` and `runUpdate()` before the template render |
| 81-82 | The capability of the page is a literal of the registration [VERIFY: DashboardPage.php#register] | CONFIRMED | `DashboardPage.php:68` passes the string inline; the file holds no constant and no filter for it |
| 83 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rnE 'TODO\|FIXME' src/Packetery/Module/Dashboard/` returns 0 lines |

CONFIRMED 29 · INCORRECT 7 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-dashboard.yaml

Separate fragments no longer exist; the rows below verify the `packetery-module-dashboard` records of `ai-docs/manifest.yaml`.

| entry | class | evidence |
|---|---|---|
| `aliases` — `packetery-module-dashboard` (`manifest.yaml:10`) | CONFIRMED | Same id as the `module:` front matter, the document file name and `modules[].name` |
| assumption — PSR-4 boundary `Packetery\Module\Dashboard`, counts from a `--split-psr4` run, test directories excluded (`manifest.yaml:55-57`) | CONFIRMED | `find src/Packetery/Module/Dashboard -type d` returns the directory itself only; the tests live in `tests/Module/Dashboard/`, outside the namespace path; the per-file lines in `ai-docs/ast/map.json` (29+38+159+65) add up to the manifest value |
| `modules[]` — name, `path: src/Packetery/Module/Dashboard`, `lines: 291`, `doc: reference/packetery-module-dashboard.md` (`manifest.yaml:138-141`) | CONFIRMED | Directory exists with 4 `.php` files; non-blank non-comment lines = 291; the document file exists. The `modules:` section carries `path:` and no `evidence:` key, here and for every other module |
| `exposes[]` — library `buildItems`, module dashboard, consumers `woocommerce`, `auth: none`, evidence `DashboardItemBuilder.php#buildItems`, notes "Builds the eight setup steps and their finished state" (`manifest.yaml:319-326`) | CONFIRMED | `buildItems():67-127` is public, has no capability check and returns exactly 8 `DashboardItem` objects with a finished flag each |
| `exposes[]` — html `packeta-home`, module dashboard, `auth: wordpress-capability`, evidence `DashboardPage.php#SLUG`, notes "Home page of the plugin and the parent of every other Packeta page. It needs the manage_woocommerce capability" (`manifest.yaml:471-479`) | CONFIRMED | `DashboardPage.php:15` defines the slug, `:68` the `'manage_woocommerce'` capability, and the five other admin pages register under the slug. Note: the top-level menu entry with the same slug is added in `Options/Page.php:211-218` with `'manage_options'`, so two capabilities guard the same slug |
| `unknowns[]` — "Which setup steps a shop must finish before the plugin is usable", checked "the eight steps that the builder creates", `resolved_by: a person owning the onboarding` (`manifest.yaml:877-880`) | CONFIRMED | Honest open question: the code fixes the eight steps and their finished expressions (`DashboardItemBuilder::buildItems()`), but nothing in the repository states which of them a shop must finish |

CONFIRMED 6 · INCORRECT 0 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
