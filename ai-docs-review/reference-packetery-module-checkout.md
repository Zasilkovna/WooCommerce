## ai-docs/reference/packetery-module-checkout.md @ 3d44ef98 — 2026-09-22

Verified against the working tree of branch `docs/teams`. `git diff 3d44ef98 HEAD -- src/Packetery/Module/Checkout` is empty, so the working tree equals the commit in the front matter. `Checkout/` below stands for `src/Packetery/Module/Checkout/`.

| line | claim | class | evidence |
|---|---|---|---|
| 17 | The module puts the Packeta delivery options into the WooCommerce cart and checkout. | CONFIRMED | `Checkout/ShippingRateFactory.php#createShippingRates` builds the rates, `Checkout/CheckoutRenderer.php` renders the checkout fields, `Checkout/Checkout.php#actionCalculateFees` adds the cart fees. |
| 19 | The module builds the shipping rates of the Packeta carriers. | CONFIRMED | `Checkout/ShippingRateFactory.php:105-164` `createShippingRates()` loops `$availableCarriers` and fills `$customRates`. |
| 21 | It renders the widget button and the hidden fields of the checkout form. | CONFIRMED | `Checkout/CheckoutRenderer.php:65-78` `actionRenderHiddenInputFields()` renders `template/checkout/input_fields.latte`; `:85-134` render the widget button. |
| 23 | It validates what the customer selected. | CONFIRMED | `Checkout/CheckoutValidator.php:113-118` `actionValidateCheckoutData()` adds `packeta_cart_validation_failed` to the `WP_Error`. |
| 25 | After WooCommerce creates the order, the module writes the pickup point, the address and the car delivery data to the order. | CONFIRMED | `Checkout/OrderUpdater.php:126-216` `actionUpdateOrder()` calls `getPropsFromCheckoutData()`, `updateHomeDelivery()`, `updateCarDelivery()` and `orderRepository->save()`. |
| 28 | The module supports the classic checkout and the block checkout and detects which one the shop uses. | CONFIRMED | `Checkout/CheckoutService.php:244-264` `areBlocksUsedInCheckout()` reads `getCheckoutDetection()` and falls back to `hasBlockInPage(..., 'woocommerce/checkout')`. |
| 29 | The module holds 12 files. | CONFIRMED | `ls Checkout/` returns 12 `.php` files. |
| 29 | The module holds 2162 lines of logic. | CONFIRMED | Non-blank, non-comment lines per file sum to 2162 (243+435+99+141+157+149+206+31+201+288+38+174). |
| 29 | The module holds 69 public methods. | CONFIRMED | 68 `public function` plus 1 `public static function` (`RateCalculator::getProductShippingClassSlug`, `Checkout/RateCalculator.php:260`) = 69. |
| 31 | One class registers most hooks of the namespace, and the other classes give the callbacks. | CONFIRMED | `Checkout/Checkout.php:79-159` `registerHooks()` registers 10 callbacks of the namespace; no other file of the namespace calls `addAction`/`addFilter`. |
| 33 | The cart fee callback and the guest session callback are registered outside this module. | CONFIRMED | `src/Packetery/Module/Hooks/HookRegistrar.php:595-616` `registerFrontEnd()` registers `woocommerce_cart_calculate_fees` → `Checkout::actionCalculateFees` and `woocommerce_guest_session_to_user_id` → `CheckoutStorage::migrateGuestSessionToUserSession`. |
| 34 | The widget settings AJAX callback is registered outside this module. | CONFIRMED | `src/Packetery/Module/Hooks/HookRegistrar.php:436-444` registers `wp_ajax_get_settings` and `wp_ajax_nopriv_get_settings` → `CheckoutSettings::actionCreateSettingsAjax`. |
| 36 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder. |
| 40-41 | The module exposes its members as WooCommerce hook callbacks and as services, and registers no REST route of its own. | CONFIRMED | `grep -r 'register_rest_route\|registerRestRoute' Checkout/` returns 0 hits; the namespace only consumes `Api\Internal\CheckoutRouter` for URLs. |
| 45 | `registerHooks` registers the checkout hooks of the namespace, except the three that the plugin registers. | CONFIRMED | `Checkout/Checkout.php:79-159` against `HookRegistrar.php:436,598-616` (3 distinct callbacks outside). |
| 46 | `actionCalculateFees` adds the age verification fee and the cash on delivery surcharge. | CONFIRMED | `Checkout/Checkout.php:222-223` `addAgeVerificationFee()` and `addCodSurchargeFee()`. |
| 47 | `woocommerce_available_payment_gateways` removes the gateways that the selected carrier does not allow. | CONFIRMED | `Checkout/Checkout.php:108` registration; `:410-416` `unset( $availableGateways[ $key ] )` for unsupported COD and `hasCheckoutPaymentMethodDisallowed`. |
| 48 | `createShippingRates` builds one rate for each available Packeta carrier. | CONFIRMED | `Checkout/ShippingRateFactory.php:135-161`. |
| 49 | `woocommerce_after_checkout_validation` rejects an incomplete or invalid Packeta selection. | CONFIRMED | `Checkout/Checkout.php:89` registration; `Checkout/CheckoutValidator.php:113-118`. |
| 50 | `woocommerce_store_api_checkout_update_order_from_request` does the same check for the block checkout and throws a REST exception. | CONFIRMED | `Checkout/Checkout.php:91-96` registration; `Checkout/CheckoutValidator.php:142` `throw new WC_REST_Exception( 'packeta_cart_validation_failed', $error )`. |
| 51 | `woocommerce_checkout_update_order_meta` and `woocommerce_store_api_checkout_order_processed` write the Packeta data to the new order. | CONFIRMED | `Checkout/Checkout.php:98-105`; the first hook calls `actionUpdateOrderById()`, which delegates to `actionUpdateOrder()` (`Checkout/OrderUpdater.php:118`). |
| 52 | `woocommerce_review_order_before_submit` renders the hidden fields of the pickup point, the address and the car delivery. | CONFIRMED | `Checkout/Checkout.php:81-87`; `Checkout/CheckoutRenderer.php:69-75` merges `$pickupPointAttributes`, `$homeDeliveryAttributes` and `$carDeliveryAttributes`. |
| 53 | `woocommerce_review_order_after_shipping` and `woocommerce_after_shipping_rate` render the widget button in the configured place. | CONFIRMED | `Checkout/Checkout.php:124-150`: the `init` closure picks `actionRenderWidgetButtonTableRow` when `getCheckoutWidgetButtonLocation() === 'after_transport_methods'`, otherwise `actionRenderWidgetButtonAfterShippingRate`. |
| 54 | `createSettings` builds the settings and the translations for the widget in the browser. | CONFIRMED | `Checkout/CheckoutSettings.php:169-210`, including the `translations` key. |
| 56-57 | `packeta_shipping_price` is an extension filter of the module. | CONFIRMED | `Checkout/RateCalculator.php:102` and `:188` `applyFilters( 'packeta_shipping_price', ... )`. |
| 57-58 | `packetery_price` is an extension filter of the module. | CONFIRMED | `Checkout/CurrencySwitcherService.php:64` `applyFilters( 'packetery_price', $price )`. |
| 59-60 | `packeta_widget_weight` and `packeta_widget_language` are extension filters of the module. | CONFIRMED | `Checkout/CheckoutSettings.php:160` and `:167`. |
| 56 | "The module gives four extension filters to other code" (completeness). | CONFIRMED | Exactly four Packeta-owned filter names are applied in the namespace. The namespace also re-applies three filters owned by WooCommerce or by a third party (`wc_session_expiration` `CheckoutStorage.php:218`, `woocommerce_calc_shipping_tax` `ShippingRateFactory.php:260`, `woocs_exchange_value` `CurrencySwitcherService.php:81`); those are not filters the module defines. |
| 60-62 | The module invalidates the WooCommerce rate cache **when the customer changes the payment method**, anchor `SessionService#actionUpdateShippingRates`. | INCORRECT | The anchored method has no payment-method condition and runs on every order review: `Checkout/Checkout.php:110-116` registers it on `woocommerce_review_order_before_shipping`, and `Checkout/SessionService.php:47-52` is `foreach ( $packages as $index => $package ) { $this->wcAdapter->sessionSet( 'shipping_for_package_' . $index, false ); }`. The payment method reaches the cache through a different member, `filterUpdateShippingPackages()` (`SessionService.php:64-71`), which the document itself anchors correctly on line 142. |
| 66-68 | The price comes from the carrier options. | CONFIRMED | `Checkout/RateCalculator.php:86-188` `getShippingRateCost()` reads `$carrierOptions->toArray()`. |
| 68-70 | The module reads the weight limits or the order value limits and takes the first limit that the cart does not exceed. | CONFIRMED | `Checkout/RateCalculator.php:105-125`: both loops `break` on the first `<=` match. |
| 70-72 | With per class options the module groups the cart items by shipping class, computes a price per class, then adds the prices or takes the most expensive one. | CONFIRMED | `Checkout/RateCalculator.php:308-372`; `$calculationType === 'per_order_most_expensive'` → `max()`, otherwise `array_sum()`. |
| 73-74 | A free shipping limit or a free shipping coupon sets the price to zero, anchor `ShippingRateFactory#formatCarrierNameWithFreeShipping`. | INCORRECT | The anchored method sets no price; it only builds the label: `Checkout/ShippingRateFactory.php:271-273` `return sprintf( '%s: %s', $carrierName, $this->wpAdapter->__( 'Free', 'packeta' ) );`. The prose statement itself holds, but its evidence is `Checkout/RateCalculator.php:151-153` (`if ( $cartPrice >= $freeShippingLimit ) { $cost = 0; }`) and `:163-165` (coupon). |
| 75 | When no limit applies, the price stays `null` and the module does not offer that carrier. | CONFIRMED | `Checkout/RateCalculator.php:138-140` returns `null`; `Checkout/ShippingRateFactory.php:190` `return $cost !== null ? ... : null;` and `:158-160` skips a `null` rate. |
| 76-77 | The cash on delivery surcharge follows the same first match rule over the surcharge limits. | CONFIRMED | `Checkout/RateCalculator.php:199-212` returns on the first `$cartPrice <= $surchargeLimit['order_price']`. |
| 79-81 | The module hides a carrier over age verification, maximum cart value, product size, product category and disallowed rate identifiers. | CONFIRMED | `Checkout/ShippingRateFactory.php:196-226`: `isAgeVerificationRequired`, `getEffectiveMaxCartValueLimit`, `cartContainsProductOversizedForCarrier`, `isShippingRateRestrictedByProductsCategory`, `getDisallowedShippingRateIds`. |
| 81-83 | The size check compares the three largest dimensions of a product with the size restrictions of the carrier. | CONFIRMED | `Checkout/CartService.php:301-320` sorts both the carrier dimensions and `$biggestProductSizeByLength` with `rsort( ..., SORT_NUMERIC )` and compares them index by index; `:288-299` add `maximum_length` and `dimensions_sum`. |
| 84-86 | For a pickup point order the validator checks the required attributes, the country of the carrier and, when the shop enables the check, the pickup point itself. | CONFIRMED | `Checkout/CheckoutValidator.php:212-223` required attributes, `:230-237` `isValidForCountry()`, `:239-256` guarded by `isPickupPointValidationEnabled()`. |
| 86-88 | For a home delivery order the validator checks the validated address when the carrier option requires it. | CONFIRMED | `Checkout/CheckoutValidator.php:294-311`: `$addressValidation === 'required'` and `ADDRESS_IS_VALIDATED !== '1'`. |
| 92-93 | The module depends on the carrier configuration, on the order module and on the WooCommerce cart. | CONFIRMED | `use Packetery\Module\Carrier`, `use Packetery\Module\Order`, `use Packetery\Module\Framework\WcAdapter` across the namespace. |
| 95-107 | The 13 `references →` lines name modules the namespace imports. | CONFIRMED | Each has a matching `use` statement: `Packetery\Core\*`, `Module\Carrier`, `Module\Order`, `Module\Options`, `Module\Product`, `Module\Framework`, `Module\Api`, `Module\Shipping`, `Module\DiagnosticsLogger`, `Module\Exception`, `Module\Views`, `Module\Log`, `Module\ProductCategory`. |
| 95-107 | The `references →` block is the dependency set of the module (completeness). | INCORRECT | `Packetery\Module\Payment\PaymentHelper` is imported and used but has no `references →` line: `Checkout/Checkout.php:17` `use Packetery\Module\Payment\PaymentHelper;` (called at `:271`, `:397`) and `Checkout/CheckoutValidator.php:18` (called at `:288`). Root-namespace imports (`ModuleHelper`, `Plugin`, `Transients`, `WidgetOptionsBuilder`, `WidgetUrlResolver`, `ShippingMethod`, `EntityFactory\SizeFactory`) are also absent from the block. |
| 108 | `calls → packeta-widget (sync, HTTPS)` | CONFIRMED | `Checkout/CheckoutSettings.php:194` `'widgetUrl' => $this->widgetUrlResolver->getUrl()`; `src/Packetery/Module/WidgetUrlResolver.php:7-8` both constants use the `https://` scheme. |
| 110-111 | The module reads the carrier entity, the carrier options and the pickup point configuration from `packetery-module-carrier`. | CONFIRMED | `Checkout/ShippingRateFactory.php:210` `carrierOptionsFactory->createByOptionId()` and `:196` the `Entity\Carrier` argument; the pickup point configuration is `Carrier\PacketaPickupPointsConfig` in `Checkout/CheckoutValidator.php:89,179`, not in the anchored method. |
| 112-113 | It uses the attribute keys, the address mapper, the order repository and the pickup point validator of `packetery-module-order`. | CONFIRMED | `Checkout/OrderUpdater.php:238` `Order\Attribute::$pickupPointAttributes`, `:257` `mapper->toWcOrderShippingAddress()`, `:214` `orderRepository->save()`; `Checkout/CheckoutValidator.php:244` `pickupPointValidator->validate()`. |
| 113-114 | It starts the automatic packet submission after the order. | CONFIRMED | `Checkout/OrderUpdater.php:215` `packetAutoSubmitter->handleEventAsync( Order\PacketAutoSubmitter::EVENT_ON_ORDER_CREATION_FE, ... )` — in `actionUpdateOrder()`, not in the anchored `getPropsFromCheckoutData()`. |
| 114-116 | The module gives the widget its settings, its translations and the URLs of the internal REST routes. | CONFIRMED | `Checkout/CheckoutSettings.php:187-190` the four `apiRouter->get*Url()` calls, `:194` `widgetUrl`, `:198-208` `translations`. |
| 117-120 | The module uses the internal REST router of `packetery-module-api`, the shipping method classes of `packetery-module-shipping`, and the logger, exceptions, views and product category helpers of the other Packeta modules. | CONFIRMED | `Checkout/CheckoutSettings.php:105` `Api\Internal\CheckoutRouter`; `Checkout/ShippingRateFactory.php:16` `Shipping\BaseShippingMethod`; `Checkout/CheckoutRenderer.php:12` `Views\UrlBuilder`; `Checkout/CartService.php:13` `ProductCategory\ProductCategoryEntityFactory`; `DiagnosticsLogger` and `Exception\ProductNotFoundException` across the namespace. |
| 121-122 | The module reads the cart and the session **only** through the adapters of `packetery-module-framework`. | INCORRECT | The module also calls `WC_Cart` methods directly. `Checkout/RateCalculator.php:245` reads the cart without an adapter: `$coupons = $cartOrOrder->get_coupons();`. `Checkout/Checkout.php:167` takes the raw object (`public function actionCalculateFees( WC_Cart $cart )`) and writes to it at `:261` and `:304` `$cart->add_fee( ... )`. `Checkout/CartService.php:99` reads a cart item product directly: `$cartItem['data']->get_price( 'raw' )`. |
| 122-124 | One external currency switcher plugin is supported, and every other conversion goes through a filter. | CONFIRMED | `Checkout/CurrencySwitcherService.php:43-45` `$supportedCurrencySwitchers` holds one entry; `:55-64` falls through to `applyFilters( 'packetery_price', $price )`. |
| 128 | The module owns no database table. | CONFIRMED | `grep -r 'wpdb\|CREATE TABLE\|->query('` over `Checkout/` returns 0 hits. |
| 128-129 | The module keeps the `checkout-data` in one WordPress transient and the selected methods in the WooCommerce session. | CONFIRMED | `Checkout/CheckoutStorage.php:55-61` `setTransient()`; `Checkout/SessionService.php:30,40` `sessionGetArray`/`sessionGetString`. |
| 133 | `CHECKOUT_DATA_PREFIX`: the transient name adds the session token of the user or the customer id of a guest. | CONFIRMED | `Checkout/CheckoutStorage.php:131-140`: `getSessionToken()` when `isUserLoggedIn()`, otherwise `sessionGetCustomerId()`, concatenated to `Transients::CHECKOUT_DATA_PREFIX`. |
| 134 | `POINT_ID`: pickup point that the customer selected in the widget. | CONFIRMED | `Checkout/CheckoutStorage.php:103-107` restores `Order\Attribute::POINT_ID` and the pickup point attributes. |
| 135 | `ADDRESS_IS_VALIDATED`: result of the address validation in the widget. | CONFIRMED | `Checkout/CheckoutValidator.php:306-307` compares `$checkoutData[ Order\Attribute::ADDRESS_IS_VALIDATED ]` with `'1'`. |
| 136 | `CAR_DELIVERY_ID`: car delivery selection. | CONFIRMED | `Checkout/OrderUpdater.php:297` `setCarDeliveryId( $checkoutData[ Order\Attribute::CAR_DELIVERY_ID ] )`. |
| 137 | `CARRIER_ID`: carrier of the selected shipping rate. | CONFIRMED | `Checkout/OrderUpdater.php:139-142` `$propsToSave[ Order\Attribute::CARRIER_ID ] = $carrierId;`. |
| 138 | expiration: the filter `wc_session_expiration` can change the default. | CONFIRMED | `Checkout/CheckoutStorage.php:215-223`: default `2 * DAY_IN_SECONDS`, the filtered value wins when it is numeric and larger. |
| 139 | session `chosen_shipping_methods`: shipping method that the customer selected. | CONFIRMED | `Checkout/SessionService.php:30` `sessionGetArray( 'chosen_shipping_methods' )`. |
| 140 | session `chosen_payment_method`: payment method for the surcharge and the gateway filter. | CONFIRMED | `Checkout/SessionService.php:40`; used at `Checkout/Checkout.php:268` (surcharge) and `:361` (gateway filter path). |
| 141 | session `shipping_for_package_`: the module clears the cached rates of a package. | CONFIRMED | `Checkout/SessionService.php:50` `sessionSet( 'shipping_for_package_' . $index, false )`. |
| 142 | package `packetery_payment_method`: the module adds the payment method to the package so the rate cache hash changes. | CONFIRMED | `Checkout/SessionService.php:64-70`; the class comment at `:55-58` states the cache-hash reason. |
| 144-145 | The module reads the stored data back into the POST data of the checkout. | CONFIRMED | `Checkout/CheckoutStorage.php:75-125` merges the transient into `$this->httpRequest->getPost()`. |
| 145-147 | When a guest signs in, the module moves the transient to the session of the user. | CONFIRMED | `Checkout/CheckoutStorage.php:174-184` `migrateGuestSessionToUserSession()`, registered on `woocommerce_guest_session_to_user_id` in `HookRegistrar.php:608-614`. |
| 147-149 | The order module owns the attribute keys and this module uses them as the keys of the stored data. | CONFIRMED | `Checkout/CheckoutStorage.php:103-123` uses `Order\Attribute::POINT_ID`, `ADDRESS_IS_VALIDATED`, `CAR_DELIVERY_ID`, `CARRIER_ID`; the anchored `getFromTransient()` (`:48-50`) itself names no attribute key. |
| 153-155 | The cart values are zero before `wp_loaded`, and the weight of the cart is then zero. | CONFIRMED | `Checkout/CartService.php:81-83` `if ( $this->wpAdapter->didAction( 'wp_loaded' ) === 0 ) { return 0.0; }`. |
| 156-157 | The size of the largest product is `null` in the same situation. | CONFIRMED | `Checkout/CartService.php:221-223` returns `null` on the same `didAction( 'wp_loaded' ) === 0` guard. |
| 157-159 | Only one currency switcher plugin is supported by name, and its plugin file is a hardcoded string. | CONFIRMED | `Checkout/CurrencySwitcherService.php:55` `isPluginActive( 'woocommerce-currency-switcher/index.php' )`. |
| 161-163 | The order update ends without an error when the chosen method is missing, the stored checkout data is empty, or the carrier is not found; it writes a log line and returns. | CONFIRMED | `Checkout/OrderUpdater.php:128-135`, `:156-160`, `:172-177` — each logs through `diagnosticsLogger` and returns `void`. |
| 163-165 | The fee logic of the cart is skipped in the same silent way when the customer selects a method of another plugin. | CONFIRMED | `Checkout/Checkout.php:171-184` logs `'No packetery shipping method chosen'` and returns. |
| 165-167 | The name of the shipping class group falls back to a hardcoded slug when a product has no class. | CONFIRMED | `Checkout/RateCalculator.php:260-267` `getProductShippingClassSlug()` returns `'no_class'`; reached from the anchored `getFinalShippingClassesCost()` through `getCartItemsGroupedByShippingClass()` (`:282`). |
| 168 | The module contains no TODO comment and no FIXME comment. | CONFIRMED | `grep -r 'TODO\|FIXME' Checkout/` returns 0 hits. |
| 170-172 | The class that builds the widget settings gives an AJAX callback, but the registration is outside this module. | CONFIRMED | `Checkout/CheckoutSettings.php:218-226` `actionCreateSettingsAjax()`; registered in `HookRegistrar.php:436-444`. |

CONFIRMED 68 · INCORRECT 4 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-checkout.yaml

| entry | class | evidence |
|---|---|---|
| `service: woocommerce` | CONFIRMED | Repository root holds the WooCommerce plugin; matches `repo: woocommerce` in the document front matter. |
| `source_commit: 3d44ef98` | CONFIRMED | `git log 3d44ef98` resolves to "docs: add reference page and fragment for packetery-module-order"; matches the document front matter. |
| `aliases: [packetery-module-checkout]` | CONFIRMED | Same id as `modules[0].name` and the document `module:` field. |
| assumption: "Generated on branch docs/teams at commit 3d44ef98." | CONFIRMED | `git branch --show-current` returns `docs/teams`; `3d44ef98` is an ancestor of HEAD. |
| assumption: module boundary from the PSR-4 namespace; counts from a `--split-psr4` run; test directories excluded | CONFIRMED | `ai-docs/ast/map.md` lists project rows only (`WooCommerce`, `deps-composer`), so the committed map has no per-module split. The 2162 line count reproduces exactly over the 12 namespace files, which hold no test directory. |
| assumption: "The STE skill esterka:writing-technical-docs-in-ste was invoked for the prose of this document." | UNCERTAIN | A skill invocation leaves no trace in the repository; nothing here confirms or refutes it. |
| `modules[0]` name / path / lines: 2162 / doc | CONFIRMED | `src/Packetery/Module/Checkout` exists with 12 files; the line count matches (see above); `ai-docs/reference/packetery-module-checkout.md` exists. |
| `exposes: createShippingRates` (consumers woocommerce, auth none) | CONFIRMED | `Checkout/ShippingRateFactory.php:105`; called from `src/Packetery/Module/ShippingMethod.php:67` and `src/Packetery/Module/Shipping/BaseShippingMethod.php:137`, both WooCommerce shipping methods, with no auth check. |
| `exposes: actionValidateCheckoutData` | CONFIRMED | `Checkout/CheckoutValidator.php:113`, registered on `woocommerce_after_checkout_validation` (`Checkout/Checkout.php:89`). |
| `exposes: actionUpdateOrder` | CONFIRMED | `Checkout/OrderUpdater.php:126`, registered on `woocommerce_store_api_checkout_order_processed` (`Checkout/Checkout.php:99-105`). |
| `exposes: createSettings` | CONFIRMED | `Checkout/CheckoutSettings.php:138`; consumed by `src/Packetery/Module/Views/AssetManager.php:150` `localizeScript( 'packetery-checkout', 'packeteryCheckoutSettings', ... )`. |
| `calls: packeta-widget` (evidence `CheckoutSettings#createSettings`) | CONFIRMED | `Checkout/CheckoutSettings.php:194` passes `widgetUrl` to the browser; `src/Packetery/Module/WidgetUrlResolver.php:19-25` resolves it. |
| `owns_data: checkout-data`, store mysql, evidence `CheckoutStorage#setTransient` | CONFIRMED | `Checkout/CheckoutStorage.php:55-61` `setTransient()`; one transient per session token or guest customer id (`:131-140`). WordPress stores a transient in the options table unless a persistent object cache is installed. |
| `unknowns[0]`: "Where WordPress registers the AJAX callback that returns the widget settings", resolved_by "the module that builds the service container" | INCORRECT | The place is known and the document already names it. Prose line 33-34 anchors `src/Packetery/Module/Hooks/HookRegistrar.php#registerBackEnd`, and the code is `HookRegistrar.php:436` `$this->wpAdapter->addAction( 'wp_ajax_get_settings', [ $this->checkoutSettings, 'actionCreateSettingsAjax' ] );`. `HookRegistrar` registers hooks; it does not build the service container. |
| `unknowns[1]`: "Where the checkout script is enqueued...", checked "the namespace holds no enqueue call", resolved_by `packetery-module-root` | INCORRECT | The "checked" half holds (`grep -i enqueue Checkout/` returns 0 hits), but the pointer is wrong: the enqueue lives in `packetery-module-views`, not in the root namespace — `src/Packetery/Module/Views/AssetManager.php:122` `enqueueFrontAssets()`, `:149-150` `enqueueScript( 'packetery-checkout', 'public/js/checkout.js', ... )` and `localizeScript( ..., $this->checkoutSettings->createSettings() )`. |
| `unknowns[2]`: "Which currency switcher plugins the shops must support", checked "the supported plugin list of this module" | CONFIRMED | `Checkout/CurrencySwitcherService.php:43-45` holds one entry (`WOOCS`); the question is a business one and stays open. |

CONFIRMED 13 · INCORRECT 2 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
