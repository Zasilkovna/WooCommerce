## ai-docs/reference/packetery-module-views.md @ b34fe03c — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 6 | `source-commit: b34fe03c` | CONFIRMED | `ai-docs/manifest.yaml:24` `source_commit: b34fe03c3d932c007f21b970c38acd8241184d07` |
| 8 | `covers: [src/Packetery/Module/Views]` | CONFIRMED | `ai-docs/manifest.yaml:211` `path: src/Packetery/Module/Views`; the directory holds 6 `.php` files and no templates |
| 13 | Repo woocommerce · Module packetery-module-views · Type reference | CONFIRMED | `ai-docs/manifest.yaml:1` `service: woocommerce`, module entry at `manifest.yaml:210`, `doc: reference/packetery-module-views.md` |
| 17 | packetery-module-views renders what the Packeta plugin shows | INCORRECT | 25 files outside the module render Latte templates, for example `src/Packetery/Module/Order/Metabox.php`, `src/Packetery/Module/Order/GridExtender.php`, `src/Packetery/Module/MessageManager.php`, `src/Packetery/Module/DashboardWidget.php` (`grep -rl 'latteEngine->render' src/Packetery \| grep -v /Views/` = 25). The module renders 5 of the 65 templates in `template/` |
| 17–18 | it loads the scripts and the styles that those screens need | CONFIRMED | `src/Packetery/Module/Views/AssetManager.php:94` `enqueueScript`, `:110` `enqueueStyle`, `:122` `enqueueFrontAssets`, `:157` `enqueueAdminAssets` |
| 18–20 | The module holds the templates of the admin order detail, of the customer order detail and of the email footer [VERIFY: ViewAdmin.php#renderDeliveryDetail] | INCORRECT | The anchored method loads the template from outside the module: `ViewAdmin.php:90` `PACKETERY_PLUGIN_DIR . '/template/order/delivery-detail.latte'`; likewise `ViewFrontend.php:73` `'/template/order/detail.latte'` and `ViewMail.php:88` `'/template/email/order.latte'`. `covers:` is `src/Packetery/Module/Views` only, and `find src/Packetery/Module/Views -type f` returns 6 `.php` files and no `.latte`. The module renders the templates, it does not hold them |
| 20–21 | It decides which asset belongs to which screen [VERIFY: AssetManager.php#enqueueAdminAssets] | CONFIRMED | `AssetManager.php:157–409`, branches on `isOrderGridPage`, `isOrderDetailPage`, `isProductPage`, `$page === Carrier\OptionsPage::SLUG`, `$page === Options\Page::SLUG`, `isPluginsOverviewPage` |
| 23–24 | runs the onboarding tours, which show the administrator one setting after another [VERIFY: WizardAssetManager.php#enqueueWizardAssets] | CONFIRMED | `WizardAssetManager.php:51` `enqueueWizardAssets`, `:74–76` loads `public/libs/driverjs-1.3.4/driver.js.iife.js`, `:97` `enqueueTourScript`, tour builders at `:105`, `:441`, `:487` |
| 25 | The module holds 6 files | CONFIRMED | `find src/Packetery/Module/Views -name '*.php' \| wc -l` = 6 (AssetManager, UrlBuilder, ViewAdmin, ViewFrontend, ViewMail, WizardAssetManager); no test file is inside `covers:` |
| 25 | 1090 lines of logic | CONFIRMED | non-empty non-comment lines of the 6 files = 1090; the same number is in `ai-docs/manifest.yaml:212` `lines: 1090` |
| 25 | 18 public methods | CONFIRMED | `grep -cE '^\s*(final )?public( static)? function'` over the 6 files = 2+2+4+5+3+2 = 18 |
| 25–26 | It carries no Packeta business rule: every value comes from another module | INCORRECT | `AssetManager.php:164–167` computes a rule of its own: `'deliverOnMinDate' => $this->wpAdapter->date( CoreHelper::DATEPICKER_FORMAT, strtotime( 'tomorrow' ) )` — the earliest delivery date is set here, not taken from another module. `WizardAssetManager.php:105–585` also builds its own tour texts and hard-codes Packeta support links, and `:563` sets the default page size `get_option( 'edit_shop_order_per_page', 20 )` |
| 28 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 32–33 | the module exposes the render methods and the asset methods that the hook module registers | INCORRECT | Two of the ten members of the table below are not registered by the hook module: `grep -rn 'UrlBuilder' src/Packetery/Module/Hooks/` returns nothing, so `buildAssetUrl` and `getCarrierConfigLink` are library calls, not hook callbacks. `HookRegistrar.php:377, 404, 450, 451, 526, 541, 598, 616` register the other eight |
| 37 | `enqueueFrontAssets` loads the checkout style and either the widget library or the checkout script | CONFIRMED | `AssetManager.php:127` `packetery-front-styles`, `:140–147` `packetery-widget-library` when blocks are used, `:148–150` `packetery-checkout` otherwise |
| 38 | `enqueueAdminAssets` loads the styles and the scripts of the admin screen that the request opens | CONFIRMED | `AssetManager.php:157–409` |
| 39 | `buildAssetUrl` builds the URL of a plugin asset and adds a cache key from the file time | CONFIRMED | `UrlBuilder.php:28–36`, `addQueryArg( [ 'v' => md5( (string) filemtime( $filename ) ) ], $url )` |
| 40 | `getCarrierConfigLink` builds the admin link to the settings of one carrier | CONFIRMED | `UrlBuilder.php:39–53`, `OptionsPage::SLUG` plus `OptionsPage::PARAMETER_CARRIER_ID` on `getAdminUrl( null, 'admin.php' )` |
| 41 | `renderDeliveryDetail` shows the pickup point and the validated address on the order detail | CONFIRMED | `ViewAdmin.php:89–105`, template params `pickupPoint` and `validatedDeliveryAddress` |
| 42 | `renderConfirmModalTemplate` renders the confirmation window of the packet actions | CONFIRMED | `ViewAdmin.php:111–125`, `template/confirm-modal-template.latte` |
| 43 | `echoInactiveWooCommerceNotice` shows the notice that WooCommerce is not active | CONFIRMED | `ViewAdmin.php:132–149`, returns when `isWooCommercePluginActive() === true`, else renders `template/admin-notice.latte` |
| 44 | `renderOrderDetail` shows the delivery information to the customer | CONFIRMED | `ViewFrontend.php:56–95`, `template/order/detail.latte` |
| 45 | `renderEmailFooter` adds the delivery information to the order email through one filter | CONFIRMED | `ViewMail.php:52–104`, `applyFilters( 'packeta_email_footer', $emailHtml, $templateParams )` |
| 46 | `enqueueWizardAssets` starts the tour that the query parameters and the stored flags select | CONFIRMED | `WizardAssetManager.php:52–55` reads `wizard-enabled` and the two `PACKETERY_TUTORIAL_*` options, `:57–70` branches on them |
| 50–52 | the checkout gets the front style, the widget library with the block checkout, the checkout script with the classic one [VERIFY: AssetManager.php#enqueueFrontAssets] | CONFIRMED | `AssetManager.php:123` `isCheckout()` guard, `:127`, `:140` `areBlocksUsedInCheckout()`, `:148` `elseif` |
| 53–54 | the module passes the widget settings to the browser under one name, and the values come from the checkout module | CONFIRMED | `AssetManager.php:150` `localizeScript( 'packetery-checkout', 'packeteryCheckoutSettings', $this->checkoutSettings->createSettings() )`, `CheckoutSettings` from `Packetery\Module\Checkout` |
| 58 | Checkout: `packetery-front-styles`, `packetery-checkout` or `packetery-widget-library`, data `packeteryCheckoutSettings` | CONFIRMED | `AssetManager.php:127, 142, 149, 150` |
| 59 | Order grid: `packetery-admin-styles`, `packetery-admin-grid-order-edit-js`, `packetery-admin-stored-until-modal-js`, data `datePickerSettings` | CONFIRMED | `AssetManager.php:271, 275–286, 288–298` |
| 60 | Order detail: `packetery-multiplier`, `admin-order-detail`, pickup point picker and address picker, data `packeteryPickupPointPickerSettings`, `packeteryAddressPickerSettings` | CONFIRMED | `AssetManager.php:305, 314, 353–368, 371–385` |
| 61 | Carrier settings: `packetery-select2`, `packetery-multiplier`, `packetery-admin-country-carrier`, data `packeteryCountryCarrier` | CONFIRMED | `AssetManager.php:182, 186, 195, 205–212` under `$page === Carrier\OptionsPage::SLUG` |
| 62 | Plugin settings: `packetery-admin-options`, the WordPress editor and the bug report editor, data `translationsAdminOptions` | CONFIRMED | `AssetManager.php:217, 227–238, 239 (enqueueScript 'editor'), 240–249 ('packetery-bug-report-editor')` |
| 63 | Plugin list: `packetery-admin-confirm-deactivation`, data `translationsDeactivation` | CONFIRMED | `AssetManager.php:390–406` under `isPluginsOverviewPage()` |
| 64 | Onboarding tour: the tour library and one script for each tour, data `wizardTourConfig` | CONFIRMED | `WizardAssetManager.php:74–76` driver.js library, `:147–149` `enqueueScript( 'packetery-admin-wizard-tour', 'public/js/tours/' . $scriptName … )` and `localizeScript( …, 'wizardTourConfig', … )` |
| 66–68 | The module renders five templates: admin delivery detail, confirmation modal, admin notice, customer order detail, email footer | CONFIRMED | Five render sites: `ViewAdmin.php:90`, `ViewAdmin.php:116`, `ViewAdmin.php:141`, `ViewFrontend.php:73`, `ViewMail.php:88`. The anchor on `ViewFrontend.php#renderOrderDetail` shows one of the five; the count holds for the module |
| 68–70 | A shop can add its own checkout style: the module loads a file of a fixed name from the content directory when it exists | CONFIRMED | `AssetManager.php:130–138`, `'packeta-custom-front.css'` under `WP_CONTENT_DIR`, guarded by `file_exists` |
| 74–82 | `references →` list: core, root, order, checkout, carrier, options, log | INCORRECT | The set is incomplete. `packetery-module-framework` is missing although every class uses it (`use Packetery\Module\Framework\WpAdapter;` in all 6 files, `WcAdapter` in `AssetManager.php:13` and `WizardAssetManager.php:6`). `packetery-module-dashboard` is missing (`AssetManager.php:12` `use Packetery\Module\Dashboard\DashboardPage;`, used at `:266`). `packetery-module-shipping` is missing (`WizardAssetManager.php:11` `use Packetery\Module\Shipping\ShippingProvider;`). The module-to-path mapping is `ai-docs/manifest.yaml:113–213` |
| 84–85 | The admin order detail reads the order through the repository of `packetery-module-order` [VERIFY: AssetManager.php#enqueueAdminAssets] | INCORRECT | The anchored method holds no order repository: `AssetManager.php:66–84` injects `ContextResolver`, `Metabox`, `Request`, `CheckoutSettings`, `WpAdapter`, `WcAdapter`, `CheckoutService`, `WidgetUrlResolver` only. The repository read is in `ViewAdmin.php:81` `$this->orderRepository->getByWcOrderWithValidCarrier( $wcOrder )` |
| 85–86 | the widget settings of the pickup point picker come from its metabox [VERIFY: AssetManager.php#enqueueAdminAssets] | CONFIRMED | `AssetManager.php:326–327` `$this->orderMetabox->getPickupPointWidgetSettings()` and `getAddressWidgetSettings()`, `Metabox` from `Packetery\Module\Order` |
| 86–87 | The checkout settings come from `packetery-module-checkout` [VERIFY: AssetManager.php#enqueueFrontAssets] | CONFIRMED | `AssetManager.php:150` `$this->checkoutSettings->createSettings()`, `use Packetery\Module\Checkout\CheckoutSettings;` at `:10` |
| 88–89 | The module asks `packetery-module-root` which admin screen the request opens [VERIFY: ViewAdmin.php#renderConfirmModalTemplate] | CONFIRMED | `ViewAdmin.php:112` `$this->contextResolver->isConfirmModalPage()`; `ContextResolver` lives at `src/Packetery/Module/ContextResolver.php`, which `manifest.yaml:206–207` maps to `packetery-module-root` (`path: src/Packetery/Module`) |
| 89–91 | The URL of the widget library comes from the resolver of `packetery-module-root` [VERIFY: UrlBuilder.php#buildAssetUrl] | INCORRECT | The anchored method never touches the widget resolver: `UrlBuilder.php:29` `$this->wpAdapter->pluginDirUrl( PACKETERY_PLUGIN_DIR . '/packeta.php' ) . $asset` — it builds plugin asset URLs. The widget library URL comes from `AssetManager.php:143` `$this->widgetUrlResolver->getUrl()` (also `:345`) |
| 95–96 | Three render methods end without a message when the input is not a WooCommerce order, or when the order has no valid carrier | CONFIRMED | `ViewAdmin.php:75–84`, `ViewFrontend.php:57–66`, `ViewMail.php:53–70`; all three `return` with no user message |
| 97–98 | The module writes the type error to the log and returns [VERIFY: ViewMail.php#renderEmailFooter] | INCORRECT | The anchored method writes nothing to the log. `ViewMail.php:64–66` is `if ( $wcOrder === null ) { return; }`, and `ViewMail.php` does not use `ArgumentTypeErrorLogger` at all (`grep -n 'use ' src/Packetery/Module/Views/ViewMail.php` lists `Engine`, `WpAdapter`, `ModuleHelper`, `DetailCommonLogic`, `Repository`, `WC_Email`, `WC_Order`). Only two of the three methods log: `ViewAdmin.php:76` and `ViewFrontend.php:58` `$this->argumentTypeErrorLogger->log( __METHOD__, 'wcOrder', WC_Order::class, $wcOrder )` |
| 98–100 | The asset URL is `null` when the file is missing, and the caller gets no error [VERIFY: UrlBuilder.php#buildAssetUrl] | CONFIRMED | `UrlBuilder.php:32–34` `if ( ! file_exists( $filename ) ) { return null; }` |
| 102–103 | The onboarding tour reads a page number from the request without a nonce check, and a comment says that the read is safe [VERIFY: WizardAssetManager.php#enqueueWizardAssets] | CONFIRMED | `WizardAssetManager.php:562` `// phpcs:ignore WordPress.Security.NonceVerification.Recommended -- Only reading paged parameter for listing`, `:563` `$page = isset( $_GET['paged'] ) ? max( 1, (int) $_GET['paged'] ) : 1;`. The private method `hasTableGridOurShippingMethods()` is reachable from the anchored entry point: `:64 enqueueOrderGridTours()` → `:121 hasTableGridOurShippingMethods()` |
| 104–105 | The decision which asset belongs to which screen is one long branch, so a new admin screen changes that method [VERIFY: AssetManager.php#enqueueAdminAssets] | CONFIRMED | `AssetManager.php:157–409`: one method of about 250 lines with 12 sequential `if` blocks keyed on page and screen |
| 106 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rn 'TODO\|FIXME' src/Packetery/Module/Views/` returns nothing |

CONFIRMED 38 · INCORRECT 8 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-views.yaml

Separate fragments no longer exist; the entries below are verified in `ai-docs/manifest.yaml`.

| entry | class | evidence |
|---|---|---|
| `aliases:` `- packetery-module-views` (manifest.yaml:22) | CONFIRMED | The module directory exists: `src/Packetery/Module/Views` with 6 `.php` files |
| `modules:` `name: packetery-module-views` / `path: src/Packetery/Module/Views` (manifest.yaml:210–211) | CONFIRMED | `find src/Packetery/Module/Views -name '*.php' \| wc -l` = 6; the path matches `covers:` in the document front matter |
| `modules:` `lines: 1090` (manifest.yaml:212) | CONFIRMED | non-empty non-comment lines of the 6 `.php` files = 1090; same number in the document line 25 |
| `modules:` `doc: reference/packetery-module-views.md` (manifest.yaml:213) | CONFIRMED | `ai-docs/reference/packetery-module-views.md` exists and its front matter says `module: packetery-module-views` |
| `modules:` `evidence:` for packetery-module-views | NOT FOUND | The key does not exist. `awk 'NR>=113 && NR<=213' ai-docs/manifest.yaml \| grep -c evidence` = 0, so no `modules:` entry of any of the 25 modules carries `evidence:`; the key is used under `exposes:` only. This is the manifest schema, not a defect of this module |
| `exposes:` `kind: library`, `name: buildAssetUrl`, `module: packetery-module-views`, `evidence: src/Packetery/Module/Views/UrlBuilder.php#buildAssetUrl`, notes "Builds the URL of a plugin asset with a cache key." (manifest.yaml:311–319) | CONFIRMED | `UrlBuilder.php:28` `public function buildAssetUrl( string $asset ): ?string`, cache key at `:35` `md5( (string) filemtime( $filename ) )` |
| `exposes:` `kind: html`, `name: renderEmailFooter`, `module: packetery-module-views`, `evidence: src/Packetery/Module/Views/ViewMail.php#renderEmailFooter`, notes "Adds the delivery information to the order email of the shop." (manifest.yaml:583–591) | CONFIRMED | `ViewMail.php:52` `public function renderEmailFooter( $email ): void`, renders `template/email/order.latte` and applies the `packeta_email_footer` filter; registered at `HookRegistrar.php:404` |
| `exposes:` `kind: html`, `name: renderOrderDetail`, `module: packetery-module-views`, `evidence: src/Packetery/Module/Views/ViewFrontend.php#renderOrderDetail`, notes "Shows the pickup point or the validated address to the customer." (manifest.yaml:592–600) | CONFIRMED | `ViewFrontend.php:56` `public function renderOrderDetail( $wcOrder ): void`, renders `template/order/detail.latte` with `displayPickupPointInfo`; registered at `HookRegistrar.php:616` |
| No other entry carries `module: packetery-module-views` or `resolved_by: packetery-module-views` (`calls:`, `owns_data:`, `unknowns:`) | CONFIRMED | `grep -n 'packetery-module-views' ai-docs/manifest.yaml` returns only lines 22, 210, 213, 313, 585, 593; `grep -n 'resolved_by' ai-docs/manifest.yaml` shows no value `packetery-module-views` |

CONFIRMED 8 · INCORRECT 0 · UNCERTAIN 0 · NOT FOUND 1 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
