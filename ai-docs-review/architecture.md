## ai-docs/architecture.md @ 4034cda7 — 2026-09-22

Verified against the working tree of branch `docs/teams` at commit 44060f2e (the file gained the
section "runtime flows of the carrier list and the label print" in that commit while this
verification ran; line numbers below are those of the 158-line file).

| line | claim | class | evidence |
|---|---|---|---|
| 6 | front matter `source-commit: 4034cda7` | INCORRECT | `index.md` says "woocommerce — documentation generated from commit `b34fe03c`" and `manifest.yaml` carries `source_commit: b34fe03c3d932c007f21b970c38acd8241184d07`; 4034cda7 ("docs: fix review findings in packetery-core") is older than b34fe03c, and the page itself was last changed at 44060f2e |
| 17-18 | one WordPress plugin with a domain module and a set of integration modules [Plugin.php#run] | CONFIRMED | one plugin file `packeta.php`; `manifest.yaml` `modules:` lists 25 modules under `src/Packetery`, `packetery-core` = `src/Packetery/Core`. The anchor itself shows only `$this->hookRegistrar->register()` (Plugin.php:54-55) |
| 18-19 | the domain module holds the Packeta contract and no WordPress code | CONFIRMED | no WordPress or WooCommerce symbol in `src/Packetery/Core` (0 hits for `add_action|add_filter|get_option|wp_*|__(|WC_|wc_`); Core holds Api, Entity, Interfaces, Log, PickupPointProvider, Validator |
| 19-20 | "Every other module speaks to WordPress and to WooCommerce" | INCORRECT | `src/Packetery/Module/Exception` (5 exception classes) and `src/Packetery/Module/Payment/PaymentHelper.php` contain no WordPress or WooCommerce call (0 hits); PaymentHelper takes only `OptionsProvider` |
| 20 | "one module registers all the callbacks" [HookRegistrar.php#addMenuPages] | INCORRECT | `addMenuPages()` (HookRegistrar.php:652) only calls `registerMenuPage()`, `registerSubmenuPage()` and the `register()` of five admin pages — it registers no plugin callback. The symbol that registers the callbacks is `HookRegistrar::register()` (line 372), which holds 25 `register()` calls |
| 24-33 | diagram edge `checkout --> shipping` | INCORRECT | the direction is the other way round: `BaseShippingMethod::calculate_shipping` (Shipping, line 125-141) calls `$this->shippingRateFactory->createShippingRates(...)` (Checkout). `dependencies.md` records `\| packetery-module-shipping \| packetery-module-checkout \| references \|`, and this page says the same at line 46-48 |
| 22-33 | diagram edge set as the picture of the module composition | INCORRECT | `dependencies.md` "links between modules" lists `packetery-module-checkout -> packetery-module-order` [OrderUpdater#actionUpdateOrder] and `Most modules -> packetery-module-framework` (line 58) [WcAdapter]; neither edge is in the diagram. The diagram's `hooks --> options` is not in that table (the code supports it, HookRegistrar.php:653) |
| 35-43 | "Textual description of the links of the diagram" | INCORRECT | the list names 7 modules, the diagram has 9 nodes: `packetery-module-options` (line 26) and `packetery-module-root` (line 32) are missing. Every line also lacks the source module ("references → X"), so the list records no direction, unlike the two-column table in `dependencies.md` |
| 45-46 | the plugin class starts packetery-module-hooks, which registers the callbacks of every module for the current request [Plugin.php#run] | CONFIRMED | Plugin.php:54-55 `run()` → `$this->hookRegistrar->register()`; `HookRegistrar::register()` (line 372) is the single entry point and calls the `register()` of the other modules (25 call sites) |
| 46-48 | shipping gives WooCommerce one method per carrier, and that method asks checkout for the price [BaseShippingMethod.php#calculate_shipping] | CONFIRMED | one generated class per carrier (`Module/Shipping/Generated`, `ShippingMethodGenerator::generateClass`); `calculate_shipping` (line 125) collects the zone methods and calls `Checkout\ShippingRateFactory::createShippingRates` |
| 49-50 | "packetery-module-order and packetery-module-carrier call the API clients of packetery-core" [PacketSubmitter.php#submitPacket] | INCORRECT | true for order (`submitPacket` line 196+ uses `$this->soapApiClient`), false for carrier: `Module/Carrier/Downloader.php` downloads the feed with `Module\WebRequestClient` (`$this->webRequestClient->get(...)` in `download_json`), and its only Core import is the exception class `Packetery\Core\Api\WebRequestException` (line 12). `dependencies.md` anchors carrier → core at `PacketaPickupPointsConfig#getVendorCarriers`, which is entities and providers, not an API client |
| 50-52 | packetery-module-api writes the browser data into the checkout storage and into the order [OrderController.php#saveModal] | CONFIRMED | the anchor shows the order half only (`$this->orderRepository->save( $order )`, OrderController.php:248); the checkout-storage half is `CheckoutController::save` → `CheckoutStorage` (CheckoutController.php:38, 97-125) |
| 58-59 | flow step 1: WooCommerce asks the method of each carrier for a rate, the method asks the checkout module [BaseShippingMethod.php#calculate_shipping] | CONFIRMED | BaseShippingMethod.php:125-141, `createShippingRates` then `add_rate` |
| 60-61 | flow step 2: the checkout hides the carriers that cannot deliver the cart and computes the price of the others [ShippingRateFactory.php#canCreateShippingRate] | CONFIRMED | `canCreateShippingRate` (line 196-240) drops the disallowed rate ids and the carriers that do not fit; the price is computed one level up in `createShippingRateOfCarrier` → `RateCalculator` |
| 62-63 | flow step 3: the browser saves the selection through the internal REST routes [CheckoutController.php#saveSelectedPickupPoint] | CONFIRMED | CheckoutController.php:97-101, route `PATH_SAVE_SELECTED_PICKUP_POINT` registered at line 49-58 |
| 64-65 | flow step 4: the checkout validates the selection before WooCommerce creates the order [CheckoutValidator.php#actionValidateCheckoutData] | CONFIRMED | Checkout.php:89 hooks it on `woocommerce_after_checkout_validation`, which fires before the order is created |
| 66-67 | flow step 5: the order module writes the pickup point, the address and the carrier to its own table [OrderUpdater.php#actionUpdateOrder] | CONFIRMED | `OrderUpdater::actionUpdateOrder` ends with `$this->orderRepository->save( $order )` (line 214), i.e. `Module\Order\Repository`. Note the step is orchestrated in packetery-module-checkout (`namespace Packetery\Module\Checkout`, OrderUpdater.php:5) |
| 68-69 | flow step 6: the administrator submits the packet, or the automatic submission does it on an order state [PacketAutoSubmitter.php#handleEvent] | CONFIRMED | `handleEvent` (line 119) checks the auto-submission option and the event mapped to the payment gateway, then calls `packetSubmitter->submitPacket` |
| 70-71 | flow step 7: a scheduled job reads the packet status back and can change the order status [PacketSynchronizer.php#syncStatuses] | CONFIRMED | CronService.php:85-86 binds `syncStatuses` to the two packet-status hooks; `syncStatus` calls `apiSoapClient->packetStatus` and `wcOrderActions->updateOrderStatus` |
| 73-74 | a carrier that no feed contains is marked as deleted and stops appearing [Carrier/Repository.php#set_as_deleted] | CONFIRMED | Repository.php:188 `UPDATE ... SET deleted = 1 WHERE id IN (...)`; the read queries filter `WHERE deleted = false` (Repository.php:180) |
| 75-76 | a failure of the Packeta API stops the step and writes the fault to the order and to the log [PacketSubmitter.php#submitPacket] | CONFIRMED | PacketSubmitter.php:236-250 and 273-287: `logger->add( $record )` plus `order->updateApiErrorMessage(...)`, then `return` |
| 80-82 | a scheduled job downloads the feed of the shop [Carrier/Downloader.php#run] | CONFIRMED | CronService.php:84 binds `CRON_CARRIERS_HOOK` to `carrierDownloader->runAndRender`, which calls `run()` → `fetch_as_array` → `download_json` |
| 82-84 | "the update validates every carrier of the answer and writes the differences to the carrier table" [Carrier/Updater.php#carriers_mapper] | INCORRECT | `carriers_mapper` (Updater.php:113) is private and only maps the feed fields to the table columns ("Maps input data to storage structure"). The validation is `validate_carrier_data` (line 82, called by `Downloader::run`), and the write plus the difference log is `save()` (line 151: `update`/`insert`, `getArrayDifferences`) |
| 84-85 | a carrier that the feed no longer holds gets the deleted flag [Carrier/Repository.php#set_as_deleted] | CONFIRMED | Updater.php:222 `$this->carrierRepository->set_as_deleted( array_keys( $carriersInDb ) )` for the carriers left over after the feed pass |
| 86-87 | a carrier without a generated shipping method class gets one [ShippingMethodGenerator.php#generateClass] | CONFIRMED | Updater.php:202 calls `$this->shippingMethodGenerator->generateClass(...)` inside `save()`; the failures are collected in `$carriersNotGenerated` (line 213) |
| 87-89 | the checkout offers the new carrier as soon as the shop activates it in a zone [ShippingProvider.php#getSortedCachedMethods] | CONFIRMED | HookRegistrar.php:639-641 passes the `woocommerce_shipping_methods` list through `getSortedCachedMethods`, whose `addMethods` adds the generated class of every carrier of the zone countries |
| 93-94 | label step 1: the administrator selects orders in the grid and starts a bulk action [BulkActions.php#handleActions] | CONFIRMED | BulkActions.php:118-129 handles `LabelPrint::ACTION_PACKETA_LABELS` and `ACTION_CARRIER_LABELS`, stores the order ids in a transient and redirects to the print page |
| 95-96 | label step 2: the print page reads the label format and the offset of the sheet [LabelPrintParametersService.php#getLabelFormat] | CONFIRMED | `getLabelFormat` (line 117) and `getOffset` (line 77) in the same class |
| 97-98 | label step 3: for a carrier label the plugin first reads the courier number of each packet [CarrierLabelService.php#getPacketaPacketIdsWithCourierNumbers] | CONFIRMED | `LabelPrint::getResponse` (line 492-519) calls it before `requestCarrierLabels` |
| 99-100 | label step 4: the page asks Packeta for the label document and sends it to the browser as PDF [LabelPrint.php#requestPacketaLabels] | CONFIRMED | LabelPrint.php:295-297 builds `Request\PacketsLabelsPdf` and calls `soapApiClient->packetsLabelsPdf` |
| 101-102 | label step 5: a failed carrier label falls back to the Packeta label when the shop allows it [LabelPrint.php#getResponse] | CONFIRMED | LabelPrint.php:514-517 `if ( $fallbackToPacketaLabel === true && $response->hasFault() ) { return $this->requestPacketaLabels(...); }` |
| 104-105 | the handover protocol creates a shipment of the selected packets and reads its barcode [CollectionPrint.php#requestShipment] | CONFIRMED | `requestShipment` (line 237) calls `soapApiClient->createShipment`; line 165 `requestBarcodePng( $shipmentResult->getBarcode() )` |
| 111-113 | the shop authenticates with the API password, and the API key is derived from that password [Options/Page.php#sanitizePacketeryOptions] | CONFIRMED | Page.php:818 `$options['api_key'] = substr( $apiPass, 0, 16 );` and `$this->packetaClient->setApiPassword( $apiPass )` |
| 113-114 | inside WordPress the plugin relies on the capabilities of WordPress and on the nonce of a form | CONFIRMED | capability strings in the page registrations and the REST permission callbacks; `wp_verify_nonce` in PacketActionsCommonLogic.php:84 |
| 118 | Admin pages — capability `manage_options` | INCORRECT | only three admin screens use it (Options/Page.php:214 menu page, :226 settings submenu, Carrier/OptionsPage.php:105). The dashboard (Dashboard/DashboardPage.php:68), the log page (Log/Page.php:83), the label print (Order/LabelPrint.php:278) and the collection print (Order/CollectionPrint.php:213) use `manage_woocommerce`, and the order metabox uses `current_user_can( 'edit_post', $orderId )` (Order/Metabox.php:515) |
| 119 | Packet actions — nonce of the action and the order number [PacketActionsCommonLogic.php#checkAction] | CONFIRMED | line 84 `wp_verify_nonce( ..., self::createNonceAction( $action, $order->getNumber() ) )`, and `createNonceAction` joins the action and the order number |
| 120 | Order REST routes — capability `edit_posts`, no nonce [OrderController.php#registerRoutes] | CONFIRMED | OrderController.php:156-158 and 169-171, `permission_callback` returns `current_user_can( 'edit_posts' )`; no nonce check in the callbacks |
| 121 | Checkout REST routes — none, the permission callback always returns true [CheckoutController.php#registerRoutes] | CONFIRMED | all four routes use `'permission_callback' => '__return_true'` (lines 55, 65, 75, 85) |
| 122 | Packeta API — API password and API key of the shop [Core/Api/Soap/Client.php#setApiPassword] | CONFIRMED | the SOAP client sends `$this->apiPassword` with every request; the API key authenticates the other endpoints (Carrier/Downloader.php:158, Order/PickupPointValidator.php:53, Checkout/CheckoutSettings.php:185). The anchor shows the password only |
| 126-127 | the data lives in five tables and in the options of WordPress [Upgrade.php#runCreateTables] | CONFIRMED | `runCreateTables` (line 241) creates log, carrier, order and, through `createCustomsDeclarationTables`, the customs declaration and the customs declaration item table (CustomsDeclaration/Repository.php:368 and 396) |
| 127-129 | each table belongs to the module that reads it, and the repository of that module creates it [Order/Repository.php#createOrAlterTable] | CONFIRMED | Order/Repository.php:206, Carrier/Repository.php:63, Log/Repository.php:149, CustomsDeclaration/Repository.php:368/396 |
| 133 | order — Packeta row of a WooCommerce order, packetery-module-order | CONFIRMED | Order/Repository.php:206 `createOrAlterTable`, table `packeteryOrder` |
| 134 | carrier — carriers of the feed, packetery-module-carrier | CONFIRMED | Carrier/Repository.php:63, table `packeteryCarrier`, filled by `Carrier\Updater::save` |
| 135 | customs declaration — declaration of an order and its items, packetery-module-customsdeclaration | CONFIRMED | CustomsDeclaration/Repository.php:368 `createOrAlterTable` and :396 `createOrAlterItemTable` |
| 136 | log — result of every API call [Log/Repository.php#createTable] | NOT FOUND | `Module/Log/Repository.php` has no `createTable`; the method is `createOrAlterTable()` (line 149), which `Upgrade::createLogTable` calls |
| 137 | settings — five options of the plugin [Options/OptionNames.php:11] | CONFIRMED | line 11 is the `PACKETERY` constant; the five settings groups are `packetery`, `packetery_sync`, `packetery_auto_submission`, `packetery_advanced`, `packetery_currency_rates` (lines 11-15). `reference/packetery-module-options.md` line 58 says the same: "keeps the settings in five WordPress options" |
| 138 | checkout-data — transient of the selection of one customer [CheckoutStorage.php#setTransient] | CONFIRMED | CheckoutStorage.php:55-61 `setTransient` with the session expiration |
| 140 | "The upgrade owns the schema of every table" [Upgrade.php#check] | INCORRECT | the schema is in each module repository — `CREATE TABLE` lives in Log/Repository.php:149, Carrier/Repository.php:63, Order/Repository.php:206 and CustomsDeclaration/Repository.php:368/396. `Upgrade::runCreateTables` (line 241) only calls those four repositories, as this page itself states at line 127-129 |
| 140-141 | the upgrade runs the migrations between plugin versions [Upgrade.php#check] | CONFIRMED | `check()` (line 133) compares the stored version with `Plugin::VERSION` and runs the version-guarded migration steps |
| 141-142 | the uninstall drops the tables only when the shop permits it [Uninstaller.php#cleanUp] | CONFIRMED | `uninstall()` (line 40-50) calls `cleanUp` only when the constant `PACKETERY_REMOVE_ALL_DATA` is true; `cleanUp` (line 70-75) drops the five tables |
| 150 | scheduled jobs — carrier feed, packet status, log deletion and transient purge, all through the Action Scheduler [CronService.php#deactivate] | CONFIRMED | CronService.php:95-116 uses `as_schedule_recurring_action` and `as_schedule_cron_action` for the four job kinds (five hooks); `deactivate()` unschedules the same five |
| 151 | logging — "Every API result and every carrier change" [Log/DbLogger.php#DbLogger] | INCORRECT | a failed carrier feed call writes no log record: `Carrier/Downloader::run` returns the error text in all three failure branches ("Carrier download failed: ...") and the class has no logger at all (constructor takes Updater, OptionsProvider, WebRequestClient, WpAdapter). Carrier changes are logged (Updater.php:188-192, 295-299) |
| 152 | platform adapters — "Every call of a WordPress or WooCommerce function" [Framework/WpAdapter.php#WpAdapter] | INCORRECT | direct calls bypass the adapters: CronService.php:82-88 `add_action`, Upgrade.php:134 `get_option`, Api/Internal/OrderController.php:157 `current_user_can`, Order/PacketSubmitter.php:392 `apply_filters`, Order/BulkActions.php `set_transient`/`admin_url`, HookRegistrar.php:634 `add_action`. The WooCommerce calls also go through `Framework/WcAdapter.php`, which the anchor does not name — `dependencies.md` anchors the module-to-framework link at `WcAdapter#WcAdapter`, and it now says "Most modules", not every module |
| 153 | order storage modes — classic storage and HPOS [ModuleHelper.php#isHposEnabled] | CONFIRMED | ModuleHelper.php:163 `isHposEnabled()`; `Order/QueryProcessor.php:55-56` serves both `posts_clauses` and `woocommerce_orders_table_query_clauses` |
| 154 | flash messages — the result of an admin action [MessageManager.php#flash_message] | CONFIRMED | MessageManager.php:85 `flash_message( string $message, string $type = self::TYPE_SUCCESS, ... )` |
| 156 | the plugin dispatches no in-process event of its own | CONFIRMED | no `do_action`/`doAction` call anywhere in `src/Packetery`; only `apply_filters`/`applyFilters` |
| 157-158 | "the filters that it gives to other plugins are listed in the reference page of each module" [Checkout/RateCalculator.php#getShippingRateCost] | INCORRECT | `packetery_product_list_filters_enabled` (Product/ProductGridExtender.php:205) and `packeta_email_footer` (Views/ViewMail.php:96) appear in no page of `ai-docs/`, although `reference/packetery-module-product.md` and `reference/packetery-module-views.md` exist. The anchor itself is fine: RateCalculator.php:102 applies `packeta_shipping_price` |

CONFIRMED 43 · INCORRECT 13 · UNCERTAIN 0 · NOT FOUND 1 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
