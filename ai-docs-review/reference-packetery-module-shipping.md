## ai-docs/reference/packetery-module-shipping.md @ ffca7e2e — 2026-09-22

Verified against the code in the working tree of branch `docs/teams` at HEAD `b1053fb3`. `git diff ffca7e2e b1053fb3 -- src/Packetery/Module/Shipping` is empty, so the code is identical to the documented commit. All paths below are relative to the repository root; `Shipping/` stands for `src/Packetery/Module/Shipping/`.

Anchor precision note (does not change a class, because the recipe allows the verifier's own lookup): three rows carry an anchor that does not hold the claim. They are marked in the evidence column with "anchor imprecise".

| line | claim | class | evidence |
|---|---|---|---|
| 17-19 | Each carrier gets its own WooCommerce shipping method, which the shop owner adds to a shipping zone like any other method | CONFIRMED | `Shipping/BaseShippingMethod.php:18` extends `\WC_Shipping_Method`; `:84-88` `$this->supports = ['shipping-zones', 'instance-settings', 'instance-settings-modal']`. Anchor imprecise: `get_instance_form_fields` holds the instance settings, the zone support is in `__construct` |
| 20-21 | One base class holds the behaviour of every carrier, a generated class holds the identifier of one carrier | CONFIRMED | `Shipping/ShippingMethodGenerator.php:32` `setExtends( BaseShippingMethod::class )`, `:35` `addConstant( 'CARRIER_ID', $carrierId )`; sample `Shipping/Generated/ShippingMethod_106.php` holds only `public const CARRIER_ID = '106';` |
| 23-25 | The module computes no price; the method asks the checkout module for the rates of the carriers of the zone | CONFIRMED | `Shipping/BaseShippingMethod.php:137-142` `$this->shippingRateFactory->createShippingRates(...)`, type `Packetery\Module\Checkout\ShippingRateFactory` (`:9`) |
| 26-27 | The module answers whether an order used a Packeta method | CONFIRMED | `Shipping/ShippingProvider.php:107-115` `wcOrderHasOurMethod( WC_Abstract_Order $wcOrder ): bool` |
| 28 | The namespace holds 148 files | CONFIRMED | `find src/Packetery/Module/Shipping -type f` returns 148; all 148 are `.php` |
| 28 | 1521 lines of logic | CONFIRMED | Non-blank non-comment lines, the metric that reproduces `ai-docs/ast/map.json` per file exactly: 122 + 136 + 38 + 73 = 369 for the four hand-written files, 144 x 8 = 1152 for `Generated/`, total 1521 |
| 28 | 18 public methods | CONFIRMED | `grep -cE '^\s*public( static)? function' Shipping/*.php`: 6 + 6 + 4 + 2 = 18; `Generated/` holds 0 |
| 28-29 | 144 of the files are generated carrier classes that the repository keeps under version control | CONFIRMED | `ls Shipping/Generated` = 144; `git ls-files src/Packetery/Module/Shipping/Generated` = 144; `git check-ignore` matches nothing |
| 31 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 35-36 | The module exposes the carrier shipping methods and three services of the plugin | CONFIRMED | `config/config.neon:152-154` registers `Shipping\ShippingProvider`, `Shipping\ShippingMethodGenerator`, `Shipping\ShippingMethodBulkGenerator`, and no other class of the namespace |
| 40 | `packeta_method_` is the identifier prefix of every generated carrier method | CONFIRMED | `Shipping/BaseShippingMethod.php:19` `public const PACKETA_METHOD_PREFIX = 'packeta_method_';` |
| 41 | `getShippingMethodId` builds the method identifier from the carrier identifier | CONFIRMED | `Shipping/BaseShippingMethod.php:204` `return self::PACKETA_METHOD_PREFIX . static::CARRIER_ID;` |
| 42 | `calculate_shipping` asks the checkout module for the rates and adds them to the package | CONFIRMED | `Shipping/BaseShippingMethod.php:137-145` `createShippingRates(...)` then `$this->add_rate( $customRate )` |
| 43 | `init` loads the title and the settings of one zone instance | CONFIRMED | `Shipping/BaseShippingMethod.php:105-109` sets `instance_form_fields` and `$this->title = $this->get_option( 'title' )` |
| 44 | `getSortedCachedMethods` gives WooCommerce the methods of the carriers the zone allows, sorted by title | CONFIRMED | `Shipping/ShippingProvider.php:226-241`; sort in `sortMethods()` `:206-219` `strcmp( $objectA->method_title, $objectB->method_title )` |
| 45 | `isPacketaMethod` answers whether a method identifier belongs to Packeta | CONFIRMED | `Shipping/ShippingProvider.php:120-125`, matches `ShippingMethod::PACKETERY_METHOD_ID` or the prefix |
| 46 | `wcOrderHasOurMethod` answers whether an order used a Packeta method | CONFIRMED | `Shipping/ShippingProvider.php:107-115` |
| 47 | `loadClasses` includes the generated classes and skips the car delivery carriers when the shop disables them | CONFIRMED | `Shipping/ShippingProvider.php:77-91`; `:83-88` `if ( $this->carDeliveryConfig->isDisabled() ) { ... continue; }` |
| 48 | `generateClass` writes the class of one carrier | CONFIRMED | `Shipping/ShippingMethodGenerator.php:22-46`, `file_put_contents( $filePath, (string) $file )` |
| 49 | `generateClasses` writes the class of every carrier that has none | CONFIRMED | `Shipping/ShippingMethodBulkGenerator.php:63-75`, `if ( ShippingMethodGenerator::classExists( $carrierId ) ) { continue; }` |
| 51-53 | The instance settings link to the carrier settings page and show the carrier modal as a template fragment | CONFIRMED | `Shipping/BaseShippingMethod.php:156-162` builds `carrierSettingsLink` from `Carrier\OptionsPage::SLUG`; `:188-191` renders `template/carrier/carrier-modal-fragment.latte` |
| 57-58 | The generator builds the file with a code generator, not with a template | CONFIRMED | `Shipping/ShippingMethodGenerator.php:8` `use Packetery\Nette\PhpGenerator\PhpFile;`, `:23` `new PhpFile()`; no Latte call in the class |
| 58-60 | The class name is the carrier identifier with a fixed prefix, the class extends the base method, its only member is the carrier identifier | CONFIRMED | `Shipping/ShippingMethodGenerator.php:53` `return 'ShippingMethod_' . $carrierId;`, `:32` `setExtends`, `:35` the single `addConstant` |
| 60-62 | The target directory is a subdirectory of the namespace | CONFIRMED | `Shipping/ShippingMethodGenerator.php:48-50` `getTargetDirectory(): string { return __DIR__ . '/Generated'; }` |
| 62-64 | The generator tests that the directory is writable and returns `false` when it is not | CONFIRMED | `Shipping/ShippingMethodGenerator.php:38-40` `if ( $this->wpAdapter->isWritable( $targetDirectory ) === false ) { return false; }`. Anchor imprecise: the anchored `classExists` (`:56-58`) only runs `return class_exists( self::TARGET_NAMESPACE . '\\' . self::getClassnameFromCarrierId( $carrierId ) );` and has no writability test |
| 66-68 | The bulk generator takes the internal pickup point carriers from the carrier configuration and the feed carriers from a fresh download | CONFIRMED | `Shipping/ShippingMethodBulkGenerator.php:34` `array_merge( $this->pickupPointConfig->getCompoundCarriers(), $this->pickupPointConfig->getVendorCarriers() )`; `:39` `$this->carrierDownloader->fetch_as_array( 'en' )` |
| 69 | A carrier that already has a class is skipped | CONFIRMED | `Shipping/ShippingMethodBulkGenerator.php:65-67` |
| 69-70 | When the download gives no answer, the run ends and writes nothing | CONFIRMED | `Shipping/ShippingMethodBulkGenerator.php:40-42` `if ( $feedCarriers === null ) { return; }`, before any write |
| 70 | The carrier update writes the class of one new carrier in the same way | CONFIRMED | `src/Packetery/Module/Carrier/Updater.php:202` `$this->shippingMethodGenerator->generateClass( (string) $carrierId, (string) $carrier['name'] )` |
| 72-73 | The plugin then includes every generated file at runtime | CONFIRMED | `Shipping/ShippingProvider.php:77-90` `scandir` + `require_once` over `Generated/`. Caveat, which line 47 of this page states: `loadClasses()` skips the car delivery files when `carDeliveryConfig->isDisabled()`; the CLI variant `loadAllClasses()` (`:65-72`) includes all of them |
| 73-75 | The method list of a zone keeps only the carriers of the countries of that zone and only the active carriers | CONFIRMED | `Shipping/ShippingProvider.php:140` `getCountryCodesForShippingZone( $zoneId )` then `getByCountryIncludingNonFeed()`; `:192` `if ( $carrierOptions->isActive() )`. `:136-143` falls back to all methods when there is no zone context or the zone has no country |
| 79-80 | The module depends on the carrier module for the carrier data and on the checkout module for the price | CONFIRMED | `Shipping/ShippingProvider.php:8-10` `Carrier\CarDeliveryConfig`, `Carrier\CarrierOptionsFactory`, `Carrier\EntityRepository`; `Shipping/BaseShippingMethod.php:9` `Checkout\ShippingRateFactory` |
| 82 | references → packetery-core | CONFIRMED | `Shipping/ShippingProvider.php:7` `use Packetery\Core\Entity\Carrier;` |
| 83 | references → packetery-module-carrier | CONFIRMED | `Carrier\EntityRepository`, `Carrier\CarDeliveryConfig`, `Carrier\CarrierOptionsFactory`, `Carrier\Downloader`, `Carrier\PacketaPickupPointsConfig`, `Carrier\OptionsPage` |
| 84 | references → packetery-module-checkout | CONFIRMED | `Shipping/BaseShippingMethod.php:9` `use Packetery\Module\Checkout\ShippingRateFactory;` |
| 85 | references → packetery-module-root | CONFIRMED | `CompatibilityBridge`, `ContextResolver`, `ShippingMethod`, `ShippingZoneRepository`, `ModuleHelper`, all in `src/Packetery/Module/` itself |
| 82-85 | The four structured lines are the reference set of the module | INCORRECT | The set omits `packetery-module-framework`. Three of the four hand-written files use it: `Shipping/BaseShippingMethod.php:11-12` `use Packetery\Module\Framework\WcAdapter; use Packetery\Module\Framework\WpAdapter;`, `Shipping/ShippingMethodGenerator.php:7`, `Shipping/ShippingMethodBulkGenerator.php:9`. The sibling pages name the same dependency: `ai-docs/reference/packetery-module-checkout.md:102` `references → packetery-module-framework`, likewise `packetery-module-carrier.md:90` and `packetery-module-order.md:77` |
| 87-89 | The method reads the carrier entity and the carrier options of packetery-module-carrier and uses them for the title and for the active state | CONFIRMED | title: `Shipping/BaseShippingMethod.php:74-81` `$this->carrierRepository->getAnyById(...)` then `$carrier->getName()`; active state: `Shipping/ShippingProvider.php:191-192` `createByCarrierId(...)->isActive()` |
| 89-92 | The method gives the checkout call the identifiers of the other Packeta methods of the zone | INCORRECT | The code passes carrier identifiers, not method identifiers, and it includes the calling method itself, not only the other ones: `Shipping/BaseShippingMethod.php:130-134` `foreach ( $shippingMethods as $shippingMethod ) { if ( $shippingMethod instanceof self ) { $allowedCarrierNames[ $shippingMethod::CARRIER_ID ] = $shippingMethod->options['title'] ?? $shippingMethod->get_title(); } }`. The array maps carrier id to title and `$this` is one of `$shippingMethods` |
| 92-94 | The base method takes its services from the service container, because WooCommerce builds a shipping method without a container | CONFIRMED | `Shipping/BaseShippingMethod.php:60-63` `CompatibilityBridge::getContainer()` then `getByType(...)`; the constructor signature is `__construct( int $instanceId = 0 )`, so WooCommerce injects nothing |
| 95-96 | The module reads the countries of a zone through the repository of packetery-module-root | CONFIRMED | `Shipping/ShippingProvider.php:140` `$this->shippingZoneRepository->getCountryCodesForShippingZone( $zoneId )`, type `Packetery\Module\ShippingZoneRepository` (`:13`). Anchor imprecise: the call is in `addMethods()`, not in the anchored `loadClasses()` |
| 96-97 | The module reads the car delivery identifiers of packetery-core | CONFIRMED | `Shipping/ShippingProvider.php:84` `implode( '|', Carrier::CAR_DELIVERY_CARRIERS )`, `Carrier` = `Packetery\Core\Entity\Carrier` (`:7`) |
| 101-103 | The generated classes live in the repository, so a new carrier of the feed changes the source tree of an installed plugin | CONFIRMED | `Shipping/ShippingMethodGenerator.php:49` writes into `__DIR__ . '/Generated'`, inside the plugin source tree; `git ls-files` tracks all 144 files there |
| 103-106 | An installation with a read only plugin directory gets no class for a new carrier, and the generator reports that state only as a return value and a message | CONFIRMED | `Shipping/ShippingMethodBulkGenerator.php:55-59` returns after `ModuleHelper::renderString( "Shipping method classes directory $targetDirectory is not writable.\n" )`; `:80-82` prints the not-generated list; `Shipping/ShippingMethodGenerator.php:38-40` returns `false`. No exception, no log call, no admin notice in the namespace |
| 108-109 | The loading of the generated classes reads the directory without a test of the result | CONFIRMED | `Shipping/ShippingProvider.php:79` `foreach ( scandir( $generatedClassesPath ) as $filename )`, the `false` return of `scandir` is not tested; same at `:67` |
| 109-111 | The base method sets the enabled state and the tax status as fixed values, and a comment says the enabled state can become a setting | CONFIRMED | `Shipping/BaseShippingMethod.php:83` `$this->enabled = 'yes'; // This can be added as a setting.`; `:90` `$this->tax_status = 'taxable';` |
| 111-113 | The sorted method list is cached for one request under a hash of the original list | CONFIRMED | `Shipping/ShippingProvider.php:233` `$cacheKey = crc32( serialize( $originalMethods ) );` into the static `$sortedMethodsCache` (`:20`), which lives for one PHP request |
| 114 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rcE 'TODO|FIXME' src/Packetery/Module/Shipping` returns 0 |

CONFIRMED 45 · INCORRECT 2 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-shipping.yaml

| entry | class | evidence |
|---|---|---|
| `service: woocommerce` | CONFIRMED | `ai-docs/ast/map.json` `"repo": "woocommerce"`; the document front matter carries the same value |
| `source_commit: ffca7e2e` | CONFIRMED | `git cat-file -t ffca7e2e` = commit, `ffca7e2e docs: add reference page and fragment for packetery-module-options`; `git diff ffca7e2e b1053fb3 -- src/Packetery/Module/Shipping` is empty |
| `aliases: [packetery-module-shipping]` | CONFIRMED | matches the `module:` field of the document front matter and the file name of the fragment |
| assumption "Generated on branch docs/teams at commit ffca7e2e." | CONFIRMED | `git branch --show-current` = `docs/teams`; `ffca7e2e` is an ancestor of the current HEAD `b1053fb3` |
| assumption about the PSR-4 boundary, `map.json` without `--split-psr4` at 11523b71, tests excluded | CONFIRMED | `ai-docs/ast/map.json` `"source-commit": "11523b7131c7c289991c414d1419747fc9b4e819"` and two whole-repo projects (`WooCommerce`, `deps-composer`), so no PSR-4 split; the namespace has no test directory and `tests/` holds no `Shipping` file, and 1521 is the count of `src/` alone |
| assumption "The counts hold the 144 generated carrier classes of the subdirectory Generated, which the repository keeps under version control." | CONFIRMED | 144 files in `Shipping/Generated`, all 144 returned by `git ls-files` |
| assumption "The STE skill esterka:writing-technical-docs-in-ste was invoked for the prose of this document." | UNCERTAIN | A skill invocation leaves no trace in this repository. Nothing here confirms or denies it |
| `modules[0] name/path` = packetery-module-shipping, `src/Packetery/Module/Shipping` | CONFIRMED | the directory exists and holds the namespace `Packetery\Module\Shipping` |
| `modules[0] lines: 1521` | CONFIRMED | same measure as line 28 of the document: 369 hand-written + 1152 generated |
| `modules[0] doc: reference/packetery-module-shipping.md` | CONFIRMED | `ai-docs/reference/packetery-module-shipping.md` exists and is the verified document |
| exposes `packeta_method_` (kind library, auth none, evidence `BaseShippingMethod.php#PACKETA_METHOD_PREFIX`) | CONFIRMED | `Shipping/BaseShippingMethod.php:19`; WooCommerce receives the methods through the `woocommerce_shipping_methods` filter |
| exposes `getSortedCachedMethods` (evidence `ShippingProvider.php#getSortedCachedMethods`) | CONFIRMED | `Shipping/ShippingProvider.php:226`, called from `src/Packetery/Module/Hooks/HookRegistrar.php:641` |
| exposes `wcOrderHasOurMethod` (evidence `ShippingProvider.php#wcOrderHasOurMethod`) | CONFIRMED | `Shipping/ShippingProvider.php:107`, called from `Order/Repository.php:313`, `Order/DetailCommonLogic.php:132`, `Hooks/UpdateOrderHook.php:76` |
| unknown "Where WordPress registers the method list and the class loading of this module." → `resolved_by: packetery-module-root` | INCORRECT | The registration is not in the root namespace. `src/Packetery/Module/Hooks/HookRegistrar.php:407` `$this->wpAdapter->addFilter( 'woocommerce_shipping_methods', [ $this, 'addShippingMethods' ] );` and `:416` `add_action( 'init', [ $this->shippingProvider, 'loadClasses' ] );`, namespace `Packetery\Module\Hooks`. The module id of that namespace is `packetery-module-hooks`, by the same convention the sibling pages use for `packetery-module-framework`. The question itself and `checked:` are correct: the namespace holds no `add_filter` and no `add_action` |
| unknown "How an installation with a read only plugin directory gets the class of a new carrier." → resolved_by a person | CONFIRMED | The namespace offers no fallback: `ShippingMethodGenerator::generateClass()` returns `false` and `ShippingMethodBulkGenerator::generateClasses()` returns after a message. Nothing ships a class by another route |

CONFIRMED 13 · INCORRECT 1 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0

Both tables: CONFIRMED 58 · INCORRECT 3 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0
