## ai-docs/reference/packetery-module-options.md @ 33f911c4 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 6 | `source-commit: 33f911c4` | CONFIRMED | `git cat-file -t 33f911c4` = commit, on branch `docs/teams` |
| 8 | `covers: [src/Packetery/Module/Options]` | CONFIRMED | Directory exists, holds 6 `.php` files, no subdirectory |
| 13 | Repo, module, type and status header | CONFIRMED | Matches the front matter and the `ai-docs/reference/` layout |
| 17-18 | Keeps the settings and gives them to every other module | INCORRECT | Completeness claim. `grep -rl OptionsProvider` returns nothing for `src/Packetery/Module/Shipping`, `Blocks`, `Product`, `ProductCategory`, `CustomsDeclaration`, and nothing under `src/Packetery/Core`. `packetery-module-shipping` and `packetery-core` are documented modules that do not read the provider |
| 18-19 | Renders the settings page with its tabs, validates the values and writes them to five WordPress options | CONFIRMED | `Page::create_form()` builds the `packetery` container; `Page::admin_init():744` `register_setting( 'packetery', 'packetery', [ $this, 'sanitizePacketeryOptions' ] )`; `Page::saveOptions():1227` `updateOption( $optionKey, $values )`; `OptionsProvider::getAllOptions()` returns exactly five option names |
| 20-21 | The module reads those options once and answers each question with a typed method | CONFIRMED | `OptionsProvider::__construct()` calls `getOption()` five times (`:81`, `:86`, `:91`, `:96`, `:101`) and caches them in `$data`, `$syncData`, `$autoSubmissionData`, `$advancedData`, `$currencyRatesData`; 59 public typed getters follow |
| 23-24 | Builds the settings export for the Packeta support | CONFIRMED | `Exporter::getExportContent():128` renders `template/options/export.latte` with settings, environment, carriers, zones, taxes, plugins |
| 25-26 | Removes the expired checkout transients of the plugin | CONFIRMED | `TransientPurger::purgeForSite():56` reads `getExpiredTransientsByPrefix( Transients::CHECKOUT_DATA_PREFIX )` and calls `deleteTransient()` |
| 26-27 | 6 files, 1593 lines of logic, 90 public methods | CONFIRMED | `ls *.php` = 6; non-blank non-comment lines = 1593 (182+20+437+868+54+32); `grep -c 'public function\|public static function'` = 59+3+22+2+4 = 90 |
| 27-28 | The settings provider is the largest part | INCORRECT | `Page.php` is larger on every file measure: 1240 physical lines and 868 lines of logic against 853 / 437 for `OptionsProvider.php`. The provider leads only on public methods (59 of 90) |
| 30 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder |
| 34 | Exposes one admin page, one settings provider and one export action | CONFIRMED | `Page::registerSubmenuPage()` is the only `addSubmenuPage()` call in the module; `OptionsProvider`; `Exporter::ACTION_EXPORT_SETTINGS` |
| 38 | Settings page `packeta-options`, admin submenu page with the tabs | CONFIRMED | `Page::SLUG = 'packeta-options'` (`Page.php:60`); `registerSubmenuPage():221` passes `render` as the callback; slug is a page id, not a secret |
| 39 | `admin_init` registers the option group and the validation of the main settings | CONFIRMED | `Page::admin_init():741-746` — `add_filter( 'pre_update_option_packetery', … )`, `register_setting()`, `add_settings_section( 'packetery_main', … )` |
| 40 | `processActions` validates the sender name against the Packeta API and saves the answer | CONFIRMED | `Page::processActions():939` calls `validateSender()`, which calls `packetaClient->senderGetReturnRouting()` (`:879`) and persists a `Log\Record` through `logger->add()` (`:895`) |
| 41 | `get_api_key`, `get_api_password`, `get_sender` give the account settings | CONFIRMED | `OptionsProvider.php:172`, `:186`, `:195` |
| 42 | `getCheckoutDetection`, `getCheckoutWidgetButtonLocation`, `isPickupPointValidationEnabled` | CONFIRMED | `OptionsProvider.php:287`, `:273`, `:819` |
| 43 | `getLabelFormats`, `get_packeta_label_format`, `getLabelMaxOffset` | CONFIRMED | `OptionsProvider.php:617`, `:213`, `:659` |
| 44 | `getDimensionsUnit`, `getSanitizedDimensionValueInMm`, `getDefaultWeight` | CONFIRMED | `OptionsProvider.php:305`, `:326`, `:353` |
| 45 | `isPacketAutoSubmissionEnabled`, `getPacketAutoSubmissionEventForPaymentGateway` | CONFIRMED | `OptionsProvider.php:603`, `:594` |
| 46 | `getStatusSyncingPacketStatuses`, `getValidAutoOrderStatusFromMapping` | CONFIRMED | `OptionsProvider.php:470`, `:763`, the second one reading `syncData['order_status_change_packet_statuses']` |
| 47 | `export-settings` sends the settings and the environment as a text file | CONFIRMED | `Exporter::ACTION_EXPORT_SETTINGS = 'export-settings'` (`:32`); `outputExportTxt():120-123` sets `Content-Type: text/plain` and a `.txt` attachment header |
| 49-50 | The page and its parent menu need the `manage_options` capability | CONFIRMED | `Page::registerMenuPage():214` and `registerSubmenuPage():226` both pass `'manage_options'` |
| 50-53 | Six tabs: general, advanced, packet status sync, auto submission, currency rates, support | CONFIRMED | `Page.php:62-67` — `TAB_GENERAL`, `TAB_ADVANCED`, `TAB_SUPPORT`, `TAB_PACKET_STATUS_SYNC`, `TAB_AUTO_SUBMISSION`, `TAB_CURRENCY_RATES`; `render():1019-1037` dispatches on all six |
| 53-54 | The carrier settings live in `packetery-module-carrier` and not on this page | CONFIRMED | `src/Packetery/Module/Carrier/OptionsPage.php` registers its own page; no carrier option form in `Module/Options` |
| 58 | The module owns no database table | CONFIRMED | No `CREATE TABLE` and no `createOrAlterTable` in `src/Packetery/Module/Options` |
| 58-60 | Five WordPress options, and the names of every option are constants | CONFIRMED | `OptionNames.php` holds every plugin option name; the only string literals passed to `getOption()` in the module are WooCommerce option names in `Exporter.php:139-155` |
| 64 | `packetery` — main settings: account, checkout, labels, weight and size | CONFIRMED | `OptionNames::PACKETERY`; `Page::FORM_FIELDS_CONTAINER = 'packetery'` (`:50`); `create_form()` adds the account, checkout, label and dimension controls |
| 65 | `packetery_sync` — packet status synchronisation and the order status mapping | CONFIRMED | `OptionsProvider::getMaxStatusSyncingPackets():415` and `:780` read `syncData['max_status_syncing_packets']` and `syncData['order_status_change_packet_statuses']` |
| 66 | `packetery_auto_submission` — automatic submission per payment gateway | CONFIRMED | `OptionsProvider::isPacketAutoSubmissionEnabled():604` reads `autoSubmissionData['allow']`; `getPacketAutoSubmissionEventForPaymentGateway():594` reads the per-gateway entry |
| 67 | `packetery_advanced` — carrier configuration mode of the shop | CONFIRMED | `OptionsProvider::isWcCarrierConfigEnabled():739` reads `advancedData['new_carrier_settings_enabled']` |
| 68 | `packetery_currency_rates` — custom currency rates | CONFIRMED | `OptionsProvider::getCustomCurrencyRates():838` reads `currencyRatesData['rates']` |
| 69 | `api_password`, `api_key`, `sender` are keys of `packetery`; the module derives the key from the password | CONFIRMED | `Page::sanitizePacketeryOptions():818` — `$options['api_key'] = substr( $apiPass, 0, 16 );`, else `''` at `:821`. Key names only, no stored value quoted |
| 70 | `packeta_label_format`, `carrier_label_format`, `label_note` are keys of `packetery` | CONFIRMED | `OptionsProvider.php:214`, `:205`, `:218` |
| 71 | `default_weight`, `packaging_weight`, `dimensions_unit`, `default_length/width/height` | CONFIRMED | `OptionsProvider.php:302`, `:306`, `:353`, `:376-380`; `Page::sanitizePacketeryOptions():824-835` |
| 72 | State options `packetery_version`, `packetery_activated`, `packetery_last_carrier_update`, `packetery_last_settings_export` | CONFIRMED | `OptionNames.php:8`, `:16`, `:10`, `:9` |
| 73 | Feature flag options use a different prefix | CONFIRMED | `OptionNames.php:19-21` — `packeta_feature_flags`, `packeta_feature_flags_error_counter`, `packeta_feature_flags_disabled_due_errors`, against the `packetery_` prefix of the rest |
| 75-77 | The module reads the option table directly for two tasks; it finds the expired transients of one prefix | CONFIRMED | `Repository::getExpiredTransientsByPrefix():44` and `getAllOptionNamesByPrefixes():102` are the only two `wpdbAdapter->get_col()` calls in the module |
| 77-78 | It lists every option name of a set of prefixes | CONFIRMED | `Repository::getAllOptionNamesByPrefixes():90-102` builds a `LIKE` disjunction over the prefixes |
| 78-80 | The purge deletes the expired checkout transients of every site of a multisite installation | CONFIRMED | `TransientPurger::purge():43-52` loops `Plugin::getSites()` with `switchToBlog()` / `restoreCurrentBlog()` and calls `purgeForSite()` |
| 84-85 | Depends on the Packeta API client and on the modules that own the values it shows | CONFIRMED | `Page.php` constructor takes `Api\Soap\Client`, `PacketSynchronizer`, `PacketAutoSubmitter`, `CurrencyConversion`, `Upgrade`, `Log\ILogger` |
| 87 | references → packetery-core | CONFIRMED | `use Packetery\Core\Api;`, `Packetery\Core\Log`, `Packetery\Core\CoreHelper`, `Packetery\Core\Entity\PacketStatus` |
| 88 | references → packetery-module-carrier | CONFIRMED | `use Packetery\Module\Carrier;` (Page), `Carrier\CarrierUpdater`, `Carrier\CountryListingPage` (Exporter) |
| 89 | references → packetery-module-order | CONFIRMED | `use Packetery\Module\Order\PacketSynchronizer;`, `Order\PacketAutoSubmitter`, `Order\CurrencyConversion` |
| 90 | references → packetery-module-dashboard | CONFIRMED | `use Packetery\Module\Dashboard\DashboardPage;`, used as the parent slug at `Page.php:215` and `:223` |
| 91 | references → packetery-module-log | CONFIRMED | `use Packetery\Module\Log\DbLogger;` (Exporter), `Log\ArgumentTypeErrorLogger` (Page) |
| 92 | calls → packeta-api (sync, SOAP) | CONFIRMED | `Page::validateSender():879` calls `packetaClient->senderGetReturnRouting()` on `Packetery\Core\Api\Soap\Client`, a blocking call in the request |
| 94-95 | The page validates the sender with the SOAP client of `packetery-core` and writes the answer to the plugin log | CONFIRMED | `Page::validateSender():878-895` — `SenderGetReturnRouting`, then `logger->add( $senderValidationLog )` with `STATUS_SUCCESS` or `STATUS_ERROR` |
| 96-97 | The page builds the choice list of the packet statuses from `packetery-module-order` | CONFIRMED | `Page::getAllPacketStatusesChoiceData():315` calls `packetSynchronizer->getPacketStatuses()` on `Packetery\Module\Order\PacketSynchronizer` |
| 96-97 | The page builds the choice list of the order statuses from `packetery-module-order` | INCORRECT | `Page::getOrderStatusesChoiceData():345` — `$orderStatuses = wc_get_order_statuses();`. The order statuses come from WooCommerce itself, not from `packetery-module-order` |
| 98-99 | The maximum cart value combines the plugin setting with the carrier setting | CONFIRMED | `OptionsProvider::getEffectiveMaxCartValueLimit():260-265` — `$carrierOptions->getMaxCartValue() ?? $this->getMaxCartValue()` when `isWcCarrierConfigEnabled()` |
| 100-101 | The export reads the carrier list, the shipping zones and the last five days of the plugin log | CONFIRMED | `Exporter::getExportContent():174` `getCarriersForOptionsExport()`, `:175` `\WC_Shipping_Zones::get_zones()`, `:177` `dbLogger->getForPeriodAsArray( [ [ 'after' => '5 days ago' ] ] )` |
| 101-103 | The menu of the plugin belongs to `packetery-module-dashboard` and this page is one of its children | CONFIRMED | `Page::registerSubmenuPage():223` passes `DashboardPage::SLUG` (`'packeta-home'`) as the parent. The top-level entry is added here with an empty render closure (`:216`), the dashboard module owns the slug and its own submenu (`DashboardPage.php:64-69`) |
| 107-108 | The export action runs from two query parameters | CONFIRMED | `Exporter::outputExportTxt():112-117` returns early unless `getQuery( 'page' ) === Page::SLUG` and `getQuery( 'action' ) === self::ACTION_EXPORT_SETTINGS` |
| 108-109 | The export class contains no capability check and no nonce check | CONFIRMED | `grep -n "current_user_can\|nonce\|checkAdminReferer" Exporter.php` returns nothing. The hook is `HookRegistrar.php:587` `addAction( 'admin_init', [ $this->exporter, 'outputExportTxt' ] )`, and `HookRegistrar.php` holds no `current_user_can` either |
| 109-111 | The export masks the API password, removes the API key, and every other setting goes into the file as it is | CONFIRMED | `Exporter::getExportContent():130-138` — masking is `sprintf( '%s...%s (%s)', substr( …, 0, 16 ), substr( …, -2, 2 ), strlen( … ) )` and `unset( $globalSettings[ OptionNames::PACKETERY ]['api_key'] )`; no other key is touched before `formatVariable()` |
| 111-113 | An object of an unknown class is printed as its type alone | INCORRECT | `Exporter::formatVariable():278-279` — `$output .= gettype( $variable ) . ' ' . get_class( $variable ) . PHP_EOL;`. The export prints the type **and** the class name; only the second half of the sentence (no content of the value) matches the code |
| 115-116 | The settings page needs the SOAP extension for the sender check | CONFIRMED | `Page::render():1038` `if ( ! extension_loaded( 'soap' ) )`; the sender check itself runs through `Api\Soap\Client` |
| 116-117 | Without the extension the page shows an error and the other settings still save | CONFIRMED | `Page::render():1039` sets `$latteParams['error']` only; neither `admin_init()`, `sanitizePacketeryOptions()` nor `saveOptions()` guards on the extension |
| 117-119 | Two settings remain in the provider only for the upgrade path, and their comments say so | CONFIRMED | `OptionsProvider.php:711` and `:784` — both doc blocks end with "Used in upgrade only."; the consumer is `Module/Upgrade.php:194` |
| 119-121 | The label catalogue is a hardcoded list of formats | CONFIRMED | `OptionsProvider::getLabelFormats():617-650` returns a literal array of six formats |
| 121-123 | The logo of the plugin menu is a hardcoded constant of the page class | CONFIRMED | `Page.php:71` `const PACKETA_SVG_LOGO` holds an inline `data:image/svg+xml;base64,…` string, passed to `addMenuPage()` at `:217`. The document names the constant and does not quote its value |
| 125-126 | The purge has no schedule in this module; the registration is outside the namespace | CONFIRMED | No `wp_schedule_event` or cron hook in `src/Packetery/Module/Options`; `Module/CronService.php:83` `add_action( self::CRON_PURGE_TRANSIENTS, [ $this->transientPurger, 'purge' ] )` |

CONFIRMED 58 · INCORRECT 4 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-options.yaml

| entry | class | evidence |
|---|---|---|
| `service: woocommerce` | CONFIRMED | Matches `repo: woocommerce` in the front matter and the other fragments in `ai-docs/` |
| `source_commit: 33f911c4` | CONFIRMED | `git cat-file -t 33f911c4` = commit, reachable from `docs/teams` |
| `aliases: [packetery-module-options]` | CONFIRMED | Same id as `modules[0].name`, the document file name and the `module:` front matter |
| assumption 1 — branch `docs/teams`, commit 33f911c4 | CONFIRMED | `git rev-parse --abbrev-ref HEAD` = `docs/teams`; the commit exists |
| assumption 2 — PSR-4 boundary, `ast/map.json` without `--split-psr4` at 11523b71, tests excluded | CONFIRMED | `ai-docs/ast/map.json` header: `"source-commit": "11523b7131c7c289991c414d1419747fc9b4e819"`, entries are per file with no per-namespace split; `find src/Packetery/Module/Options -type d` returns the directory itself only, so no test directory is in scope |
| assumption 3 — the STE skill was invoked for the prose | UNCERTAIN | No evidence in the repository; not checkable from the code |
| `modules[0]` — name, path `src/Packetery/Module/Options`, lines 1593, doc `reference/packetery-module-options.md` | CONFIRMED | Directory exists with 6 `.php` files; non-blank non-comment lines = 1593; the document file exists |
| `exposes[0]` — html `packeta-options`, auth `wordpress-capability`, evidence `Page#registerSubmenuPage`, notes `manage_options` | CONFIRMED | `Page::SLUG` (`Page.php:60`) and the `'manage_options'` argument at `:226` |
| `exposes[1]` — html `export-settings`, auth `unknown`, evidence `Exporter#ACTION_EXPORT_SETTINGS`, notes "checks no capability and no nonce" | CONFIRMED | `Exporter.php:32` defines the action; `outputExportTxt():112-125` gates on two query parameters only; no `current_user_can` or nonce call in `Exporter.php`, and none around `HookRegistrar.php:587` |
| `exposes[2]` — library `getAllOptions`, auth none, notes "All other modules read the settings through this provider" | INCORRECT | The notes overstate. `OptionsProvider` is not referenced anywhere in `src/Packetery/Module/Shipping`, `Blocks`, `Product`, `ProductCategory`, `CustomsDeclaration`, nor in `src/Packetery/Core`; `packetery-module-shipping` and `packetery-core` are documented modules with their own fragments. The method itself (`OptionsProvider.php:135`) is public and has no capability check, so `auth: none` holds |
| `calls[0]` — service `packeta-api`, kind other, sender validation over SOAP, evidence `Page#processActions` | CONFIRMED | `Page::processActions():939` → `validateSender():879` → `packetaClient->senderGetReturnRouting()` on `Packetery\Core\Api\Soap\Client` |
| `owns_data[0]` — entity settings, store mysql, evidence `Page#saveOptions`, notes five options plus state options | CONFIRMED | `Page::saveOptions():1226-1227` calls `updateOption()`; `OptionsProvider::getAllOptions()` names the five; `OptionNames.php` adds the state and feature flag names |
| `unknowns[0]` — where WordPress schedules the purge of the expired transients | CONFIRMED | Honest open question for this namespace: no scheduling code under `Module/Options`; the answer is `Module/CronService.php:83` |
| `unknowns[1]` — whether a capability check protects the export outside this namespace | CONFIRMED | Honest open question: the hook at `HookRegistrar.php:587` adds no check, and `HookRegistrar.php` holds no `current_user_can` at all, so the protection, if any, would come from the WordPress admin context and not from this code |
| `unknowns[2]` — which code migrates the two legacy settings | CONFIRMED | `Module/Upgrade.php:194` calls `optionsProvider->getAutoOrderStatus()`, which is outside the namespace, as the fragment says |

CONFIRMED 13 · INCORRECT 1 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
