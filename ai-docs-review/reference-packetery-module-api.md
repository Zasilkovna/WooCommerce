## ai-docs/reference/packetery-module-api.md @ 2c742814 — 2026-09-22

| line | claim | class | evidence |
|---|---|---|---|
| 6 | `source-commit: 2c742814` | CONFIRMED | `git cat-file -t 2c742814` = commit ("docs: fix review findings in packetery-module-carrier"), ancestor of HEAD `b1053fb3` on branch `docs/teams`; `git diff --stat 2c742814 HEAD -- src/Packetery/Module/Api` is empty, so the working tree matches the documented commit |
| 8 | `covers: [src/Packetery/Module/Api]` | CONFIRMED | the six files of the namespace `Packetery\Module\Api` live there: `BaseRouter.php`, `Registrar.php`, `Internal/CheckoutController.php`, `Internal/CheckoutRouter.php`, `Internal/OrderController.php`, `Internal/OrderRouter.php` |
| 17 | The module gives the browser six internal REST routes | CONFIRMED | `CheckoutController::registerRoutes` registers 4 routes (CheckoutController.php:49-88), `OrderController::registerRoutes` registers 2 (OrderController.php:150-174); no `register_rest_route` call exists outside `src/Packetery/Module/Api` |
| 17-19 | The checkout routes keep the widget selection because the WooCommerce session is not available at that moment | CONFIRMED | CheckoutController.php:20 class docblock: "Input is validated in Checkout. We use transient because WC session is not yet loaded."; `CheckoutController::save` writes through `CheckoutStorage::setTransient` (:184) |
| 20-21 | The order routes save what the administrator writes in the modal windows of the order detail | CONFIRMED | `OrderController::saveModal` reads the modal form fields and saves the order (OrderController.php:183-277); `saveStoredUntil` uses `Order\StoredUntilModal::MODAL_ID` (:292) |
| 23-25 | Every route lives in one REST namespace of Packeta, and no route is part of the public API of the shop | CONFIRMED | `CheckoutRouter::$namespace = 'packeta/internal'` (CheckoutRouter.php:31) and `OrderRouter::$namespace = 'packeta/internal'` (OrderRouter.php:26); `BaseRouter::registerRoute` passes that namespace to `register_rest_route` (BaseRouter.php:57) |
| 26-27 | The routers also build the URL of each route for the scripts of the plugin | CONFIRMED | `BaseRouter::getRouteUrl` calls `get_rest_url` (BaseRouter.php:36-38); `CheckoutRouter` exposes four URL getters (:44-72), `OrderRouter` two (:40-54) |
| 28 | The module holds 6 files | CONFIRMED | `find src/Packetery/Module/Api -type f` returns 6 PHP files |
| 28 | 391 lines of logic | CONFIRMED | non-blank, non-comment lines of the six files = 391 (`cat src/Packetery/Module/Api/*.php src/Packetery/Module/Api/Internal/*.php \| grep -v '^\s*$' \| grep -v '^\s*\*' \| grep -v '^\s*/\*' \| grep -v '^\s*//' \| wc -l`); physical total is 893 |
| 28 | 21 public methods | CONFIRMED | `grep -rc "public function"`: Registrar 2, BaseRouter 3, CheckoutRouter 4, OrderRouter 2, CheckoutController 6, OrderController 4 = 21 (`CheckoutController::save` is private, :170) |
| 30 | `> ⚠ add business context (elicitation)` | PENDING | standard placeholder |
| 34 | Six REST routes in the namespace `packeta/internal` | CONFIRMED | 4 + 2 `registerRoute` calls, both routers carry `$namespace = 'packeta/internal'` |
| 34-35 | Every route accepts the editable methods of WordPress | CONFIRMED | `'methods' => WP_REST_Server::EDITABLE` in all six registrations (CheckoutController.php:53, :63, :73, :83; OrderController.php:154, :167) |
| 35 | which are `POST`, `PUT` and `PATCH` | UNCERTAIN | true of WordPress core, but `WP_REST_Server` is not defined anywhere in this repository (no stub, no vendored WordPress): `grep -rln "class WP_REST_Server\|const EDITABLE" --include='*.php' .` returns nothing |
| 39 | Route registration — `registerRoutes` registers the routes of the two controllers at one place | CONFIRMED | `Registrar::registerRoutes` calls `$this->orderController->registerRoutes(); $this->checkoutController->registerRoutes();` (Registrar.php:52-55) |
| 40 | `/checkout/save-selected-pickup-point` → `saveSelectedPickupPoint` | CONFIRMED | `CheckoutRouter::PATH_SAVE_SELECTED_PICKUP_POINT = '/save-selected-pickup-point'` with `$restBase = 'checkout'`; `BaseRouter::getRoute` builds `"/{$this->restBase}{$path}"` (BaseRouter.php:47); callback `saveSelectedPickupPoint` saves `Attribute::$pickupPointAttributes` (CheckoutController.php:98) |
| 41 | `/checkout/save-validated-address` → `saveValidatedAddress` | CONFIRMED | `CheckoutRouter::PATH_SAVE_VALIDATED_ADDRESS`; callback stores `Attribute::$homeDeliveryAttributes` (CheckoutController.php:110) |
| 42 | `/checkout/save-car-delivery-details` → `saveCarDeliveryDetails` | CONFIRMED | `CheckoutRouter::PATH_SAVE_DELIVERY_ADDRESS = '/save-car-delivery-details'` (CheckoutRouter.php:23); callback stores `Attribute::$carDeliveryAttributes` (CheckoutController.php:122) |
| 43 | `/checkout/remove-saved-data` removes the data of one carrier, or all stored checkout data | CONFIRMED | `removeSavedData`: without `carrierId` it calls `deleteTransient()` (CheckoutController.php:137-138), otherwise `unset( $savedData[ $params['carrierId'] ] )` and re-saves (:150-155) |
| 44 | `/order/save-modal` validates and saves weight, size, value and delivery date | CONFIRMED | `OrderRouter::PATH_SAVE_MODAL` with `$restBase = 'order'`; `saveModal` sets weight, size, COD, value and deliver-on and validates the form (OrderController.php:189-243) |
| 45 | `/order/save-stored-until` sends a longer storage time to Packeta and saves it on the order | CONFIRMED | `saveStoredUntil` calls `PacketSetStoredUntil::setStoredUntil` (OrderController.php:312), then `setStoredUntil` on the order and `orderRepository->save` (:318-320) |
| 46 | Route URL — `getSaveModalUrl`, `getSaveStoredUntilUrl` build the URL that the admin scripts call | CONFIRMED | OrderRouter.php:45-54, both delegate to `BaseRouter::getRouteUrl` |
| 47 | `getRoute` builds the path of a route from the base of the router | CONFIRMED | `return "/{$this->restBase}{$path}";` (BaseRouter.php:47) |
| 49-50 | The two order routes need the `edit_posts` capability | CONFIRMED | `'permission_callback' => function () { return current_user_can( 'edit_posts' ); }` on both registrations (OrderController.php:156-158, :169-171) |
| 49-50 | and they check no nonce | CONFIRMED | `grep -rni "nonce" src/Packetery/Module/Api` returns 0 hits; neither callback nor the two route callbacks inspect `_wpnonce` or call `wp_verify_nonce` |
| 50-52 | The four checkout routes have an open permission callback, so any visitor can call them | CONFIRMED | `'permission_callback' => '__return_true'` four times (CheckoutController.php:55, :65, :75, :85); docblock :44 "Endpoints are public." |
| 56 | The module owns no storage of its own | CONFIRMED | no `$wpdb`, `get_option` or transient call in the six files; writes go through `CheckoutStorage` (CheckoutController.php:12, :38) and `Order\Repository` (OrderController.php:25, :248) |
| 56-57 | The checkout routes write to the transient of the checkout module, the order routes write to the order entity | CONFIRMED | `CheckoutStorage::setTransient`/`deleteTransient` (CheckoutStorage.php:55, :63) called from CheckoutController.php:138, :144, :154, :184; `OrderController` mutates the `Packetery\Core\Entity\Order` and saves it (:227-248) |
| 61 | `packetery_rate_id` is the request key for the selection of one carrier | CONFIRMED | `private const RATE_ID = 'packetery_rate_id';` (CheckoutController.php:26); `$rateId = $parameters[ self::RATE_ID ];` and `$savedData[ $rateId ][ … ]` (:177-180) — the constant names the request parameter, the stored array key is its value |
| 62 | The route takes the attribute list of the order module and stores the values it knows | CONFIRMED | `CheckoutController::save` iterates `$carrierAttrs` from `Packetery\Module\Order\Attribute` (:13, :98, :110, :122) and writes `$parameters[ $attribute['name'] ]` (:178-180); the static lists exist at Attribute.php:48, :92, :132 |
| 63 | order — `packeteryWeight`, `packeteryOriginalWeight` | CONFIRMED | OrderController.php:192-193 |
| 64 | order — `packeteryLength`, `packeteryWidth`, `packeteryHeight`, converted to millimetres | CONFIRMED | OrderController.php:194-196; `$this->optionsProvider->getSanitizedDimensionValueInMm(...)` for all three (:222-224) |
| 65 | order — `packeteryCOD`, `packeteryValue`, `hasPacketeryAdultContent`, `packeteryDeliverOn` as manual values | CONFIRMED | OrderController.php:197-202; `setManualCod` (:232), `setManualValue` (:238), `setAdultContent` (:236), `setDeliverOn` (:243) |
| 66 | order — `packeteryStoredUntil` | CONFIRMED | `$storedUntil = $parameters['packeteryStoredUntil'] ?? null;` (OrderController.php:290) |
| 68-69 | The order routes answer with the new values and with the state of the order | CONFIRMED | `saveModal` returns the field values plus `orderIsSubmittable`, `orderWarningFields`, `invalidFieldsMessage` (:254-275); `saveStoredUntil` returns `packeteryStoredUntil` plus the same three state keys (:329-334) |
| 68-69 | …and with the HTML fragment of the grid cell, so the admin page needs no reload | INCORRECT | only `saveModal` returns a fragment: `'fragments' => [ … => $this->gridExtender->getWeightCellContent( $order ) ]` (OrderController.php:255-257). `saveStoredUntil` returns `$data['data'] = [ 'packeteryStoredUntil' => …, 'orderIsSubmittable' => …, 'orderWarningFields' => …, 'invalidFieldsMessage' => … ];` (:329-334) — no `fragments` key, so the claim does not hold for both order routes |
| 70-73 | A validation error, an order that does not load, a failed save and a fault of the Packeta API each give status 400 with its own error code | CONFIRMED | `WP_Error( 'form_invalid', …, 400 )` (:207, :300), `'order_not_loaded'` (:213, :216, :306, :309), `'order_not_updated'` (:250, :322), `'packetery_fault'` (:315) |
| 77 | "packetery-module-api depends on the modules that own the data it writes" + the `references →` block as the module's dependency set | INCORRECT | the block omits references the code imports: `use Packetery\Module\Framework\WpAdapter;` (OrderController.php:18, used at :250 and :322) and `use Packetery\Module\Exception\InvalidCarrierException;` (:15, caught at :212 and :305). Sibling pages list the first one under the same module id — `references → packetery-module-framework` in ai-docs/reference/packetery-module-checkout.md:102, packetery-module-order.md:77 and packetery-module-carrier.md:90 — so the set drifts from the other pages |
| 79 | references → packetery-core | CONFIRMED | `use Packetery\Core\CoreHelper;`, `Core\Entity\Size`, `Core\Validator` (OrderController.php:12-14) |
| 80 | references → packetery-module-order | CONFIRMED | `Packetery\Module\Order\Attribute` (CheckoutController.php:13); `Order\Form`, `Order\GridExtender`, `Order\OrderValidatorFactory`, `Order\PacketSetStoredUntil`, `Order\Repository` (OrderController.php:20-25) |
| 81 | references → packetery-module-checkout | CONFIRMED | `use Packetery\Module\Checkout\CheckoutStorage;` (CheckoutController.php:12) |
| 82 | references → packetery-module-options | CONFIRMED | `use Packetery\Module\Options\OptionsProvider;` (OrderController.php:19) |
| 83 | references → packetery-module-forms | CONFIRMED | `use Packetery\Module\Forms\FormData\OrderFormData;` and `use Packetery\Module\Forms\StoredUntilFormFactory;` (OrderController.php:16-17) |
| 85-87 | The checkout routes take the attribute keys of `packetery-module-order` and write through the storage of `packetery-module-checkout` | CONFIRMED | `CheckoutController::save` reads `Attribute::$…Attributes` and calls `$this->checkoutStorage->setTransient` (:178-185) |
| 87-88 | The order routes validate with the forms of `packetery-module-forms` | CONFIRMED | `StoredUntilFormFactory::createForm` + `isValid` (OrderController.php:292-299), `$form->getValues( OrderFormData::class )` (:219); note the modal form object itself comes from `Packetery\Module\Order\Form` (:21, :189) |
| 88-89 | …and with the order validator of `packetery-core` | CONFIRMED | `Packetery\Core\Validator` (:14), `$this->orderValidator->validate( $order )` (:245, :325), `isValid` (:269, :331) |
| 89 | …and they save through the order repository | CONFIRMED | `$this->orderRepository->save( $order )` (:248, :320) |
| 90-92 | The size of the packet goes through the unit conversion of `packetery-module-options` | CONFIRMED | `OptionsProvider::getSanitizedDimensionValueInMm` three times (OrderController.php:222-224) |
| 92-94 | The storage date leaves the shop through the packet operation of `packetery-module-order`, which calls Packeta | CONFIRMED | `Packetery\Module\Order\PacketSetStoredUntil::setStoredUntil` (OrderController.php:312) calls `$this->soapApiClient->packetSetStoredUntil( $request )` (src/Packetery/Module/Order/PacketSetStoredUntil.php:73) |
| 98-100 | The four checkout routes accept a request from any visitor, because their permission callback always returns true | CONFIRMED | `'permission_callback' => '__return_true'` (CheckoutController.php:55, :65, :75, :85) |
| 100-103 | A request with an invalid data structure is not stored, and the route still answers with status 200 | CONFIRMED | `if ( $this->checkoutStorage->validateDataStructure( $savedData ) ) { … }` with no else branch (CheckoutController.php:182-185); the three callers return `new WP_REST_Response( [], 200 )` unconditionally (:100, :112, :124) |
| 105-106 | The two order routes check the `edit_posts` capability and no nonce | CONFIRMED | `current_user_can( 'edit_posts' )` (OrderController.php:157, :170); 0 nonce hits in the module |
| 106-108 | The key of the stored checkout data is a hardcoded request parameter | CONFIRMED | `private const RATE_ID = 'packetery_rate_id';` (CheckoutController.php:26), read without a fallback at :177 |
| 108-109 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rn "TODO\|FIXME" src/Packetery/Module/Api` returns 0 hits |

CONFIRMED 50 · INCORRECT 2 · UNCERTAIN 1 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-api.yaml

| entry | class | evidence |
|---|---|---|
| `service: woocommerce` | CONFIRMED | repository root is the WooCommerce plugin; `ai-docs/ast/map.json` carries `"repo": "woocommerce"` |
| `source_commit: 2c742814` | CONFIRMED | commit exists and is an ancestor of HEAD `b1053fb3`; `src/Packetery/Module/Api` is unchanged between the two |
| `aliases: [packetery-module-api]` | CONFIRMED | matches the PSR-4 namespace `Packetery\Module\Api` and the path `src/Packetery/Module/Api` |
| assumption "Generated on branch docs/teams at commit 2c742814." | CONFIRMED | `git branch --show-current` = `docs/teams`; commit is an ancestor of HEAD |
| assumption — committed `ai-docs/ast/map.json` produced without `--split-psr4` and at commit 11523b71 | CONFIRMED | map.json header: `"generated-by": "tool:ast-map@0.3.1"`, `"source-commit": "11523b7131c7c289991c414d1419747fc9b4e819"`; entries are per file with a `namespace` field, and the string `packetery-module-api` does not occur in the file |
| assumption — per-module counts come from a `--split-psr4` run of the same tool | UNCERTAIN | no artifact of that run is committed; the resulting count is consistent with the code (391 non-blank, non-comment lines), but the run itself cannot be checked in this repository |
| assumption "Test directories are excluded from the counts." | CONFIRMED | `tests/Integration/Module/Api` exists but is not part of the 391; the count matches `src/Packetery/Module/Api` alone |
| assumption — the STE skill was invoked for the prose | UNCERTAIN | process claim; no artifact in the repository records the invocation |
| `modules[0]`: name `packetery-module-api`, path `src/Packetery/Module/Api`, lines 391, doc `reference/packetery-module-api.md` | CONFIRMED | path exists with 6 files; 391 = non-blank, non-comment lines; `ai-docs/reference/packetery-module-api.md` exists |
| `exposes` /checkout/save-selected-pickup-point — auth none, evidence `CheckoutController#saveSelectedPickupPoint` | CONFIRMED | `PATH_SAVE_SELECTED_PICKUP_POINT` + `$restBase='checkout'`; `'permission_callback' => '__return_true'` (CheckoutController.php:55); callback at :97 |
| `exposes` /checkout/save-validated-address — auth none | CONFIRMED | CheckoutController.php:59-68 (`__return_true` at :65), callback at :109 |
| `exposes` /checkout/save-car-delivery-details — auth none | CONFIRMED | `PATH_SAVE_DELIVERY_ADDRESS = '/save-car-delivery-details'` (CheckoutRouter.php:23); `__return_true` at :85; callback at :121 |
| `exposes` /checkout/remove-saved-data — auth none, "removes the data of one carrier or of the whole session" | CONFIRMED | `__return_true` at :75; `removeSavedData` deletes the whole transient without `carrierId` (:137-138) or unsets one carrier (:150) |
| `exposes` /order/save-modal — auth wordpress-capability, "needs edit_posts and checks no nonce" | CONFIRMED | `current_user_can( 'edit_posts' )` (OrderController.php:157); 0 nonce hits in the module; callback at :183 |
| `exposes` /order/save-stored-until — auth wordpress-capability, "sends a new storage date to Packeta and saves it" | CONFIRMED | `current_user_can( 'edit_posts' )` (:170); `PacketSetStoredUntil::setStoredUntil` (:312) reaching `soapApiClient->packetSetStoredUntil` |
| `unknowns[0]` — where WordPress calls the registration; resolved_by `packetery-module-hooks` | CONFIRMED | no call site inside the namespace; the hook lives in `Packetery\Module\Hooks`: `$this->wpAdapter->addAction( 'rest_api_init', [ $this->apiRegistrar, 'registerRoutes' ] );` (src/Packetery/Module/Hooks/HookRegistrar.php:385) |
| `unknowns[1]` — whether the open permission callback is a decision or a defect; "the permission callbacks of both controllers, which differ" | CONFIRMED | `'__return_true'` in CheckoutController (:55, :65, :75, :85) against `current_user_can( 'edit_posts' )` in OrderController (:157, :170); the intent is not recorded in the code |

CONFIRMED 15 · INCORRECT 0 · UNCERTAIN 2 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
