## ai-docs/reference/packetery-module-hooks.md @ e4971ab0 — 2026-09-22

Repository root: `/Users/pf/projects/workshop/WooCommerce`, branch `docs/teams`, HEAD `a6ef312a`.
Module numbers measured on `src/` only, tests excluded:
`find src/Packetery/Module/Hooks -name '*.php' | wc -l` = 3 ·
non-empty non-comment lines of those files = 562 ·
`grep -cE '^\s*(final )?public( static)? function'` = 16.

| line | claim | class | evidence |
|---|---|---|---|
| 17 | packetery-module-hooks connects **every part** of the plugin to WordPress and WooCommerce | INCORRECT | 25 files outside `src/Packetery/Module/Hooks` register WordPress hooks themselves. `src/Packetery/Module/Log/ArgumentTypeErrorLogger.php:51` calls `$this->wpAdapter->addAction( 'woocommerce_init', … )`, and `HookRegistrar.php` never names that class (`grep -c ArgumentTypeErrorLogger` = 0). |
| 18–19 | One class holds the map and decides what to register from the context of the request [VERIFY: HookRegistrar.php:372] | CONFIRMED | `HookRegistrar.php:372` is `public function register(): void {`; it branches on `isAdmin()` (:396) and `doingAjax()` (:435). |
| 19–21 | The other modules keep their callbacks, and most of them get their hooks from here | CONFIRMED | `register()`, `registerBackEnd()`, `registerFrontEnd()` and `addMenuPages()` call `register()`/`registerHooks()` on ~24 collaborators (`HookRegistrar.php:408-413, 520-527, 652-660`). |
| 23–26 | Lifecycle callbacks: translation, compatibility declaration, plugin list links, packet actions of a query parameter [VERIFY: PluginHooks.php#handleActions] | CONFIRMED | `PluginHooks.php`: `loadTranslation` :86, `declareWooCommerceCompability` :74, `addPluginActionLinks` :108, `addPluginRowMeta` :136, `handleActions` :152. The anchor covers only the last item; the other three are in the same file. |
| 25–26 | The module holds 3 files, 562 lines of logic and 16 public methods | CONFIRMED | Measured above; `ai-docs/manifest.yaml:168` records the same `lines: 562`. |
| 28 | `> ⚠ add business context (elicitation)` | PENDING | Standard placeholder. |
| 32 | The module exposes one entry point and the callbacks that WordPress calls directly (summary of the table below) | INCORRECT | The table lists two private methods — `private function registerBackEnd(): void {` (:426) and `private function registerFrontEnd(): void {` (:595) — and omits three public callbacks that WordPress calls directly: `declareWooCommerceCompability` (`before_woocommerce_init`, :383), `addPluginActionLinks` and `addPluginRowMeta` (`plugin_action_links_*`, `plugin_row_meta`, :625-628). |
| 36 | Entry point `register` [VERIFY: HookRegistrar.php:372] | CONFIRMED | `HookRegistrar.php:372`; called from `src/Packetery/Module/Plugin.php:55`. |
| 37 | `registerBackEnd` is a member of the public interface [VERIFY: HookRegistrar.php#registerBackEnd] | INCORRECT | `private function registerBackEnd(): void {` (`HookRegistrar.php:426`). The behaviour text is right, the exposure is not: nothing outside the class can call it. |
| 38 | `registerFrontEnd` is a member of the public interface [VERIFY: HookRegistrar.php#registerFrontEnd] | INCORRECT | `private function registerFrontEnd(): void {` (`HookRegistrar.php:595`). |
| 39 | `addMenuPages` adds the dashboard, the settings, the carriers, the label pages and the log | CONFIRMED | `HookRegistrar.php:652-660`: `optionsPage->registerMenuPage()`, `dashboardPage->register()`, `optionsPage->registerSubmenuPage()`, `carrierOptionsPage->register()`, `labelPrint->register()`, `orderCollectionPrint->register()`, `logPage->register()`. |
| 40 | `addShippingMethods` adds the Packeta method to the WooCommerce method list | CONFIRMED | `HookRegistrar.php:639-650`; `$methods[ ShippingMethod::PACKETERY_METHOD_ID ] = ShippingMethod::class;`. |
| 41 | `addLinksToPluginGrid` adds the settings link and the documentation link | CONFIRMED | `HookRegistrar.php:624-630` registers `plugin_action_links_*` → `addPluginActionLinks` (settings link, `PluginHooks.php:108`) and `plugin_row_meta` → `addPluginRowMeta` (documentation link, `PluginHooks.php:136`). |
| 42 | `activatePlugin`, `redirectAfterActivation` mark the activation and open the dashboard once | CONFIRMED | `HookRegistrar.php:662-671`: sets `OptionNames::PACKETERY_ACTIVATED`, then deletes it and redirects to `DashboardPage::SLUG`. |
| 43 | `handleActions` runs the submit, claim and cancel action of a packet from a query parameter | CONFIRMED | `PluginHooks.php:152-166`, three branches on `ACTION_SUBMIT_PACKET`, `ACTION_SUBMIT_PACKET_CLAIM`, `ACTION_CANCEL_PACKET`. |
| 44 | `loadTranslation` loads the translation file for the current locale | CONFIRMED | `PluginHooks.php:86-104`; reads `$this->moduleHelper->getLocale()` and falls back to `loadDefaultTextDomain()`. |
| 45 | `updateOrder` saves the Packeta fields, or deletes the Packeta row when the order lost the method | CONFIRMED | `UpdateOrderHook.php:64-86`; `delete()` on `! ShippingProvider::wcOrderHasOurMethod( $wcOrder )`, otherwise `saveFields()`. |
| 49–50 | The plugin first tests WooCommerce [VERIFY: HookRegistrar.php:372] | INCORRECT | The test is second. `HookRegistrar.php:373` runs `$this->wpAdapter->addAction( 'init', [ $this->pluginHooks, 'loadTranslation' ] );` before `if ( $this->moduleHelper->isWooCommercePluginActive() === false ) {` at :375. |
| 50–51 | Without WooCommerce the module shows one admin notice and registers nothing more | INCORRECT | The notice is inside `if ( $this->wpAdapter->isAdmin() ) {` (:376), so on the front end nothing is shown; and the `init`/`loadTranslation` action from :373 stays registered. |
| 55 | Every request: translation, HPOS compatibility, upgrade check, REST routes, cron service, packet auto submission, status synchronisation, order save, shipping method list | CONFIRMED | `HookRegistrar.php:373, 383, 384, 385, 408-413, 407`. |
| 56 | Plugin lifecycle: activation hook, deactivation hook that stops the scheduled actions | CONFIRMED | `HookRegistrar.php:387-395`; `CronService::deactivate()` runs five `as_unschedule_action()` calls (`src/Packetery/Module/CronService.php:126-132`). |
| 57 | Admin, AJAX: the settings callback under `wp_ajax_get_settings` and `wp_ajax_nopriv_get_settings`, **and nothing else** | INCORRECT | Six grid filters and `$this->productCategoryFormFields->register();` run at `HookRegistrar.php:427-433`, before the AJAX branch `if ( $this->wpAdapter->doingAjax() === true ) {` at :435. The AJAX request therefore also carries the product and category column hooks. |
| 58 | Admin, order grid: filter links, order type select, columns, sortable columns, column content | CONFIRMED | `HookRegistrar.php:458-461, 479-520`. |
| 59 | Admin, bulk actions with priority 20 and their handlers | CONFIRMED | `HookRegistrar.php:541-556`: `addActions` with `20`, `handleActions` with `10, 3`. |
| 60 | Admin, actions: label print, handover protocol, settings export, log deletion, packet actions, all on `admin_init` | CONFIRMED | `HookRegistrar.php:570-576` plus `admin_init` → `redirectAfterActivation` (:448). |
| 61 | Admin, screens: metaboxes, modal windows, product and category grids, product data tab, dashboard widget [VERIFY: HookRegistrar.php#addMenuPages] | INCORRECT | Anchor does not support the claim. `addMenuPages` (:652-660) only registers menu and submenu pages. The listed items are registered in `registerBackEnd`: `$this->metaboxesWrapper->register();`, `$this->orderModal->register();`, `$this->productTab->register();`, `$this->dashboardWidget->register();` (:524-528, :578) and the grid filters at :427-433. |
| 62 | Front end: checkout hooks, front assets, cart fees with priority 20, guest session migration | CONFIRMED | `HookRegistrar.php:596-613`; `woocommerce_cart_calculate_fees` with `20`, `woocommerce_guest_session_to_user_id`. |
| 63 | Front end, no AJAX: customer order detail and the block checkout callbacks | CONFIRMED | `HookRegistrar.php:615-621`, inside `if ( $this->wpAdapter->doingAjax() === false )`. |
| 64 | Email: the footer callback of the configured email hook when automatic insertion is enabled | CONFIRMED | `HookRegistrar.php:402-405`: `getEmailHook() !== '' && isAutoEmailInfoInsertionEnabled()` → `addAction( $wcEmailHook, [ $this->viewMail, 'renderEmailFooter' ] )`. |
| 66–68 | The module registers the classic shape and, from WooCommerce 7.9.0, the shape of the HPOS orders page | INCORRECT | The `version_compare( $wooCommerceVersion, '7.9.0', '>=' )` branch (:467-476) gates only `views_woocommerce_page_wc-orders` and `woocommerce_order_list_table_restrict_manage_orders`. The columns, custom column content, sortable columns and bulk actions of `woocommerce_page_wc-orders` are registered unconditionally at :480-520 and :542-556. |
| 68–70 | Admin notices run with a very low priority so they stand above other plugins [VERIFY: HookRegistrar.php#MIN_LISTENER_PRIORITY] | CONFIRMED | `private const MIN_LISTENER_PRIORITY = - 9998;` (:52), used on both `admin_notices` registrations (:451-457, :558-565). |
| 74 | packetery-module-hooks depends on **every** module that has a callback (summary of the list below) | INCORRECT | The list omits nine modules the code imports: `packetery-module-framework` (`WpAdapter`, `WcAdapter` in all three files), `packetery-module-blocks` (`Blocks\BlockHooks`), `packetery-module-dashboard` (`Dashboard\DashboardPage`), `packetery-module-diagnosticslogger`, `packetery-module-email` (`Email\EmailShortcodes`), `packetery-module-log`, `packetery-module-product`, `packetery-module-productcategory`, `packetery-module-upgrade` — all in the `use` block of `HookRegistrar.php:7-48`. |
| 76 | references → packetery-core | INCORRECT | No file of the module references `src/Packetery/Core`: `grep -rn 'Packetery.Core' src/Packetery/Module/Hooks/` returns nothing, and no `use Packetery\Core…` appears in the three files. |
| 77 | references → packetery-module-root | CONFIRMED | `use Packetery\Module\CronService`, `ModuleHelper`, `MessageManager`, `QueryProcessor`, `ShippingMethod`, `DashboardWidget` (`HookRegistrar.php:13-42`), path `src/Packetery/Module`. |
| 78 | references → packetery-module-order | CONFIRMED | `use Packetery\Module\Order\…` (`HookRegistrar.php:26-35`), `UpdateOrderHook.php:12`. |
| 79 | references → packetery-module-checkout | CONFIRMED | `use Packetery\Module\Checkout\Checkout`, `CheckoutSettings`, `CheckoutStorage` (:11-13). |
| 80 | references → packetery-module-carrier | CONFIRMED | `use Packetery\Module\Carrier\OptionsPage` (:9). |
| 81 | references → packetery-module-options | CONFIRMED | `use Packetery\Module\Options`, `Options\OptionNames`, `Options\OptionsProvider` (:22-24). |
| 82 | references → packetery-module-api | CONFIRMED | `use Packetery\Module\Api` (:7); `rest_api_init` → `apiRegistrar->registerRoutes` (:385). |
| 83 | references → packetery-module-shipping | CONFIRMED | `use Packetery\Module\Shipping\ShippingProvider` (:41), `UpdateOrderHook.php:13`. |
| 84 | references → packetery-module-views | CONFIRMED | `use Packetery\Module\Views\AssetManager`, `ViewAdmin`, `ViewFrontend`, `ViewMail`, `WizardAssetManager` (:44-48). |
| 86–88 | The module calls the registration method of the modules that keep their own map. **These are** the cron service, the checkout, the metaboxes, the modal windows, the settings page and the carrier settings page | INCORRECT | The set is much larger. `register()`/`registerBackEnd()`/`registerFrontEnd()`/`addMenuPages()` also call `register()` on `packetAutoSubmitter`, `apiExtender`, `updateOrderHook`, `packetSynchronizer`, `shortcodes`, `packetSubmitter->registerCronAction()` (:409-413), `productCategoryFormFields` (:433), `queryProcessor` (:478), `productTab` (:527), `dashboardWidget` (:578), `blockHooks` (:618), `dashboardPage`, `labelPrint`, `orderCollectionPrint`, `logPage` (:653-659). |
| 88–90 | The module reads the email hook and the insertion setting from packetery-module-options | CONFIRMED | `HookRegistrar.php:402-403`: `$this->optionsProvider->getEmailHook()`, `isAutoEmailInfoInsertionEnabled()`. |
| 90–92 | The order save callback uses the order repository of packetery-module-order and the method test of packetery-module-shipping | CONFIRMED | `UpdateOrderHook.php:70` `$this->orderRepository->getWcOrderById(…)`, :75 `ShippingProvider::wcOrderHasOurMethod( $wcOrder )`. |
| 96–98 | The map of the whole plugin sits in one class, so **every new callback of any module changes this file** | INCORRECT | Modules keep their own maps. `src/Packetery/Module/Order/Metabox.php:154-155` `add_action( 'add_meta_boxes', … )`, `src/Packetery/Module/Checkout/Checkout.php#registerHooks` (six registrations), `src/Packetery/Module/Log/ArgumentTypeErrorLogger.php:51`. A callback added there leaves `HookRegistrar.php` untouched. The sentence also contradicts line 86 of this same page. |
| 98–100 | The WooCommerce version that brings the second grid shape is a hardcoded string | CONFIRMED | `HookRegistrar.php:467`: `version_compare( $wooCommerceVersion, '7.9.0', '>=' )`. |
| 100–101 | The screen identifier of that grid is a hardcoded string of the same method | CONFIRMED | `HookRegistrar.php:464`: `$orderListScreenId = 'woocommerce_page_wc-orders';`, inside `registerBackEnd`. |
| 103–105 | The order save callback runs once per request, a static variable holds that state | CONFIRMED | `UpdateOrderHook.php:65` `static $hasBeenRun;`, guarded at :67-69 and set at :78 and :85. |
| 105–106 | An order that loses the Packeta method also loses its Packeta row in the same callback | CONFIRMED | `UpdateOrderHook.php:75-80`: `$this->orderRepository->delete( (int) $wcOrderId );`. |
| 106–108 | One block callback cannot move to the block module and a comment says so | CONFIRMED | `HookRegistrar.php:619`: `// Cannot be moved to BlockHooks register.` above `woocommerce_blocks_loaded`. |
| 108–109 | The module contains no TODO comment and no FIXME comment | CONFIRMED | `grep -rnE 'TODO|FIXME' src/Packetery/Module/Hooks/` returns 0 hits. |

CONFIRMED 37 · INCORRECT 13 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 1 · SENSITIVE 0 · INSTRUCTION 0

### manifest.part.packetery-module-hooks.yaml

The separate fragment no longer exists; the entries live in `ai-docs/manifest.yaml`.
Checked: the `modules:` record (`ai-docs/manifest.yaml:166-169`), every `exposes:` entry with
`module: packetery-module-hooks` (:455, :576, :626) and every `unknowns:` entry with
`resolved_by: packetery-module-hooks` (:752, :762). The `modules:` schema of this manifest has
`name`/`path`/`lines`/`doc` only — no `evidence:` key on any module record, so there is nothing to
verify under that name. `packetery-module-hooks` also appears in the `aliases:` list (:13); that is
a navigation list, not a claim.

| entry | class | evidence |
|---|---|---|
| `modules[packetery-module-hooks].path: src/Packetery/Module/Hooks` | CONFIRMED | The directory holds the three files of the module and nothing else. |
| `modules[packetery-module-hooks].lines: 562` | CONFIRMED | Non-empty non-comment lines of the three `src/` files = 562; matches the document. |
| `modules[packetery-module-hooks].doc: reference/packetery-module-hooks.md` | CONFIRMED | The file under review exists at that path and carries `module: packetery-module-hooks`. |
| `exposes[] register` — kind `library`, consumers `woocommerce`, auth `none`, evidence `HookRegistrar.php:372`, notes "It registers the hooks of every module for the current request" | INCORRECT | The evidence line is right (`public function register(): void {` at :372), the note is not: 25 files outside `src/Packetery/Module/Hooks` register WordPress hooks themselves, among them `src/Packetery/Module/Log/ArgumentTypeErrorLogger.php:51`, which `HookRegistrar.php` never names. The caller is also the plugin bootstrap, not WooCommerce: `src/Packetery/Module/Plugin.php:55` `$this->hookRegistrar->register();`. |
| `exposes[] handleActions` — kind `other`, consumers `woocommerce`, auth `unknown`, evidence `PluginHooks.php#handleActions`, notes "on admin_init, nonce check in the packet action" | CONFIRMED | `PluginHooks.php:152`; registered by `HookRegistrar.php:576` `addAction( 'admin_init', [ $this->pluginHooks, 'handleActions' ] )`; the nonce check is `wp_verify_nonce(…)` at `src/Packetery/Module/Order/PacketActionsCommonLogic.php:84`. `auth: unknown` is conservative given that check, but the note itself points at it. |
| `exposes[] updateOrder` — kind `other`, consumers `woocommerce`, auth `none`, evidence `UpdateOrderHook.php#updateOrder`, notes "Saves the Packeta fields after WooCommerce saves it" | CONFIRMED | `UpdateOrderHook.php:64`; hooked at :54 `add_action( 'woocommerce_update_order', [ $this, 'updateOrder' ] )`. |
| `unknowns[] "Where WordPress calls the registration of these routes." resolved_by: packetery-module-hooks` | CONFIRMED | `HookRegistrar.php:385`: `addAction( 'rest_api_init', [ $this->apiRegistrar, 'registerRoutes' ] )`. |
| `unknowns[] "Where WordPress registers the method list and the class loading of this module." resolved_by: packetery-module-hooks` | CONFIRMED | `HookRegistrar.php:407` `addFilter( 'woocommerce_shipping_methods', [ $this, 'addShippingMethods' ] )` and :416 `add_action( 'init', [ $this->shippingProvider, 'loadClasses' ] )`. |

CONFIRMED 7 · INCORRECT 1 · UNCERTAIN 0 · NOT FOUND 0 · PENDING 0 · SENSITIVE 0 · INSTRUCTION 0
