service: woocommerce
source_commit: e4971ab0
aliases: [packetery-module-hooks]

assumptions:
  - "Generated on branch docs/teams at commit e4971ab0."
  - "Module boundary taken from the PSR-4 namespace Packetery\\Module\\Hooks; the committed ai-docs/ast/map.json was produced without --split-psr4 and at commit 11523b71, so per-module counts come from a --split-psr4 run of the same tool. Test directories are excluded from the counts."
  - "The registration map lists the groups of hooks, not every hook. The class registers more than one hundred callbacks."
  - "The STE skill esterka:writing-technical-docs-in-ste was invoked for the prose of this document."

modules:
  - name: packetery-module-hooks
    path: src/Packetery/Module/Hooks
    lines: 562
    doc: reference/packetery-module-hooks.md

exposes:
  - kind: library
    name: register
    module: packetery-module-hooks
    consumers: [woocommerce]
    auth: none
    evidence: src/Packetery/Module/Hooks/HookRegistrar.php#register
    notes: "Entry point of the plugin. It registers the hooks of every module for the current request."
  - kind: other
    name: handleActions
    module: packetery-module-hooks
    consumers: [woocommerce]
    auth: unknown
    evidence: src/Packetery/Module/Hooks/PluginHooks.php#handleActions
    notes: "Runs a packet action from a query parameter on admin_init. The nonce check lives in the packet action itself."
  - kind: other
    name: updateOrder
    module: packetery-module-hooks
    consumers: [woocommerce]
    auth: none
    evidence: src/Packetery/Module/Hooks/UpdateOrderHook.php#updateOrder
    notes: "Saves the Packeta fields of an order after WooCommerce saves it."

unknowns:
  - question: Which callbacks the plugin must keep active when WooCommerce is not active.
    checked: the early return of the registration entry point
    resolved_by: a person owning the plugin
    blocks: "-"
  - question: Whether the order grid of the classic shape must stay supported.
    checked: the two shapes that the module registers, and the hardcoded WooCommerce version
    resolved_by: a person owning the plugin release
    blocks: "-"
