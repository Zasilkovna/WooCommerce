service: woocommerce
source_commit: b36b68ad
aliases: [packetery-module-log]

assumptions:
  - "Generated on branch docs/teams at commit b36b68ad."
  - "Module boundary taken from the PSR-4 namespace Packetery\\Module\\Log; per-module counts come from a --split-psr4 run of the ast-map tool. Test directories are excluded from the counts."
  - "The STE skill esterka:writing-technical-docs-in-ste was invoked for the prose of this document."

modules:
  - name: packetery-module-log
    path: src/Packetery/Module/Log
    lines: 727
    doc: reference/packetery-module-log.md

exposes:
  - kind: library
    name: add
    module: packetery-module-log
    consumers: [woocommerce]
    auth: none
    evidence: src/Packetery/Module/Log/DbLogger.php#add
    notes: "The logger of the plugin. Every module writes its API results through it."
  - kind: html
    name: packeta-logs
    module: packetery-module-log
    consumers: [woocommerce]
    auth: wordpress-capability
    evidence: src/Packetery/Module/Log/Page.php#SLUG
    notes: "Admin log page with a filter. It needs the manage_woocommerce capability."

owns_data:
  - entity: log
    store: mysql
    module: packetery-module-log
    evidence: src/Packetery/Module/Log/Repository.php#createOrAlterTable
    notes: "One table of log records. It holds no index other than the primary key."

unknowns:
  - question: How long a shop must keep the log records.
    checked: the default window of the purger and the option that holds it
    resolved_by: a person owning the plugin operation
    blocks: "-"
  - question: Which module gives the size limit and the age limit of the diagnostic file.
    checked: the constructor of the size limiter, which takes both values
    resolved_by: packetery-module-root
    blocks: "-"
